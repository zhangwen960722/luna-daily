# 变更记录 — 仓库管理

| 日期 | 变更内容 | 变更原因 |
|------|---------|---------|
| 2026-09-23 | 三份文档加分工声明；RDD 字段表删「类型」列（字段结构归数据设计）；数据设计清理越位（业务规则/校验改引用 PRD、设计决策改写为建模约定） | 落实文档职责边界，消除跨文档重复定义 |
| 2026-09-16 | **「新增/编辑港口」弹窗的「类型」下拉切换无反应** —— `handleTypeChange`（切换类型时清空港口代码）已定义但**漏写进 `setup()` 的 return**，模板 `@change="handleTypeChange"` 取不到 → 补回导出；由新增的 `.agent/scripts/check-setup-refs.js` 全仓扫描发现 | 模板用了、setup 没返回 → 绑定静默失效（点了没反应），语法检查与模板作用域检查都抓不到 |
| 2026-09-16 | **原型图标写法修复（全仓清理的其中 3 页：仓库列表_主列表 / 海外仓库列表_主列表 / 港口管理_主列表）** —— **数字占位标签**（按钮上直接显示乱字符）与 **PascalCase 自闭合图标**（图标静默丢失）改为真图标 / kebab-case 显式闭合；规范见 `style-guide.md` §原型图标写法 | 原型是**浏览器内联模板**，HTML 解析器先于 Vue 工作 —— 数字开头的标签名属非法 HTML（被降级成纯文本、直接显示在按钮上）、PascalCase 自闭合会被小写化（Vue 找不到组件、图标静默丢失）；规范见 `style-guide.md` §原型图标写法 |
| 2026-09-10 | 平台仓库列表去掉「确认数据」功能及「数据来源」「确认状态」两个字段（含确认状态流转、批量确认接口、相关校验规则），列表改为纯查询+新增/编辑；RDD/PRD/数据设计/原型同步 | 确认机制使用频率低且与冻结状态语义重叠，去掉后降低维护成本 |
| 2026-09-08 | 平台仓库列表：FBA/其他平台双Tab合并为单一列表（去Tab表头），platform字段改下拉枚举（Amazon/Walmart/eBay/SHEIN/Temu/TikTok Shop/AliExpress/Mercado Libre/OTTO/Newegg/Shopify，FBA归入Amazon）；地址信息新增「是否快递承运商专用地址」（必填，是/否）+「快递承运商SCAC」（非必填，多选，选项来自供应商明细含快递的供应商）；RDD/PRD/数据设计/原型同步 | 双Tab切换维护成本高，平台应作为列表字段而非Tab；地址需标记快递承运商专用属性，为运单派送提供SCAC |
| 2026-06-06 | 初稿，基于 Demo 反向生成 | AI PM |
| 2026-06-06 | **v2.0 完整重写**：基于 Excel 源数据（"平台仓库列表"+"仓库列表"）重写 RDD / 数据设计 / PRD。主要变更：(1) 移除港口管理模块及其 `port` 表（Excel 源数据未包含港口内容）；(2) 海外仓库新增能力对齐 Excel：仓库代码唯一性校验、确认状态默认"未确认"、运单系统调用接口说明；(3) 本地仓新增 phone 11位校验、contactName/warehouseName 改为明确必填、操作列独立显示编辑/冻结/启用按钮；(4) 移除角色级权限管控（Excel 源数据标注"无权限管控"）；(5) 实体从4个缩减为3个（移除 Port），表从4张缩减为3张；(6) PRD 页面规格从4个页面缩减为2个核心页面；(7) 枚举字典移除 PortType/PortStatus，保留 WarehouseStatus/ConfirmStatus | 基于 Excel 源数据完整重写，对齐真实业务需求 |
| 2026-06-06 | **v2.1 Demo+Excel 数据融合**：修正 v2.0 错误移除港口管理的问题。主要变更：(1) **恢复港口管理模块**：从 Demo `港口管理_主列表.html` 反向提取 Port 实体字段（portName/portCode/country/type/status），补回 RDD 流程三、数据设计 port 表、PRD 页面规格 5.3；(2) RDD 增加 AC12-AC15 港口验收标准，功能清单新增模块 C（3项功能），MVP从9项增至12项；(3) 数据设计恢复 `port` 表（4张表），ER 图重建 PORT 实体；(4) PRD 恢复 PortType/PortStatus 枚举、Port 状态机、港口页面规格(500px弹窗)、4个接口、5条错误文案、3条验收项；(5) 修正 MVP 范围：删除"港口管理不在本期范围"的错误排除；(6) 发现 Demo `其他平台仓库新增.html` 中存在 `isSupplierOnly`/`supplier` 预埋字段，经确认不纳入本期，后续需从 Demo 中移除 | v2.0 错误地以"Excel无港口Sheet"为由删除港口管理；Demo 作为真相源，港口管理页面确实存在且被运单系统依赖，必须恢复 |
| 2026-08-28 | 港口管理编辑弹窗：港口代码/所属国家/港口类型改为只读，仅港口名称可编辑 | 港口身份标识字段创建后不可变更，防止下游运单引用错乱 |
