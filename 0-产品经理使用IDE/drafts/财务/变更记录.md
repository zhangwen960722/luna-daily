# 变更记录 — 财务模块

> 每天每模块 ≤ 3 条，同日同主题合并。

| 日期 | 变更内容 |
|:---|:---|
| 2026-09-18 | **资金流水时间列统一更名「创建时间」+ 资金账户管理查看流水弹窗补「支付时间」列**：资金流水表的流水时间字段（`operator_at`）页面列名由「流水时间」统一改为「**创建时间**」—— ① 员工端「资金流水」页（客户 / 公司双 Tab）、② 「资金账户管理」的客户资金账户「查看流水」与我司账户「流水」两个只读弹窗、③ 货主端「资金账户 → 资金流水」Tab，三处列名统一；同时补上资金账户管理两个弹窗此前缺失的「支付时间」列（充值 / 代充 = 客户实际打款时间 `pay_time`，其余类型回退展示创建时间），并补上 PRD §5.5 我司账户 Tab 的「流水」入口与「资金流水（我司账户）」弹窗描述。PRD（§5.5 / §5.6 / §5.8 + 接口清单）、数据设计 `wallet_flow`、RDD 资金流水字段说明同步「流水时间 → 创建时间」 |
| 2026-09-18 | **新增「应收业务流水 / 应付业务流水」两个只读台账菜单（方案丙：两独立菜单，非单页双 Tab）**：产品指出应收业务流水（`ar_flow`）没有全局台账、只能按运单在运单审计 / 运单管理里逐票看，补一个「能筛能钻、全量」的业务流水台账。应收台账（`flow_category=10`）一期全量数据；应付台账（`flow_category=20`）**一期空态占位**（数据随 Phase 2 应付管理上线）。定位 = 只读、跨容器全量（待审计 / 已审计未成账 / 已进账单 / 作废释放），**不承载写操作**，与运单审计（写 + 审计）、草稿池（成账）、账单详情（结算）互不取代；数据层零改动（直接查 `ar_flow`，不建新表 / 新状态机，接口复用 `GET /api/finance/ar-flows` 放开 `waybillId` 必填 + 加全局筛选）；下钻：运单号 → 运单审计详情、账单号 → 账单详情、行详情 → 来源规则。RDD v1.9（§3.4 + A12/A13 + AC23/24）、数据设计 v0.12（「业务流水台账」视图）、PRD v1.9（§5.14/§5.15 + 功能清单 + 权限 + 接口 + 决策记录）、原型两页 + index 菜单权限注册。**同日收口（产品反馈）**：全部页面去掉「流水种类」展示（台账 + 账单管理草稿池/生成账单/添加流水 + 贷记单 + 运单审计 + 运单管理 + 货主端运单；`flow_kind` 字段保留，仅不展示、接口不再下发前端）；顺带修掉运单管理应收费用表重复的「审计状态」列（删流水种类时暴露）。**同日再收口（产品给字段清单）**：应收业务流水主列表按 **21 列重排**（流水号 / 运单号 / 客户编号 / 客户昵称 / 客户名称 / 销售代表 / 我司主体 / 账单编号 / 费用类型 / 审计状态 / 币种 / 汇率 / 单价 / 计价单位 / 计费量 / 金额 / 创建人 / 备注 / 描述 / 拣货时间 / 创建时间）+ 工具栏「**导入 / 导出 / 删除**」—— 导入 = 批量把流水导入运单（同运单审计「新增流水」R60，运单「应收」已整票审计不可导入）、删除 = 勾选删流水（R61：已进账单 / 已审计不可删）、导出纯读；`ar_flow` 新增业务字段 `flow_no`（流水号）。RDD §3.4 + AC23 + A12、数据设计（`flow_no` + 台账视图）、PRD §5.14 + 功能清单 + 权限 + 接口 + R60/R61 入口、原型同步。**同日再定（产品确认）**：应收业务流水的「导入 / 删除」与「运单审计 · 新增 / 删除」为**镜像双入口**（同一动作、同一 R60/R61 口径、同一权限与接口校验，防分叉护栏见 PRD §12.4），接受双入口。**同日再定（三者分工钉死）**：产品一度想把应收业务流水并进「账单管理 · 草稿池」以少一个菜单，讨论后定案**三者各司其职、不合并** —— 运单审计（按运单，改账 + 审计）、草稿池（按出账，成账工作台）、应收业务流水（按流水本身，全量查账 + 批量导 / 删 / 补）；草稿池是应收业务流水的「已审计 + 未成账」子集、任务是出账，字段有重叠、任务不同、不合并。PRD §5.14 / §5.2 定位 + §12.4 分工定案、RDD §3.4 同步。**同日再收口（应收业务流水加文件导入 + 单条审计/反审计）**：① 原「导入」按钮改名「**新增流水**」（本就是运单审计「新增流水」R60 的镜像入口，避免与文件导入语义冲突）；② 新增「**导入**」按钮 = 上传文件批量导入流水（**R75**：下载模板 → 上传 .xlsx/.xls/.csv → 逐行校验 → 失败行不落库）；③ 操作列加「**审计 / 反审计**」单条按钮（复用运单审计流水级审计/反审计 R61 第二入口：已关联账单不可审计/反审计、未审计可审计不可反审计、已审计可反审计）。PRD（§5.14 + R75 + R60/R61 入口 + A03c + 权限/接口 + §12.4 决策）、RDD（§3.4 + AC23 + A12）、数据设计（台账视图写动作）、原型同步 |
| 2026-09-18 | **数据来源取值修正：手工新增且未绑规则的行「操作人姓名」→「人工添加」**（`source` 记"这笔钱哪来的"= 报价 / 规则名 / 人工添加，操作人只落在 `created_by`，消除与创建人重复；创建人保留不动）。数据设计（`source` 字段备注 + 落库口径）、PRD（R02/R03/R06/R60/A02 + 运单审计字段矩阵）、RDD（实体字段 + 业务全景）、原型（应收业务流水 2 条 + 运单审计 6 处）同步 |
| 2026-09-17 | **资金流水页查询区移到 Tab 上方**：查询条件与「客户 / 公司」Tab 分离，按当前 Tab 切换条件（客户 Tab 5 条件、公司 Tab 4 条件），两个 Tab 筛选状态各自独立（切换不清空）；控件统一弹性宽度、客户 Tab 的流水类型与时间范围收进「更多查询条件」—— 对齐其它模块「查询区在 Tab 上方」的结构规范。PRD §5.6 查询区位置 + 交互行为同步 |
| 2026-09-17 | **资金流水字段口径：变动金额更名「入账金额」+ 新增「支付时间」列**：① `wallet_flow` 的 `amount`/`company_amount` 更名「入账金额/我司账户入账金额」，员工端资金流水页（客户/公司双 Tab）、货主端资金账户「资金流水」Tab、资金账户管理的钱包/我司账户流水弹窗统一改名；② 资金流水页与货主端资金账户「资金流水」Tab 新增「支付时间」列 —— 充值/代充显示客户实际打款时间（`pay_time`，货主端充值审核通过时由 `recharge_apply.pay_time` 回填），其余流水类型无独立支付事件、支付时间 = 流水时间（页面回退展示）。缘由：支付时间只在充值有独立时点（客户打款可早于财务入池），其余动作发生即入账 |
| 2026-09-17 | **我司主体资金账户 + 双边资金流水定案**：核销/冲账只动客户钱包、不动我司账户（现金在充值已入账）；充值/代充/提现/充值退回走「客户钱包 + 我司账户」双边流水，我司账户 = 资金账户管理 → 我司账户（`bank_account`，余额改系统自动核算、不再手工维护）；核销/批量核销弹窗补「扣款钱包账户名称」；**资金流水页拆「客户 / 公司」两个 Tab**（客户 Tab = 客户钱包流水、公司 Tab = 我司账户流水），我司账户 Tab 加「流水」入口双边查看。数据设计 `wallet_flow` 补我司账户侧三字段 + 资金流向矩阵、`bank_account.balance` 口径、PRD/RDD 资金规则与接口 body、原型（账单管理/资金账户管理/财务流水/资金流水）同步 |
| 2026-09-17 | **充值凭证上传 + 「支付时间」字段（货主端充值 + 员工端代客户充值）**：① 货主端充值、员工端「代客户充值」两个弹窗的打款凭证均支持截图 Ctrl+V 直接粘贴上传（§5.8 充值弹窗 voucher + 代充弹窗 voucher）；② 两个充值弹窗均新增「支付时间」字段（日期选择、必填）；数据设计 `recharge_apply.pay_time` 与 `wallet_flow.pay_time/voucher_url`（代充）同步 |
| 2026-09-17 | **运单审计两层解耦 + 反审计粒度细化（含两处原型修复）**：① 反审计由「按方向整票一锅端」改为**按方向 × 深度**——每个方向独立选「仅运单 / 运单及流水」：仅运单只反运单标记（放开新增、原流水仍锁定），运单及流水反标记 + 该方向「未进账单的」流水回待业务审计（已进账单的流水保持已审计、不反）；权限判定改为**两层解耦**——运单标记只管「新增」、流水标记只管「编辑」，新增不再看流水、编辑不再看运单标记。② 顺带修两处原型 bug：主列表「反审计」点一下同时弹「运单详情 + 反审计」两个弹窗（事件冒泡到行点击）已阻断；整票审计此前只改标记、流水没跟着置「已审计」（仅当前详情票被动过）已修正为逐票把该方向全部流水置状态 |
| 2026-09-17 | **新增「冲账（坏账冲销）」**（R73/R74、A11、AC21/AC22）：客户不付、钱包没钱、收不回来的烂账，财务在「账单管理 → 已发送」行内点「冲账」认亏平账 —— 填金额（默认全额）+ 必填原因 → 二次确认 → 未核销金额清 0、核销状态置「已冲账」、出账龄；写一条「冲账」账面流水（不动钱包余额），可反冲账。缘由：烂账没有结清出口，会永远挂在账龄台账里越滚越多；同日收口：核销记录菜单改名为「结清记录」、冲账也写 `ar_writeoff`（复用结清记录表，反冲账入口移到结清记录菜单，贷记单结转不进本菜单） |
| 2026-09-17 | **贷记单结转写的那条资金流水，类型由「赔付入账」统一更名「贷记单入账」**（`flow_type = 70` 不变，只改类型名）—— 缘由：贷记单承载的负数流水**不一定是赔付**（赔付只是其来源之一），类型名跟单据走更准确；「赔付入账记录」（二期台账）与「赔付入账撤销」（已下线的 80）两个概念不受影响 |
| 2026-09-17 | **清理 R63 / R70 里对已下线「撤销入账」的 3 处残留引用**（「撤销该笔」「重复撤销」）—— 09-16 已定「结转即终态、无撤销」，这些引用指向不存在的动作、与现行口径矛盾 |
| 2026-09-17 | **货主端「账单管理」按单据类型拆「应收账单 / 贷记单」两个 Tab（§5.9 重写）** —— 原货主端「待确认 / 已确认」混排改为按 `bill_type` 拆：应收账单 Tab（核销三列只读 + 确认状态筛选「待确认 / 已确认 / 全部」默认待确认）、贷记单 Tab（「状态」列 = 待确认 / 待结转 / 已结转，无到期日期无核销三列），列全固定、无类型自适应；「已结转」仍派生判定（`flow_type = 70`）、不新增枚举；同步更新 §12.4 决策（推翻 09-16「货主端不拆」口径）与数据设计 `writeoff_status` 展示口径 | 应收与贷记是两个相反资金方向、两个结算出口，混排让贷记单的「结转状态」无处安放 |
| 2026-09-17 | **「核销 / 冲账」设计定案：冲账并入核销弹窗作「尾款冲账」** —— ① 字段 `writeoff_status` 保持「**核销状态**」且**摘掉「已冲账(50)」**（冲账是金额维度、由 `written_off_amount > 0` 表达，不占核销状态位）；② 菜单「结清记录」→「**核销/冲账管理**」；③ `unreceived_amount` 改名「**未结清金额**」、口径修 `bill_amount − received_amount − written_off_amount`；④ **删独立「冲账」按钮**，核销弹窗加「**尾款冲账**」勾选：`尾款冲账金额 = 未结清金额 − 本次核销金额`（自动算）+ 冲账原因必填，同一次操作写核销单 + 冲账单两条 `ar_writeoff`；⑤ 货主端应收账单 Tab = 核销状态 + 已核销 / 已冲账 / 未结清三列金额 | 核销（收到钱）与冲账（认亏）是两笔不同分录、金额不同源，硬塞「已冲账」当核销状态值会让「部分核销 + 尾款冲账」表达不清；改为金额维度 + 核销弹窗内勾选后彻底理顺 |
| 2026-09-17 | **资金流水落地统一流水编号 `flow_no`（`WF+yyyyMMdd+序号`）**：`wallet_flow` 补自增主键 `id` + 流水编号，散落的 `FL`（代充/提现）/`PE`（贷记单结转）两套旧前缀统一为 `WF`；员工端「资金流水」页、货主端「资金账户 → 资金流水」、员工端「客户资金账户 → 查看流水」弹窗三处列表加「流水号」列（与「关联单号」分离：流水号=流水自身编号、关联单号=业务单据号）；顺带校正「资金流水 6 类 → 9 类」过时口径。缘由：资金流水是「事实记录不可改」的账务凭证，此前既无主键也无唯一编号（其余单据表都有 WO/TR/FD 编号），且同一张表的流水编号已以 FL/PE 两种前缀散落两处，反冲账/反核销/贷记单双向互查都拿不出唯一流水标识 |
| 2026-09-16 | **原型展示修正（五项，不改任何业务口径）** —— ① **财务各页图标缺陷清理**：员工端 `财务_财务流水.html` / `财务_资金流水.html` / `财务_资金账户管理.html` / `财务_账龄台账.html` / `财务_运单审计.html` / `财务_账单管理.html` 与货主端 `账单管理.html` / `资金账户.html` 等页面，把**数字占位标签**（按钮上直接显示乱字符）与 **PascalCase 自闭合图标**（被小写化后 Vue 解析不到、图标静默丢失，如重置按钮的刷新箭头）改成正确写法（kebab-case + 显式闭合 / `<component is="…">`）。② **运单审计 / 运单管理「应收 / 应付」悬浮明细小窗加「审计状态」并两端对齐**：两页主列表小窗新增**审计状态**列（列序与页面「费用管理」Tab 一致、tag = 已审计 / 已红冲 / 待业务审计），小窗宽度 600 → **720**；**运单管理（`Waybill.html`）按运单审计搬运同一份演示流水**（原来仅 1 票有数据、其余显示「—」→ 现 **7 票逐行同数**，含多币种 USD 折合 / 部分审计 / 关联账单号），并把「运单应收净额」从**原币直接相加**改为**按行汇率折合**（与 R62 一致）。文档：PRD §5.1 列清单 / **R62 ③**（悬浮小表固定列 3 → 4）/ §5.10 新增「主列表应收 / 应付悬浮小窗两端一致」；图标规范见 `style-guide.md` §原型图标写法 —— **缘由**：① 图标标签写在浏览器内联模板里会直接决定渲染结果（数字开头的标签名属非法 HTML、会被当纯文本显示；PascalCase 自闭合会被小写化、图标消失）；② 两端看同一票运单却给出不同数字与不同明细（运单管理大部分行是「—」，且 005 那票的审计状态 / 账单号与运单审计不一致），属演示数据不同源 —— 口径统一到财务侧权威页（运单审计） ；**③ 货主端「账单管理」超期自动确认提示改成一整行纯文字**：「待确认」Tab 内、**主列表正上方**这条提示，原来是完整的 `el-alert`（自带边框 + 底色，并带左侧信息图标——夹在 Tab 行与表格之间像在"包裹"列表，图标还贴在最左边缘、正好压在主列表左上角，像一个图标单独杵在列表前面），现改为**一行 13px 浅灰文字**（`.auto-confirm-note`，`<div>` 直接渲染：无图标、无边框、无底色），**位置不变**（仍在「待确认」Tab 主列表上方、只在该 Tab 出现）；逐单的自动确认时间仍由「账单详情 → 确认信息」逐单给出|
| 2026-09-16 | **账单导出（入口 / 范围 / 交付 / 模板分类）** —— ① **入口**：「导出」按钮从查询区**移入「已发送」Tab 工具栏**（与「批量核销」同排，导出靠左、批量核销靠右），**查询区不再留导出**（同一动作不留两个入口；查询区被 5 个 Tab 共用，留在那里等于在草稿池 / 待发送 / 已作废 / 核销记录 4 个 Tab 上摆假入口）。② **范围 = 该 Tab 当前查询结果的全量已发送账单、跨客户**，不选客户、不勾账单（要"只导某几张"用查询条件收窄）—— 账单本身即「单一客户 × 单一主体 × 单一币种」，所以"跨客户"与"混装"是两件事，落盘天然不混装、不需要跨币种折合；③ **交付 = 一张账单一个 Excel**（不按客户 / 主体 / 币种合并）；④ **模板分类由财务在弹窗里选**（**常规模板** / **出口退税模板**，取自财务提供的两份模板文件 `analysis/财务导出模板/`）；⑤ **异步任务** —— 页面只负责提交，**进度与结果文件在「后台任务」**下载。**一期只做可演示链路**（选分类 → 提交 → 后台任务可见 → 结果可下载），真实套模板出 Excel 归 Phase 2。文档：PRD 新增 **R71** + §5.11 页面规格 + A26/A32 验收 + §9 接口 + §12.4 决策，U03 改写；RDD 新增业务规则⑩ + AC20 + 功能清单 A8；数据设计补「账单导出（不建表）」节；原型 `财务_账单管理.html` 按钮搬家 + 弹窗 7、`后台任务.html` 支持导出任务。**同日收口（产品实测反馈）**：① **改为「勾选前置」** —— 「已发送」列表最左加勾选列，「导出」按钮未勾选时置灰、勾选后按钮恢复可用（**按钮文案恒为「导出」，不带张数**），**范围 = 勾选的那几张**（**不再默认全量导出**；可跨客户勾选；勾选仅对当前列表有效，翻页 / 切 Tab / 改查询条件后不在结果里的行不计入）；与「批量核销」（写动作、从钱包进）形成分工：只读动作给勾选这种最省事的交互；② **页面去掉两处说明文字** —— 弹窗里的「导出范围」灰底块与模板口径灰字全撤，弹窗收敛为「只选模板分类 + 确认按钮带张数」（口径仍在 PRD §5.11，页面不承载说明）；③ style-guide 补「列表勾选前置」规范；④ **导出结果文件的交付口径定案** —— 一次导出**只给一个下载入口**，结果 = **一个 zip 压缩包**（内含每张账单一个 `{账单编号}_{客户名称}.xlsx`），**只勾 1 张时不打包、直出 xlsx**，**有失败时包内另附「导出失败清单.xlsx」**（保证"一次下载拿全"），**结果文件保留 7 天**（任务记录长期保留）、过期后「下载导出结果」置灰 + 悬浮「结果文件已过期，请重新导出」  —— 缘由： 财务需要一个把账单"拿出去"的出口（导出发给客户对账 / 客户拿数据去退税）；导出是异步批量动作，进度与结果文件交给已有的「后台任务」承载，不在业务列表里堆 N 个下载按钮 |
| 2026-09-16 | **账单管理：已作废账单详情去掉与「操作时间线」重复的留痕行** —— 详情头部原本另起一行展示「作废人 / 作废时间 / 作废原因」，与同弹窗内「操作时间线」的「作废账单」条目（时间 / 操作人 / 原因）逐字重复；现撤掉该行，作废留痕只由操作时间线承载，头部只留红色「已作废」tag。缘由：同一份留痕在同一个弹窗里出现两遍，既白占版面，后续任何一侧调整都会造成两处口径不一致 |
| 2026-09-15 | **运单审计 ↔ 账单归属互跳（内部侧）**：运单审计页三处账单号（主列表「关联账单」列、运单详情 · 费用 tab 流水表、运单详情「关联账单」描述项）改为**可点击**，点击以新标签页打开「账单管理」并**自动切到该账单所在 Tab、打开账单详情**（`?billNo={账单号}`）；一票关联多张账单时主列表展示**首张**并在 tooltip 补「本票关联 N 张账单」。缘由：「一个运单可关联多张账单」是本轮新口径（同一运单的不同流水可拆到不同账单），财务在审计页看到账单号后原本只能手工切页再搜账单号，跨页核对断链；**对外侧不变** —— 货主端仍不展示账单号、也不提供该跳转 |
| 2026-09-15 | **账单管理整体口径重定（产品复盘后重写 PRD §5.2 / 数据设计 §4.7-4.8 / RDD 流程二）**：① **自动出账** —— 账单周期 = 固定（月结 / 双月结 / 三月结）的客户由系统**每月 1 号**跑批自动出账（账单日 = 1 号）并**直接进「待发送」**，账单周期 = 不固定（周结 / 到港前结 / 签收月结 / 到海外仓结 / 签收结）的客户仍由财务在草稿池手工成账；组内无可成账流水**不生成空账单**、合同账期推不出则**跳过该组并给异常清单**、同 客户×主体×币种 同月**幂等**（不重复出账）。② **成账颗粒度改为流水级** —— 草稿池由「按运单聚合的分组行」改为**逐条流水的列表**，财务逐条勾选成账，**同一运单的不同流水可落不同账单**，因此**一个运单可关联多张账单**（缘由：后续新审计通过的流水与赔付抵扣流水天然进下一期，整票成账会把这些流水拦在池里出不去）。③ **入池门槛 = 已审计** —— 未审计流水**不进池、页面也不给未审提示**（原「可成账金额 / 待审计金额」两列取消，要看未审流水走运单审计）。④ **赔付（理赔）一期降级为一条负数应收流水** —— 业务在运单费用 tab 手工新增「赔付类型」负数流水（费用类型 is_credit=true），走运单审计后随账单抵扣运费；**「放在下一个账单抵扣」由流水生成时点天然实现**（本期账单已生成 → 自然落下一期）；**入账记录 `credit_record` 整条链与货主端「入账记录」Tab 归二期**（赔付管理立项时一并落地）。⑤ **到期日改为账单级** —— 原挂在流水上、按「拣货时间 + 合同账期 + 天数/号」算，现改为：自动出账账单日 = 1 号，**到期日 = 账单日 + 账期长度 − 1 天**（账期 = 合同推自然月：月结 1 / 双月结 2 / 三月结 3 个月，按「同日对齐、无对应日取月末」；推不出时用财务补录天数），**账期天数快照 = 到期日 − 账单日 + 1**，账龄公式（账单日 + 账期天数）**不变**；数据层 `ar_flow.due_date` 移除、`ar_bill` 新增 `due_date` 与 `generate_source`。⑥ **账单管理只到「发送」为止** —— 去掉「已核销」Tab（Tab 固定 4 个：草稿池 / 待发送 / 已发送 / 已作废），**发出后的账单一律留在「已发送」Tab** 靠「账单状态」列区分（已发送 / 已确认 / 部分核销 / 已核销 / 已反核销），**核销金额、核销单号、核销记录一律不在本页展示**（核销动作与明细全部在「收款核销」）。⑦ **字段按产品逐条指定重排** —— 草稿池 19 列（新增 **我司主体** / **创建时间**）、待发送与已发送与已作废 9-10 列（新增 **到期日期** / 客户昵称 / 客户编号）、**账单详情 26 列**（新增 **扩展单号** / **转单号** / **产品数量** / 收费重 / 体积 / 报关方式 / 商品主品名 / 实重 / 材重 / 泡比 + **审计状态**）。⑧ 同日早前的三条口径沿用：**R63 账单作废与重开**、**R64 账单详情的红冲行与金额构成**（已红冲行灰态 + 删除线 + tag、账单头「含红冲流水」、明细底部给正数 / 负数 / 净额构成、原金额不追溯）、**账单不跨币种折合**（混币种运单拆多张账单）。**同日追加（产品逐条指定）**：⑨ **草稿池列表扩充**（加 客户名称 / 账期 / 仓库代码 / 收件人 / 邮编 / 国家，共 26 列）、**账单列表与详情加「账期」**、**详情明细加「仓库代码」**；⑩ **生成账单加前置校验** —— 勾选流水必须「同一客户 + 同一我司主体 + 同一币种」，弹窗内 客户 / 我司主体 / 币种 **只读带出**、字段名统一为「到期日期」且不可编辑；⑪ **新增 R66：待发送阶段可把单条流水移出账单**（流水回草稿池、账单金额重算、账单号不变、**至少保留 1 条**，否则须走作废）；⑫ 撤掉页面级口径说明条；**同日第三批（产品逐条指定）**：⑬ 草稿池「**账期**」列**去掉账单周期前缀**（只显示 `月结` / `周结` 这类签约账期名，账单侧显示口径见下第五批）；⑭ 草稿池**查询区重排为 8 个条件**（账期 / 客户昵称 / 客户编号 / 我司主体 / 币种 / 运单号 / 客户单号 / 拣货时间）并**样式对齐「运单审计」**（控件统一 240px、条件文字写在 placeholder 不带 label、默认只露 3 个其余进「更多」、点查询后收起）；⑮ **「关联单号」全面替换为「FBA 单号」** —— 运单审计（主列表列 + 查询条件）与账单草稿池（列表列）三处同时改；⑯ 账单详情明细的**「扩展单号」列改为「FBA 单号」**，头程管理数据设计的运单级字段登记同步由 `ext_no` 换成 `fba_no`（多货件运单的 FBA 单号**取数时按运单汇总、以 ` / ` 拼接展示**）；**同日第四批（红冲口径撤除）**：⑰ **取消「红冲」机制** —— 早年设计的「已进账单流水被反审计后自动生成负数冲抵+正数重开、原流水置 `已红冲`」整体撤除（`已红冲` 状态、`is_reversal`/`reversal_of_id` 字段、R09 红冲纠错、R64 红冲行展示、A07/AC4 全部改写）；统一为 **「已进账单的流水彻底冻结、不可反审计；要冲减 → 在该运单手工新增一条负数流水（走审计）→ 进草稿池 → 于下一期账单抵扣」**（与赔付抵扣同一机制）；账单详情**去掉「审计状态」列与「含红冲流水」提示**（明细 27→26 列）、**保留「金额构成」（正数/负数/净额）**；**同日第二批（产品再拍）**：⑬ **账单状态拆成「三个独立维度 + 一个作废标记」**（发送 / 确认 / 核销 + 作废）—— 原单枚 `status`（已生成→已发送→已确认→部分核销→已核销→已反核销→已作废）表达不了「已发送但未确认」这类组合、页面也分不清；**Tab 只按发送维度与作废标记分**（待发送 = 未发送 / 已发送 = 已发送 / 已作废），**已发送 Tab 只展示「发送状态 + 确认状态」两列**，**核销维度一律不在账单管理出现**（核销动作与明细在「收款核销」），作废前置判据改为「核销状态 = 未核销」；⑭ **「账期」= 签约账期 + 天数/号**（草稿池实时取合同、只显示签约账期名如 `月结`、账期段为空显示 `待补录`；账单侧为快照 `月结+15`；账期天数 = 到期日 − 账单日 + 1 为派生值、仅供账龄）；⑮ **取消账期补录**（`term_source` 字段取消）—— **合同账期为空 / 推不出到期日一律不能出账**（自动出账跳过该组并进异常清单；财务手工成账时直接阻断「该客户在该主体下的合同账期未完善，请先在客商中心补全合同账期」），账期主数据唯一入口 = 客商中心。**同日第五批（账期显示与到期日算法终定）**：⑯ **账单侧「账期」统一显示为「签约账期 + 天数/号」** —— 待发送 / 已发送 / 已作废 的**列表「账期」列、账单详情「账单信息」、生成账单弹窗**由原 `固定 / 月结（30 天）` 改为 **`月结+15`**（`+` 直连、无空格，不再显示账单周期前缀与「（N 天）」）；草稿池只显示签约账期名（`月结` / `待补录`）；生成账单弹窗**去掉「账期天数」只读项**。⑰ **到期日算法改为按账期类型区分** —— 原「账单日 + 账期长度 − 1 天（推自然月、同日对齐）」作废：**按月类**（月结 / 双月结 / 三月结）=「账单月（账单日所在月 + 结算月 0/1/2）的第「天数/号」号」（`0` = 该月月末、该号不晚于账单日 → **顺延下一月同一号**、该月无该号 → 取月末）；**按天类**（周结）= 账单日 + 天数/号 − 1 天；**事件型**（到港前结 / 签收月结 / 到海外仓结 / 签收结）推不出到期日 → **不允许出账**。缘由：客户合同上写的是「月结 + 15 号」这类**「号」**而非天数，按天数推会与客户认知的付款日错开。账单快照新增 **`days_or_date`（天数/号）**，`payment_terms_days` 改为「到期日 − 账单日 + 1」的**派生值、仅供账龄台账起算、页面不展示**；账龄示例同步为「账单日 09-01 + 月结+15 → 到期日 09-15、逾期第 1 天 09-16」。⑱ **显示与拦截细则**：**`天数/号 = 0` 页面显示为 `月末`**（如 **`月结+月末`**，数据仍存 0）；**事件型**（到港前结 / 签收月结 / 到海外仓结 / 签收结）拦截文案统一为「**该客户合同账期为事件型（{账期名}），无法推导到期日期，暂不支持出账**」，合同账期为空 → 「该客户在该主体下的合同账期未完善，请先在客商中心补全合同账期」；**边界口径** —— **顺延条件 = 算出的「号」不晚于账单日（含当天）**，即保证**到期日严格晚于账单日**（09-15 + 月结+15 → **10-15**；09-30 + 月结+0 → **10-31**）。**同日第六批（产品逐条指定）**：⑲ **账单编号尾号改 5 位零填充** —— `AR-{主体代码}-{YYYYMM}-{5位序号}`（原 3 位），**序号按 主体 × 账期月 每月从 `00001` 重置**（跨主体各自计数）；缘由：3 位在单主体单月体量下不够用，且月内连续编号便于对账。⑳ **运单审计默认态改为「全部」**（原默认「已收货」）—— 财务进页先看全量再按需收窄。㉑ **待发送 / 已发送 / 已作废 三个 Tab 的查询条件收敛为 4 项**（账单编号 / 客户昵称 / 客户编号 / 我司主体，默认露前 3 个），撤掉币种、账单日期、到期日期、发送状态、确认状态等条件。㉒ **货主端账单管理收敛为 2 个 Tab**（待确认 / 已确认）—— 撤掉「已核销」Tab，**核销维度彻底退出客户侧**；两个 Tab 列表列统一为 **7 列**（账单编号 / 账单日期 / 到期日期 / 所属主体 / 币种 / 账单金额 / 操作），查询条件统一 **3 项**（账单编号 / 所属主体 / 币种）；**账单详情比照员工端改一版** —— 头部 **7 字段**（`我司主体` 在货主端统一叫「所属主体」，去掉「未核销金额」；**账期属内部口径、不对客户展示**）、费用明细按员工端 **26 列**（「扩展单号」改「FBA 单号」、补「仓库代码」）。㉓ **客户侧账单可见性口径落演示数据** —— 货主端**只展示「已发送」的账单**（未发送账单客户看不到），为此把演示数据铺厚：货主端可见 **5 张** —— 待确认 2 张（`AR-SZ-202608-00001` ¥1,000.00 CNY、`AR-GZ-202608-00002` $380.00 USD）、已确认 3 张（`AR-SZ-202607-00001` ¥2,160.00、`AR-GZ-202607-00002` ¥1,450.00、`AR-GZ-202608-00001` ¥1,330.50）；并在员工端账单页与运单审计页**各加 3 票运单**（016/017/018，仓库代码与 FBA 单号三页同值）、账单页账单数 8 → **11 张**，使两端账单明细逐行对得上；**货主端确认提示条去掉「核销」字样**（改为「财务将据此账单收款」），客户侧不出现核销术语。**同日第七批（核销维度展示边界重划 · 产品拍板）**：㉕ **核销的「状态与金额」正式回到账单管理** —— 员工端「账单管理 → 已发送」Tab 列表新增 **核销状态（tag）/ 已核销金额 / 未核销金额** 三列（**只读**），货主端「账单管理」两个 Tab 同样新增这三列（**客户侧也统一用「核销」口径**）；**「待发送」「已作废」两个 Tab 不加**（未发送必然未核销、已作废的前置判据就是未核销，都是恒值）。边界重划 = **核销「状态/金额」= 账单自身的属性 → 账单管理；核销「动作/弹窗/核销记录/反核销」→ 收款核销**，两页**操作菜单分开**（发账单归账单管理、核销归收款核销），只做**关联跳转** —— 账单管理的「核销状态」tag 点击 → 新标签页打开「收款核销」并带 `?billNo=` 定位该账单。缘由：原先「核销维度零展示」导致**客户侧其实已经在露核销**（资金账户有「未核销金额」列、资金流水有「收款核销」类型），唯独账单列表一个字没有 —— 客户看到欠款却查不到是哪几张单、财务在自己账单列表也看不到哪张欠款，链条断在中间。㉖ **连带对齐三页旧数据**：`财务_收款核销.html` 待核销列表换成真实账单、列名统一为「已核销金额 / 未核销金额」、钱包余额与资金账户管理同值；`财务_账龄台账.html` 账龄明细换成真实账单（账期天数按新算法：月结+15 → 15 天、月结+月末 → **31 天**、月结+5 → **5 天**），消掉不存在的 `AR-GZ-202604-001` 与旧编号撞号；`Waybill.html` 关联账单号同步为 5 位。㉗ **口径用词统一为「未核销金额」** —— 全仓（财务 PRD / 数据设计 / RDD / 原型 / 版本索引）把「未收金额」「待核销欠款」「未收总额」等旧称统一为「**未核销金额**」（汇总列用「未核销总额」），**DB 字段仍为 `unreceived_amount`**；同时明确 **账龄台账的账单口径保持「含待发送账单」**（判据 = 未核销金额 > 0 且未作废），该点已在 PRD R24 加「待确认」标记、待与财务确认后再定。㉘ **收款核销页把「部分核销」与「反核销」显性化** —— ① 待核销列表新增 **核销状态** 列（未核销 / 部分核销 / 已核销，tag 与「账单管理」同一套配色）—— 原来只能靠「已核销金额 / 未核销金额」两个数心算；② 核销记录列表新增 **账单金额** 列（与「核销金额」并排，一眼看出 2,219.50 是 3,500.00 的部分核销）；③ 演示数据补一条**反核销链路**（同一笔 400.00：一条「核销」+ 一条「已反核销」，净影响 0），用以演示 **R23「已反核销记录不再显示『反核销』按钮」**。㉙ **收款核销 / 账单核销 用词统一 + 资金账户数据按账单聚合对齐** —— ① **用词统一**：`wallet_flow.flow_type` 的显示名由「**账单核销 / 账单核销撤销**」统一为「**收款核销 / 收款核销撤销**」（**枚举值 40 / 50 不变**），与菜单 / 页面同名 —— 原先同一个动作在菜单叫「收款核销」、在资金流水叫「账单核销」，两套名字容易混；「进账↔账单核销关系表」的措辞同步简化为「进账↔核销关系表」。② **资金账户的「未核销金额 / 待确认账单」按账单聚合对齐**（员工端「客户资金账户」7 个钱包 + 货主端「账户余额」5 个钱包）—— 口径 = `Σ 该客户已确认且未核销账单的未核销金额` / `Σ 该客户已发送未确认账单的账单金额`，与「账单管理」逐笔同源（如华美 · 广州飞点 · CNY = 1,330.50 + 750.00 = **2,080.50**）。③ **补「钱包余额不足 → 只能部分核销」的演示**：迅达 · 广州飞点 · CNY 钱包余额调为 **¥800.00**（低于该客户账单未核销 ¥1,280.50），点「核销」时上限 = `min(未核销金额, 钱包余额)` = ¥800.00，天然走部分核销分支；该行「实际金额 = 余额 − 未核销金额 = **−¥480.50**」同时演示负数红字。㉚ **收款核销页两个 Tab 重排（产品逐条指定）** —— ① **待核销 Tab**：主列表 **15 列**（账单编号 / 客户编号 / 客户名称 / 客户昵称 / 我司主体 / 币种 / 账单日期 / 账期 / 到期日期 / 账单金额 / 核销状态 / 已核销金额 / 未核销金额 / 钱包余额 / 操作），查询条件 **5 项**（账单编号 / 客户昵称 / 客户编号 / 我司主体 / 核销状态）；② **核销记录 Tab**：主列表 **19 列**（核销单号 + 上述 15 列 + **核销金额 / 操作人 / 操作时间**），查询条件 **6 项**（核销单号 / 账单编号 / 客户昵称 / 客户编号 / 我司主体 / 核销状态）；两个 Tab 的查询区均在 Tab 上方、**各自独立的筛选状态（切换不清空）**、默认露前 3 个其余进「更多查询条件」、控件统一 240px、点查询后收起；③ **明确「核销单 ↔ 账单 = N:1」** —— 一个账单可以有多条核销记录（多次部分核销），核销记录列表里**同一账单编号会出现多行、差异只在核销单号**，**各条有效核销单金额之和 = 该账单当前已核销金额**（被反核销的那条不计入）；④ 演示数据补齐到 **7 条核销记录**（`AR-GZ-202607-00001` 一单 4 条：1,500.00 + 719.50 + 400.00 + 一条 400.00 已反核销；`AR-SZ-202607-00001` 一单 2 条：1,000.00 + 1,160.00；`AR-GZ-202607-00002` 一单 1 条：700.00），账单侧金额与逐条合计闭合。㉛ **核销记录改为「事实快照表」（产品识别）** —— 原先核销记录列表把账单与钱包的**实时值**（核销状态 / 已核销金额 / 未核销金额 / 钱包余额）当列展示，语义错位：核销记录是**历史事实**，要回答的是"这一笔把账单未核销金额从 `bill_before` 降到 `bill_after`、把钱包余额从 `wallet_before` 扣到 `wallet_after`"。现改为：**四个快照列**（核销前/后未核销金额、核销前/后钱包余额，数据源 = `ar_writeoff` 行内快照）+ **核销金额 / 类型 / 操作人 / 操作时间**；账单侧只引用**生成即固定**的静态属性（账单编号 / 客户 / 主体 / 币种 / 账单日期 / 账期 / 到期日期 / 账单金额）。**实时值只留在「待核销」列表**（工作台需要实时判断"还能核销多少、钱包够不够"）。**「核销记录」的查询条件同步把「核销状态」换成「类型」**（6 项全部是核销记录自身的属性、不 join 实时值）。列表 19 → 20 列。**同日再收口（方案 1）**：**「待核销」15 列 → 8 列**（只留决策必需的 账单编号 / 客户名称 / 我司主体 / 币种 / 到期日期 / 未核销金额 / 钱包余额 / 操作；去掉「核销状态」——Tab 定义即"未核销金额 > 0"、**永不出「已核销」**，以及多为 0 的「已核销金额」；账单日期 / 账期 / 账单金额 / 客户编号 / 客户昵称 收进账单编号的悬浮提示），**「核销记录」20 列 → 12 列**（四个快照列两两合并为「**未核销金额（前 → 后）**」「**钱包余额（前 → 后）**」，一行金额从 6 个降到 4 个；账单静态属性同样收进悬浮提示）；**账单编号可点击 → 新标签页打开「账单管理」并定位该账单详情**（与「账单管理 → 核销状态 tag 跳本页」构成双向关联，核销动作不搬迁）。缘由：收口前「待核销」与「账单管理 → 已发送」**重复 14 列**、两个 Tab 之间**又重复 11 列**，同一批账单静态属性在三处各摆一遍。数据设计 表 3 的四个快照字段与 `writeoff_type` 备注同步。 ㉜ **核销前置 = 客户已确认** —— 「收款核销 → 待核销」清单的准入条件补上「客户已确认」（`confirm_status = 已确认`），即 **客户未确认的账单不能核销**：客户还没认可账单金额时不能扣它的钱包。未确认账单留在「账单管理 → 已发送」看「确认状态」，客户确认后自动进入待核销，不提供强制核销入口（新增 PRD R67、数据设计视图清单、RDD 流程三规则⓪）。缘由：原先待核销只判「未核销金额 > 0 + 未作废」，会出现「客户还没认账、钱已被扣」的账；且与资金账户侧口径打架（资金账户的「未核销金额」本来就只统计已确认账单）。 ㉝ **账单超期自动确认** —— 客户一直不确认会把账单永久卡在「待确认」、连带卡住核销与账龄，故加一条截止线：**账单发送日（含当日）起满 7 个自然日仍未确认 → 每天 00:30 的定时任务自动确认**，记 `confirm_type`（客户确认 / 系统自动确认），与手动确认**效力相同、不可撤销**（账单不可红冲，后续异议走「新增负数流水、下期抵扣」）。客户侧「待确认」Tab 加**常驻说明条**（告知 7 天规则），账单详情头部补第 8 个字段「**确认信息**」（已确认 · 确认方式 · 确认时间 / 待确认 · 将于 {时间} 自动确认，把自动确认时间直接写给客户）；员工端「已发送」Tab 的「确认状态」tag 加悬浮显示同一份确认信息，账单详情补「发送时间 / 确认信息」。数据设计 `ar_bill` 补 `confirm_type`（PRD R16 重写）。 ㉞ **草稿池「账期」列明确为实时取 + 去掉待发送的跑批异常提示** —— 「待发送」Tab 顶部原有一条黄色提示条（「本次自动出账有 N 个客户因合同账期未完善被跳过，点击查看清单」）**整条下线**（清单接口、异常态 E47 一并收口）：跑批跳过是后台行为、补录入口在客商中心，本页既不能就地处理、提示也就无处落地。改为**在文档里写清「草稿池是实时视图、其中『账期』列实时取合同账期，取不到就显示『待补录』」**，客户在客商中心补齐后该列自动变正常、下次跑批即可出账 —— 一条显性列替代一条死提示。 ㉟ **资金流水 / 充值记录补「是哪本账户」** —— 货主端资金账户的「资金流水」「充值记录」两个 Tab 都是**跨账户**列表（同一客户在我司多主体、多币种下都有钱包），原来只给「主体 + 币种」，客户看不出是哪本账户的操作。两表统一补「**账户名称**」（`账号编号 币种代码`，按 账户主体 × 币种 反查钱包，与「账户余额」Tab 同源），并把「主体」列改名「**账户主体**」—— 三列合起来唯一确定钱包；充值记录里「账户名称」= 入账钱包、「打款账户」= 客户付款的银行账户，两个概念不再混。 ㊱ **菜单结构合并：「收款核销」菜单下线，核销全部并入「账单管理」** —— 缘由：**「待核销」= `已发送 ∩ 客户已确认 ∩ 未核销金额 > 0`，本来就是「已发送」Tab 的一个筛选视图**，做成第二个菜单的第二张列表后，同一批账单的静态属性在两页各摆一遍、还得双向跳转（同日已把两边列表从 15/20 列收到 8/12 列，只是把重复变小、没有消灭）。合并后 `财务_账单管理.html` = **5 个 Tab**：前 4 个（草稿池 / 待发送 / 已发送 / 已作废）是**账单**、按发送维度 + 作废标记分（同一把尺子）；第 5 个「**核销记录**」是**核销单**（`ar_writeoff`，另一张表）= **核销动作的操作日志**，承载反核销与跨账单检索、**不参与账单状态归类**。三根连接线：① 「已发送」Tab 行内「**核销**」按钮就地弹窗（`unreceived_amount > 0` 时显示；**客户未确认则置灰 + 悬浮提示** —— 当日 R67 的闸门从"不在列表里"变成**看得见的按钮状态**）；② 「**核销状态**」tag → 该账单的「**核销明细**」抽屉（= 核销记录 Tab 的子集，含反核销）；③ 核销记录的「账单编号」→ **同页定位**到该账单并打开详情。**零跨页跳转**；「已发送」Tab 查询区加「核销状态」条件（一步筛出待核销那批）。另补 **账单详情「操作时间线」**（生成 → 发送 → 确认 → 历次核销 / 反核销 → 作废，只读、按时间排序）—— 把 `sent_at / confirm_type / confirmed_at / voided_by / voided_at` 这些「文档有、页面无处看」的留痕落到界面。**原型**：`财务_收款核销.html` 撤页、菜单与权限点摘除；**连带修掉跨页悬空引用**（员工端资金账户管理 / 资金流水、货主端资金账户里那条「收款核销 −1,280.50」挂着不存在的核销单号且金额对不上任何账单 → 改为对齐真实核销记录 `WO20260709001` / −700.00 / 2026-07-09；新锐那笔 −4000.00 因该客户无有效账单改记为「提现」）；**核销单演示数据重排为 8 条**，核销/反核销日期全部落到「账单生成 + 客户确认」之后（原来存在"核销早于账单生成"的自相矛盾），给已核销 2,800.00 的 `AR-GZ-202606-00001` 补上核销记录，三条钱包链（迅达·广州飞点 CNY 5,819.50→800.00、华美·广州飞点 CNY 4,419.50→3,719.50、华美·深圳飞点 CNY 5,360.00→3,200.00）逐笔闭合到当前余额。**文档**：财务 PRD §5.2 / §5.3（改写为「与 §5.2 同一页面的核销部分」）/ 功能清单 A6 / 权限矩阵 / 全局接口表 / 导航图 / §12.4 决策，数据设计（待核销视图行 · `flow_type` 备注 · Tab 归属说明），RDD（业务全景 / ⑨ / 流程三 / AC8 / AC10 / 功能清单 A6），货主端 PRD（核销入口引用），`context/功能清单-luna.md`（菜单树 + 页面清单）。**员工端页面数 7 → 6 页**。 ㊱-2 **核销记录列表再收一列（12 → 11 列）** —— 摘掉「钱包余额（前 → 后）」（「核销记录」Tab 与账单的「核销明细」抽屉一并摘）：核销金额与钱包变动同额（核销就是从钱包扣钱），再摆一列等于把同一个数字说两遍，钱包侧进出另有「资金流水」页专管（`flow_type` 40/50）；`wallet_before / wallet_after` 字段保留（留痕 + 对账「链式闭合」校验），但不上表，钱包信息只在「核销明细」抽屉小结条里出现一次；**账单详情的「操作时间线」同步只写账单侧变化**（核销行去掉「钱包余额 前 → 后」半句）。**PRD 补上「操作时间线」的分区规格**（此前只写在决策记录里，页面规格缺失）。 ㊱-3 **补漏洞：作废原因必填** —— 已作废账单详情一直展示「作废原因」、`ar_bill.void_reason` 字段也一直在，但**作废操作没有采集入口**（单句确认框 + 原型写死「手工作废」），属"有字段、有展示、没人填"。现补「**作废账单**」弹窗：账单摘要只读 + 红底风险提示（其下 N 条流水释放回草稿池、账单不可恢复、账单号不回收）+ **作废原因 Textarea 必填**（≤200 字，空 / 纯空格阻断「请填写作废原因」），确认后 `voided_by` / `voided_at` / `void_reason` 一并落库并在**账单详情（已作废）**与**操作时间线**展示；**弹窗本身即二次确认**（不再叠加确认框）。R63 / RDD ⑥ / AC5a / 数据设计 `void_reason` 备注同步。 ㊱-4 **查询区布局加固（原型）**：账单管理的查询区原先容器允许换行 + 按钮组 `margin-left: auto`，**笔记本上按钮组整组掉到第二行**。**根因是宽度不够**：`index.html` **侧边栏固定 240px** + 页面 padding 64px → 查询区真实可用宽只有**视口宽 − 304**（1366 笔记本 ≈ **1022px**），而「3×240px 条件 + 5 个按钮 ≈ 1242px」放不下。**改法**：① 条件控件改**弹性宽度**（宽屏 240px 统一、窄屏等比收窄到 140px，**不再折成「一行一个」**）；② 外层 `nowrap` + 条件区内部折行 + 按钮组 `flex-shrink: 0`；③ **`生成账单` 移出查询按钮组 → 最终放进「草稿池」Tab 内部**（Tab 下方、表格上方、独立一行右对齐，**只有该 Tab 显示**）—— 写操作本就不该在查询区；中途两次走错：**塞进「Tab 行」**导致 `el-tabs` 只拿到「卡片宽 − 按钮宽 − 间距」、**表格被切窄 118px、列表铺不满卡片**；**只做隐藏而不换位置**会让 Tab 行在切 Tab 时跳 40px。实测 5 个 Tab：按钮仅草稿池可见、5 张可见表格右边界与卡片内容右边界**差值均为 0**、`el-tabs__header` y 恒为 128、页面无溢出。 ㊱-5 **「已发送」Tab 加「批量核销」** —— 表格加**多选列**（跨页保留勾选），Tab 工具栏加「**批量核销**」（**Tab 级批量动作、不进操作列**；无勾选置灰）→ 弹窗逐行列出勾选账单，**逐张按 min(未核销金额, 钱包余额) 全额核销**，**客户未确认 / 已结清 / 已作废的行标出并跳过、不阻断其余**，跨币种不做金额合计，确认后每张各生成一条核销记录。新增 R68。**顺带修定序缺陷**：批量 N 笔同一分钟、时间戳相同 → 「按时间取最新」分不出先后，改为**以核销单号为次级排序键**（列表 / 抽屉 / 操作时间线三处 + PRD 校验关系口径）。缘由：财务月中收款常是「一批单一起核销」，逐张点核销太慢；而批量混入未确认单时不能整批失败，只能跳过并说清。 ㊱-6 **❗ 修致命缺陷（本轮自造）**：批量核销按钮里写了自闭合的 `<Select />` —— 浏览器内联模板下 **HTML 解析器先于 Vue 工作**，`<select>` 是真实元素且属「特殊插入模式」，**把该标签之后的整段文档全部丢弃**，表现为「已发送」Tab 数据整块消失 + 后续 Tab 与全部弹窗不见（控制台那条 `Failed to resolve component` 是误导性告警）。改为 `<Check></Check>` 后 5 个 Tab 数据全部恢复。**防复发**：`check-prototype.js` 增加「HTML 元素同名冲突」检查项（特殊插入模式元素 → ERR），`style-guide.md` 立规「图标一律写显式闭合标签、禁用 `<Select />` 这类与 HTML 元素同名的自闭合写法」。 ㊱-7 **批量核销补三条规则 + 修一个资金 BUG**：① **勾选阶段硬校验**（只能勾「客户已确认 + 未核销金额 > 0」，且选定第一张后只有**同一客户 × 主体 × 币种（同一个钱包）**的账单还能勾）；② **核销顺序 = 到期日期从早到晚**（同日按账单号）；③ **逐单从同一个钱包顺序扣** —— `本次核销 = min(本单未核销金额, 钱包可用余额)`，钱包不够则部分核销、扣完后的单本次不核销。**BUG**：此前「本次核销」是打开弹窗时按"钱包当时余额"逐行独立算的，多行共用同一钱包时各自按全额算、**能把钱包扣成负数**（如三张各 100、钱包 150 会算出 300）。缘由：财务月中收款是「一个客户的钱包进了一笔钱，把它欠的单从早到晚逐张核掉」——顺序定了才不会出现"先核了后面的、前面的反而没钱核"的不公；钱包不够时只核到哪算到哪、剩下的留待下次。 ㊱-8 **批量核销入口改为「从钱包进」**（产品试用后反馈"全选 / 取消全选让我误解"）：根因是**一次批量只处理一个钱包**，而列表里同时存在多个钱包的账单时，「多选 + 全选」注定要解释"为什么这些行勾不上"，且 `el-table` 的表头全选是一次性批量赋值、可能真勾上跨客户的行再被兜底拦掉。**改法**：列表**去掉多选列**（回归纯展示）→ 工具栏「批量核销」→ 弹窗内**先选钱包**（只列「有可核销账单」的钱包，标注钱包余额 / 可核销张数 / 合计未核销；余额为 0 的仍列出并标「⚠️ 暂不可核销」）→ **默认全选该钱包下全部可核销账单**（可逐张取消，顺序与分配实时重算）→ 按到期日期顺序核销。**"全选"语义因此准确**。另补：**行内「核销」按钮在钱包余额为 0 时置灰**（否则弹窗上限为 0）。 ㊱-9 **核销钱包改为「逐级条件选」**：产品反馈「一个拼接好的长下拉又难扫又难做 —— 客户有多本钱包，能不能让财务按条件一个一个选」→ 弹窗第 1 行拆成**三个联动条件**：**客户 → 我司主体 → 币种**（后级依赖前级；选项只列「有可核销账单」的组合；**某级唯一时自动带出**），**选齐即锁定一本钱包**、下面**实时出该钱包的可核销账单**（未选齐显示空态提示）；打开弹窗默认带出第 1 个钱包的三级条件。摘要行同步改为「钱包（客户·主体·币种）/ 钱包余额 / 可核销 N 张」，余额为 0 时红字提示。④ 日期区间给 `flex: 0 0 240px` 不许压窄。**用无头 Chrome 按 index 内真实宽度实测**：936 / 1022 / 1096 / 1576 四档下默认条件全在一行且等宽、按钮同排、生成账单与 Tab 标签同带、无纵向溢出；展开「更多」后按钮仍在第一行。账龄台账撤回上一版改动（其原写法本就同排）。`style-guide.md` 补「弹性宽度」规则与「查询区可用宽 = 视口 − 304」的测量基准。 ㊱-10 **批量核销补三个演示场景**：① **钱包有钱但没账单**（已有：迅达·香港富力顿·HKD ¥25,000、新锐·广东飞点·CNY ¥12,000 —— 不进「批量核销」的钱包条件，但在资金账户看得到）；② **有账单但钱包没钱**（给迅达补第 2 张可核销账单 ¥980.00，钱包仅 ¥800.00 → 按到期日先核 07-05 那张至部分核销 800.00、钱包归零，第 2 张本次不核销）；③ **一客户多主体多币种**（给华美补 2 张：广州飞点·USD ¥620.00、深圳飞点·CNY ¥880.00 → 主体下拉 2 项、币种下拉 2 项）。同步 5 页（账单管理已发送 7→10、草稿池 7→5；账龄台账 8→11 行；员工端客户资金账户与货主端资金账户的未核销金额；货主端账单管理 7 张含新单明细），交叉核对全 OK。 ㊱-11 **批量核销弹窗取消"预选钱包"（默认值规范立规）**：产品反馈「默认那一个都不合适」—— 原实现打开弹窗就带出**列表第一本**钱包的三级条件 + 默认全选它的账单，等于"手快点两下就执行了一笔没人明确决定过的扣款"（核销不可逆、撤销要走反核销；且"第一本"只是排序的偶然结果，与财务这次要核谁无关）。改为**空态起步**：打开时三级条件全空、无表格、`确认批量核销` 置灰并悬浮说明原因，空态写明本次可核范围（4 本钱包 · 6 张可核销账单）；**唯一选项例外**保留（某级只剩一个选项、或全页只有一本可核销钱包 → 直接锁定，属"没有别的答案"）；锁定后仍默认全选。另加两处防呆：**主按钮带金额**（`确认批量核销 ¥800.00`）、**置灰原因走 tooltip**。规则同步写进 `style-guide.md` 新增章节「★ 危险操作（写操作）的默认值规范」。 ㊱-12 **批量核销弹窗三处收口（产品反馈）**：① **改上级一律清空下级、不再代填** —— 原「某级只有一个选项时自动带出」会让财务误以为条件没重置（华美 · 广州飞点 → 换成迅达，迅达的唯一主体 / 币种恰好同名，界面上看着没变），且下级若本就没有该值会残留无效值；连带**打开弹窗也不再有任何预选**（只有一本可核销钱包时改为在空态里写出来提示，不代填）。② **客户下拉支持 客户名称 / 昵称 / 编号 三字段模糊搜索**（大小写不敏感，下拉项右侧灰字显示 `昵称 · 编号`）。③ **候选范围口径钉死**：三级条件的候选**从"有可核销账单"的账单反推，不查客户主数据、不按客户启用状态过滤** —— 客户被禁用或合同已过期 / 已解约，只要还有可核销账单就照样能搜到、能核销（否则存量欠款收不回来）；`style-guide.md` 同步补两条通用规则（候选范围按业务事实反推 / 长下拉要能按标识搜）。 ④ **页面说明文字清空（产品逐条点删）**：① 弹窗第 1 行右侧那句灰字（「一本钱包 = 客户 × 主体 × 币种…不预选、不代填…」）、② 空态里那句理由（「核销会实时扣减钱包余额…所以不替你预选钱包」）、③ 空态的「本次可核范围：N 本钱包 · M 张可核销账单」、④ 摘要行里「（请逐级选 客户 → 我司主体 → 币种）」—— 四处一并删除。页面不放口径说明条：**摘要行未锁定时只显示「未选定 / — / —」，空态只留一句动作引导**；口径本身留在 PRD / RDD / 数据设计里。 ㊱-13 **批量核销弹窗撤掉「全选」行 + 演示数据补两档 + 编号与告警收尾**：① 撤掉钱包锁定后那一行（「全选本钱包可核销账单」勾选框 + 「核销顺序 = 到期日期从早到晚…」说明文字）—— 页面不放说明条；**锁定钱包后仍默认全部勾选**，需要勾回时逐行点（不设全选控件）。② 演示数据补两张：**迅达 06 月 ¥300.00**（`AR-GZ-202606-00002`，到期日最早）→ 迅达钱包 800.00 下批量核销一次演完三段式 **全额 300 / 部分 500 / 不核销 980**；**东莞市宏泰五金制品有限公司**（新增客户 KH1004，**已停用但仍欠款**）广东飞点 · CNY ¥1,860.00（`AR-GD-202608-00001`）+ 钱包 5,000.00 → 演示「客户停用、账单照收照核」。③ 联动同步：账单管理已发送 **10 → 12 张**、账龄台账 **11 → 13 行**（新增 1 组客户 × 主体 × 币种）、资金账户管理 迅达未核销 **2,260.50 → 2,560.50** + 新增宏泰钱包（账号 1000010）。④ PRD 补一条 **R 编号说明**：R41 / R42 / R43 / R45 为空号（历史口径重整下线），编号不回收，新规则从 R69 起递增。⑤ **全仓清掉 39 处自闭合组件标签**（`<Search />` / `<Picture />` / `<Link />`，30 个文件；文档里当示例写的注释保留）→ 复扫 0 处，60 页 checker 全过。 ㊱-14 **员工端客户资金账户补 2 本钱包（与货主端对齐）**：员工端「资金账户管理 → 客户资金账户」漏了华美科技的 **广东飞点 · CNY（1000008，余额 800.00）** 与 **香港富力顿 · HKD（1000009，余额 0.00）**，而货主端「资金账户」这两本都在 —— 同一客户在两端看到的钱包数不一致（**客户资金账户 8 → 10 本**）。同步三处派生的钱包映射：`财务_资金流水` 的账户名称映射补全为 **10 本一一对应**、`财务_账单管理` 的核销钱包余额映射 10 条同步、`财务_财务流水` 提现弹窗的钱包余额映射补 2 条；华美的签约主体同步补「广东飞点 / 香港富力顿」（手工开通钱包的主体边界）。 ㊱-15 **「生成账单」入口与范围改造（产品确认方案 A）**：原先必须先在草稿池勾选流水、弹窗里的客户 / 主体 / 币种全是只读、候选也只列"勾选的那批"—— 两个毛病：**表头全选跨组合后点按钮被硬拦（白勾一场）**、**想补勾一条只能关弹窗回表格**。现改为**「范围从条件进 + 候选默认全选 + 仍可逐条微调」**：① 点「生成账单」**不再要求先勾选**，未勾选直接进弹窗**空态起步**，弹窗里的客户 / 我司主体 / 币种改成**逐级联动条件**（只列"有可成账流水"的组合、改上级清空下级、不代填 —— 与批量核销同一套规则），客户下拉支持 名称 / 昵称 / 编号 模糊搜索；② 锁定组合后候选 = 该组合下**全部**可成账流水、**默认全选**、可逐条取消（表格勾选态与"已选 N 条 / 账单总额"始终一致）；③ **勾选跨多个组合不再硬拦**（提示后以空态弹窗打开，勾选不丢）；从草稿池勾选进的那条快路径保留 —— **范围 = 勾选的这批**，其余候选列出但不勾（"我只出这几条"仍成立）；④ 护栏：主按钮带金额（`确认生成账单 ¥820.00`）、二次确认写明"发送后改动需作废 + 重开（账单号不回收）"、账单总额为负时黄底提示；⑤ **账期为空 / 事件型的组合不进条件候选**（消灭"点了才被拦"的死路），从勾选路径进入时仍给具体原因。**不做跨组合批量出账（一次拆 N 张）**，列入二期候选 —— 自动出账已覆盖固定账期客户的每月出账，手工出账属补录场景。 ㊱-16 **资金流水余额链全部闭合 + 「生成账单」弹窗排版重排**：① **余额链重建（10 本钱包全部闭合）** —— 原演示数据里多条流水链断的：华美 · 广州飞点 · CNY 的 `充值 5,000` 记在 08-27（在 `转汇 −8,640` 之后），数学上不可能收到 3,719.50；迅达 · 广州飞点 · CNY 的 `充值 2,000` 记在 08-27（在 08-26 核销之后），而钱包余额 800 是由 5 笔核销/反核销逐笔扣出来的（核销记录里 `wallet_before 5,819.50 → 800`）；员工端「资金账户管理」钱包流水弹窗里迅达又是另一套链（`充值 10,440 / 转汇 −9,640`），与核销记录冲突。现按「**每本钱包一条链、末行余额 = 该钱包当前余额、核销流水与核销记录逐笔对齐**」重建：华美 · 广州飞点 · CNY `充值 5,000 → 3,000`（这样 06 月开账 10,559.50 起，07-09 核销 −700 / 08-20 提现 −500 / 08-24 转汇 −8,640 / 08-27 充值 +3,000 → 3,719.50 全程为正）；迅达的 `充值 2,000` 前移到 **06-04**（`5,819.50` 正好接上核销记录的首笔 `wallet_before`），并给「资金流水」页补齐迅达的 **5 笔核销/反核销流水**；**华美 · 香港富力顿 · HKD 钱包余额 0 → 15,000**（与两端「充值 15,000 → 余额 15,000」的流水和充值单闭合，顺带成为第三个「钱包有钱、但名下无账单」的例子）；补 **华美 · 广东飞点 · CNY 充值 800**（两端流水 + 货主端充值单）；修掉新锐 · 广东飞点 · USD 那条 `充值 0.00` 的假流水（改成 `转汇 −820`）。同步：员工端「财务_资金流水」15 行、「客户资金账户」钱包流水弹窗 10 本全覆盖、货主端「资金账户」流水 8 行 + 充值记录 5 条、`财务_财务流水` 的充值申请金额与提现余额映射。**校验脚本逐链核对：10 本钱包全部闭合、无负余额、末行余额 = 钱包余额**。② **「生成账单」弹窗排版重排**：原来上半部分是「三个裸下拉 + 三行带 label 的表单项」两种排版语言混在一起、右半边空着 —— 现统一为 **`section-title` 分区 + 两列 × 3 行等宽栅格**（「账单信息」= 客户 / 我司主体 → 币种 / 账单日期 → 账期 / 到期日期，「可成账流水」为第二区块），实测六项左边缘严格对齐（各 412px）、空态弹窗高 478px / 锁定态 601px。 ㊱-17 **账龄范围定案：「待发送」账单不计入账龄台账**（关闭 R24 的待确认项）：账龄是**对客催收口径** —— 账单还没发给客户、客户并不知道这笔钱，按「账单日 + 账期天数」硬算会出现"客户从没见过这张单却显示逾期"；反过来**账单一发出就开始跑账期**，所以**已发送但客户未确认**的账单照常计入（确认只影响能否核销，见 R67）。判据统一为 `send_status = 已发送(20)` + `unreceived_amount > 0` + `void_status = 正常(10)`。原型账龄台账剔除 3 张待发送账单（明细 **13 → 10 行**、汇总仍 5 组）：华美 · 广州飞点 · CNY 3,295.50 → **2,080.50**、华美 · 广州飞点 · USD 1,100.00 → **1,000.00**、华美 · 深圳飞点 · CNY 4,820.00 → **1,880.00**；迅达 · 广州飞点 · CNY **2,560.50**、宏泰 · 广东飞点 · CNY **1,860.00** 不变。文档：PRD R24（改写为「账龄起算与账龄范围」）/ R26 / §5.4 列表范围 / §7.5 公式范围 / A15 / §12.4 决策，RDD 流程三 5.2 业务规则 + AC11，数据设计「视图：账龄台账」+ §4.4 + 与账龄口径的关系。 ㊱-18 **「待发送」阶段开放账单编辑（加流水 + 改账单日期，编辑不留痕）**：判断标准 = **账单还没发给客户、尚未形成对客承诺 → 改动只在内部、代价最低，给足灵活性；一旦发出就是对客凭证，只能作废重开**。① **加流水（新增）** —— 账单详情「费用明细」标题右侧加「添加流水」→ 弹窗列**草稿池里同 客户 × 我司主体 × 币种 的已审计 · 未成账流水**，**默认不勾选**（这里是挑具体哪几条，与「生成账单」锁整组后默认全选不同；需要全加用表头全选），确认后流水 `bill_id` 回填、**账单金额重算**、流水从草稿池移除；候选为空给虚线空态（该合同账期推不出时不给加）。② **改账单日期（新增）** —— 账单详情头部「账单日期」在待发送阶段改成日期选择器（其余 Tab 只读），改后**到期日期与账期天数快照同步重算**；**账期月（账单号中段）变了 → 账单号按新账期月重新分配序号**（待发送尚未对外、可重编号，实测 `AR-GZ-202609-00001` → 账单日改 08-31 后 → `AR-GZ-202608-00004`，明细随号迁移）。之所以敢改：**待发送账单本来就不参与账龄**（㊱-17），改日期不会动到任何已出账龄数据。③ **删流水**沿用 R66（只剩 1 条不给删）；**永不开放**：改客户 / 主体 / 币种（等于换一张账单）、手改账单金额（金额恒等于明细代数和）。④ **按产品要求三种编辑都不进「操作时间线」**（时间线只记 生成 → 发送 → 确认 → 核销 / 反核销 → 作废）；顺带撤掉明细区下方那条「待发送账单可逐条删除流水…」的说明文字（页面不放说明条）。文档：PRD R66 改写为「待发送阶段的账单编辑（删流水 / 加流水 / 改账单日期）」、§5.3 弹窗 2 头部与明细表规格、**新增「弹窗 2b：添加流水」规格**、交互行为加 [改账单日期] / [添加流水]、A08 验收、§12.4 决策；RDD AC5；数据设计 `billing_date` / `due_date` / `bill_amount` 三个字段的编辑与重算口径。 ㊱-19 **待发送账单编辑三处修正（产品实测反馈）**：① **「添加流水」候选改为默认全选** —— 原先默认不勾选，弹窗一打开「确认添加」就是灰的，看起来像"没有保存按钮"（实测按钮一直在、勾一条就亮，但观感不行）；现与「生成账单」一致默认全选，需要挑其中几条时逐条取消，**只有候选为空时才置灰**。② **改账单日期的 toast 必须写出重算结果** —— 原 toast 只说"已重算"，而月结类账期里「账单月第 N 号不晚于账单日 → 顺延到下一月同一号」，会出现"改了账单日、到期日确实没变"的正常结果，用户看到的就是"提示重算了但没重算"。现改为带结果：到期日变了 → `到期日期 {旧} → {新}`；没变 → `按「月结+15」推导，到期日期仍为 {值}`；同时 handler 直接取事件值，与 `v-model` 的更新时序解耦。③ **账单编辑进「操作时间线」**（产品口径，推翻㊱-18 的"不留痕"）—— 每次编辑留一条 `编辑账单`：**只记 时间 + 操作人 + 动作类型**（移除流水 / 添加流水 / 修改账单日期），**不记增减了几条、哪几条流水**；一期只保留最近一次。**顺带修掉连带缺陷**：「生成账单」条目原先拿**账单日**当生成时间，而账单日恰恰是待发送阶段能改的 → 改成用真实生成时间戳（`ar_bill.generated_at`，回退账单日），否则改完日期会出现"编辑排在生成之前"的荒谬顺序。 ㊱-20 **账期「天数/号」纠正 + 账单编辑加保存 + 草稿池去勾选 + 待发送批量发送（四条产品反馈）**：① **「天数/号」不再有「月末」** —— 产品指出客户管理里这个字段**只能填数字**，我们此前把它当"0 = 月末"是自造口径，导致"改账单日期后到期日看不出变化"。现口径：`days_or_date` = **1–31 的整数**（客商中心文档同步为「只填数字，无月末 / 月初」）；**0 / 空 = 未填** → 财务侧视为账期未完善、不允许出账（与账期缺失、事件型账期同一路径）；**「该月无该号 → 取该月月末」保留**（如 2 月取不到 30 号）。演示数据华美 · 深圳飞点 `月结+月末 → 月结+30`（两端账单与账龄同步：08 月两张到期日 08-31 → **08-30**、账期天数 31 → 30）。② **账单详情加「保存」按钮** —— 产品两次反馈"这个界面没有保存按钮"：原来是改日期即时落库、界面上没有提交动作。现改为**草稿式编辑**：改账单日期**只进编辑草稿**，到期日期**实时显示重算预览并带 `未保存` 黄 tag**，点弹窗底部**「保存」**才落库（同步重算到期日 / 账期天数、跨月重编账单号、写编辑留痕）；无改动时按钮置灰 + 悬浮「账单日期未修改」；**未保存直接关弹窗 = 丢弃改动**。③ **草稿池去掉勾选列** —— 产品口径："有了「生成账单」按钮就不逐条勾流水了"。草稿池列表去掉多选列（**19 → 18 列**），生成账单收敛为**单一入口**（一律空态弹窗、逐级选条件），原"勾选快路径"与跨组合提示一并下线；弹窗内仍可**逐条取消勾选**（要只出其中几条时）。④ **「待发送」新增「批量发送」**（新增 **R69** + 弹窗 6）—— 按**客户**一次发一批（同一客户名下可能有多张待发送账单：多主体 / 多币种）：工具栏按钮 → 选客户（可按 名称 / 昵称 / 编号 搜索，选项只列"有待发送账单"的客户）→ 候选 = 当前查询结果里该客户名下**全部**待发送账单、**默认全选**、可逐条取消 → 小结**按币种分别列合计**（不跨币种合计）→ 红底提示"发送后不可撤回，发错只能作废重开" → 二次确认**列出全部账单号**、按钮带张数 → 逐张写 `sent_at`（同批同一时刻，超期自动确认的 7 天窗口各自起算）。 ㊱-21 **负数应收（贷记单）的结清口径定案 —— 新增「结转钱包」（R70）**：产品提出「我们现在能生成负数账单，这种怎么做应收核销？走核销的话客户钱包平白多出钱就是 BUG」。讨论结论：① **负数账单本来就进不了核销**（核销判据 `未核销金额 > 0` 天然排除），所以"核销后钱包多钱"这条路径不存在；② 但**负数应收流水是业务事实驱动、系统自动产生的**（识别到赔付即向运单插一条负应收），**不取决于客户是否合作** —— 所以"禁止它单独成账、留到下一期抵扣"不成立（客户一不合作就永远躺在草稿池，二期赔付规模化 = 批量挂账）；③ 也**不能借道「充值 + 提现」** —— 充值记录是**对客凭证**（客户在货主端「资金账户 → 充值记录」看得见），垫一笔"客户从没打过的充值"会直接暴露且对账无据；④ **不再造第二套"贷记台账 / 退款单"**（钱包本就是"客户可用额度"容器，再造一套 = 两个池子都要轧账）。最终方案：**净额 > 0 → 应收账单**（负数流水作账单内一行「赔付抵扣 −N」，随账单正常核销）；**净额 ≤ 0 → 贷记单**（不参与核销 / 不发送客户 / 不进账龄 / 不计入「未核销金额」）→ 财务一键「**结转钱包**」→ 写一条 `flow_type = 70 赔付入账` 的资金流水（`related_no` 挂账单号）、客户钱包余额 += 净额绝对值 → **之后有欠款就正常核销、要现金就走提现**（不需要先充值）；撤销入账 = 反向流水（`flow_type = 80`，入口在「资金流水」页那条流水行上，钱包余额不足则阻断）。**不需要客户确认**（否则客户不合作即死锁）。**"已结转"用派生判定，不新增账单状态枚举**；**表结构零改动**，只新增 2 个 `flow_type` 枚举值。同步：PRD 新增 **R70** + 待发送行操作（结转钱包 / 已结转 / 贷记单不给作废）+ 生成账单净额分支 + **资金账户「未核销金额」口径加 `> 0`（堵住负数账单冲小未核销金额的 BUG）** + A31 验收 + §12.4 决策；数据设计新增「视图：贷记单与结转钱包」小节 + `flow_type` 70/80 + 幂等约束；RDD 业务规则 ⑭ + **AC19**；原型：账单管理（贷记单派生 / 结转动作 / 待发送行操作 / 详情 tag / 批量发送排除贷记单 / 演示贷记单 `AR-GD-202608-00003` 已结转）、资金流水（赔付入账流水 + 「撤销入账」+ 类型 tag）、资金账户管理（新锐 USD 钱包 +300 与钱包流水）、货主端资金账户（类型字典对齐）。 ㊱-22 **贷记单流程补正（产品实测反馈两项）**：① **贷记单必须能发送给客户** —— 上一版为"避免客户不合作时卡死"而禁用了发送，产品指出这是**流程缺陷**（客户侧看不到这张单、也拿不到凭据）。正确做法：**照常发送 + 走同一套确认流程 + 满 7 个自然日未确认由系统自动确认兜底**（R16 现成机制），**结转前置改为「客户已确认」**（与核销对称），既保住对账闭环又不会死锁；货主端同步展示贷记单：列表「核销状态」列显示**「贷记单」黄 tag**（悬浮说明"我司应付您的款项"）、「已核销 / 未核销金额」两列显示 `—`，详情「账单金额」旁同 tag；员工端「已发送」Tab 同样用 tag 替换「未核销」，两列显示 `—`。② **撤销入账的校验** —— 存在"入账 300 → 这笔钱已被拿去核销另一张账单 → 再撤销"的情形：**校验必须做**（钱包不可透支，否则把余额扣成负数），但**不能只报"余额不足"** —— 现改为**列出这笔钱被哪些流水占用**（核销到哪张账单 / 提现）并指引「先反核销被占用的账单后再撤销」，且明确**撤销只能是全额**（不允许"部分撤销"把账做花）。③ **连带修正**：「结转钱包」入口从「待发送」Tab 移到「已发送」Tab（贷记单要先发送客户、确认后才结转，它发出后就在「已发送」）；**结转进「操作时间线」**（结清动作：金额 + 钱包余额前后 + 流水号）；演示数据把静态贷记单改挂 **华美 · 广东飞点 · CNY**（新锐不在货主端，改挂华美后客户端才看得到），其时间线为 生成 → 发送 →（09-07 系统自动确认）→ 结转钱包，钱包 800.00 → 1,100.00，两端资金账户/资金流水同步。PRD（R70 改写 + 待发送/已发送行操作 + 已发送三列 + R16 + A31 + §12.4）、数据设计（贷记单小节：可发送 / 结转前置=已确认 / 撤销全额+占用提示 / 结转留痕）、RDD（⑭ + AC19）、货主端 PRD（§5.8.1 贷记单展示）同步。 ④ **「生成账单」弹窗里残留的旧描述一并清掉**：净额 ≤ 0 的二次确认正文原写「**不发送客户**…客户不合作也能结清」（上一版口径的残留，且 markdown 星号在弹窗里会当纯文本显示）→ 改为「生成贷记单…**照常发送客户**，客户确认（或满 7 个自然日自动确认）后由财务『结转钱包』结清」；标题 / 确认按钮 / 底部按钮 / 弹窗内负数黄底提示 / 已发送核销状态列 tooltip 全部按同一套措辞对齐（按钮文案随单据类型切换 `确认生成贷记单` ↔ `确认生成账单`，**弹窗标题同步切换**）；**文档里同源的旧口径一并清掉** —— R70 ② 与数据设计「贷记单」小节的「不发送客户（货主端不展示）」改为「照常发送客户、货主端照常展示」，PRD 弹窗 1 的负数黄底提示按净额分支拆成两句、校验项序号去重（④ 净额分支 / ⑤ 二次确认）。 ⑤ **贷记单在「待发送」阶段的口径与作废闸门（产品追问「待发送能改吗 / 发送后能作废吗」）**：**保留三段式**（待发送 → 发送 → 客户确认 → 结转），**不做「生成即已发送 / 已确认」** —— 贷记单是我司对客的付款承诺，对外文件必须有人复核后发出，而「待发送」正是唯一"改完不用惊动客户"的纠错窗口，死锁问题已由 R16 的 7 天自动确认兜底。补三处：① **待发送照旧可改可废**（加 / 删流水、改账单日期后保存；整张可作废、流水释放回草稿池下期重出），并在**待发送列表的账单金额右侧加「贷记单」黄 tag**；② **新增作废闸门** —— 原先作废只判「核销状态 = 未核销」，而贷记单核销状态恒为未核销 → **客户已确认的贷记单也挂着「作废」按钮**（客户认账后财务一键删凭据、两边对不上账）；现改为**贷记单「客户已确认」（含系统自动确认）后作废入口置灰 + 悬浮说明原因**，纠错走「运单侧新增反向流水 → 下期抵扣」、已结转的指引去「资金流水」撤销该笔「赔付入账」，**未发送 / 已发送未确认仍可作废**；③ **贷记单照常参与「批量发送」**（原「只发正数账单」口径作废），弹窗内金额列带「贷记单」tag、小结条把**应收合计与贷记单合计分开列**（两种方向的钱代数相加财务看不懂）。演示数据新增一张**待发送贷记单 `AR-GD-202609-00001`**（华美 · 广东飞点 · CNY −450.00，2 条赔付流水）→ 待发送 3 → **4 张**；顺带修掉老贷记单明细的两处数据不一致（账单 CNY 而明细写 USD、运单号 `013` 与宏泰账单撞车 → 改回华美自己的 `018` 票）。 ⑥ **账单单据类型显式化（产品追问「贷记单其实不是应收账单，类型要不要区分」）**：**要区分，且区分落在数据层** —— 新增 `ar_bill.bill_type`（**10 应收账单 / 20 贷记单**），生成时按勾选流水的**净额符号**判定（> 0 → 10、< 0 → 20、**= 0 → 不出账**）；**不建第二张表**（两类单据字段与生命周期 90% 重合 —— 客户 / 主体 / 币种 / 账单日 / 账期 / 明细流水 / 发送 / 确认 / 作废 / 操作时间线，且成账入口共用草稿池，分表等于两套代码两套留痕）。**连带把四处「靠金额符号天然排除」改成显式判据** —— 核销准入（R67 / R68）、账龄台账（R24）、资金账户「未核销金额」、待核销清单视图一律以 `bill_type = 应收(10)` 为准（语义比 `bill_amount > 0` 清楚，也不怕"净额刚好为 0"这类边界）；**待发送阶段编辑若净额符号跨 0 → 阻断**（类型变了须作废重开）。**新增 PRD R72**。 ⑦ **「核销记录」与「贷记单」各自独立成菜单（产品拍板："搞在一起乱七八糟"）** —— ① **新增 `财务_核销记录.html`**：核销单（`ar_writeoff`，**另一张表**）的操作日志 + 反核销，查询 6 条件、列表 11 列（快照「未核销金额（前 → 后）」+ 类型 tag），账单编号点击新标签页开「账单管理」定位该账单；② **新增 `财务_贷记单.html`**（`bill_type = 贷记(20)`，**另一种单据类型**）：4 Tab 待发送 / 已发送 / 已结转 / 已作废，**没有核销维度、没有账期与到期日期、不进账龄**，结清 = 行内「结转钱包」（前置 = 客户已确认），待发送阶段可改账单日期 / 加删赔付流水、可整张作废（流水释放回草稿池），**客户确认后不可作废**；**逐张发送、无批量动作**；③ **「账单管理」收敛为 4 个 Tab 的纯应收账单页**，**核销「动作」留在本页**（行内核销 + 批量核销 + 只读的核销明细抽屉）、**核销「记录」与「反核销」去记录页**（动作跟着记录走）；**生成入口仍唯一** —— 贷记单由「账单管理 → 草稿池 → 生成账单」产生（净额 < 0），Toast 指路「请到「贷记单」菜单发送客户」；④ **连带回撤三处"两种对象挤一页"打的补丁**（已发送列表的贷记单 tag 与两列 `—`、批量发送的"应收 / 贷记分开合计"、核销记录 Tab）；⑤ **货主端不拆** —— 客户只关心"我有几张单要处理"，仍混排展示 + 「贷记单」tag；⑥ **「结转钱包」= 终态，取消「撤销入账」**（`flow_type = 80` 不再存在）—— 产品口径"进了客户钱包就不能撤销"：钱包是客户可用额度容器，钱进去后可能已被拿去核销别的账单（那笔核销有效），逐笔回滚不可行也说不清；**纠错统一走对冲凭证**（运单侧新增正数流水 → 下期账单作为应收费抵扣，下游账单与核销记录都不动、账面自动轧平）。落地：`财务_资金流水.html` 撤掉「撤销入账」按钮与整个 `reverseSettle`（操作列一并撤掉）、「赔付入账」类型 tag 加悬浮说明，资金流水类型字典（员工端 / 货主端）去掉「赔付入账撤销」，结转二次确认写明"结转不可撤销"。**菜单与权限同步注册**（员工端「财务」子菜单加「贷记单」「核销记录」两项 + 权限点）。 ⑧ **「结转钱包」那条流水的口径钉死（产品追问"客户确认后往钱包加钱，流水是不是多了一条、怎么处理"）**：① **它必须存在** —— 钱包余额 = Σ `wallet_flow.amount`，**只改余额不写流水会导致余额对不上账**；它与「充值」是**并列的钱包增项**，区别只是钱来自我司对客户的应付兑付（不是客户打款）。② **一条、且只有一条**：`flow_type = 70` + `related_no`（= 贷记单号）**唯一约束** → 同一张贷记单只会产生一条，重复点结转被拦「该贷记单已结转」。③ **同一次事务写三处**：钱包 `balance += \|净额\|`（带 `balance_before/after`）、贷记单**派生**「已结转」（不新增状态枚举）、贷记单操作时间线加一条 `结转钱包 {金额}`（含前后余额 + 流水号）。④ **四处可见、同源互查**：员工端「资金流水」（只读 + 类型 tag 悬浮说明 + **关联单号可点跳「贷记单」并定位**）、员工端「资金账户管理 → 客户资金账户 → 钱包流水」下钻、**货主端「资金账户 → 资金流水」**（客户看得到）、贷记单详情时间线（反过来给流水号）。⑤ **进钱包即混同**：不区分来源、核销/提现按余额扣、不做逐笔配对、**不可撤销**。文档补：PRD §7.8（钱包余额变动公式加「赔付入账（+）」+ 该流水的 7 条口径）、§5.6（类型选项补「赔付入账」+ 只读与互查说明）、货主端 PRD §5.9；数据设计 `wallet_flow` 的 `balance_before/after` 备注写明「钱包余额 = Σ 本表 amount」。原型补：`财务_资金流水.html` 的关联单号可点跳「贷记单」、`财务_贷记单.html` 支持 `?billNo=` 深链直接打开详情。 ⑨ **贷记单弹窗文案改「三步走」（产品反馈"客户确认后由财务结转钱包结清，这句没看懂"）** —— 原文案一句话里主语跳了三次（客户 → 财务 → 客户）、"结清"又容易被读成"客户把欠款结清"（方向正好相反），读不出"谁做什么、钱去哪"。改写为**动作链式**：**先点明方向**（`本单是我司应付客户的款项：不参与收款核销、不进账龄`），再按顺序写三步 —— `① 财务把本单发送给客户 → ② 客户确认这笔赔付金额（发送满 7 个自然日未确认则由系统自动确认）→ ③ 财务点「结转钱包」把这笔钱计入客户的可用额度`，并**当场定义「结清」= 本单到此结清（我司欠款兑付完毕，不是客户付款）**，最后单独一句讲后续用途（客户拿这笔额度抵扣其他账单或申请提现）。同步改写四处：生成贷记单的二次确认正文与弹窗内黄底提示（账单管理）、贷记单页顶部提示条 / 发送确认 / 结转确认；Toast 收短为 `贷记单 {单号} 已生成（我司应付客户）—— 请到「贷记单」菜单发送客户`。PRD §5.2（黄底提示 + 校验⑤ 正文）、§5.12（顶部提示条 + [发送客户] / [结转钱包] 正文）、R70 ③（补「结清 = 我司欠款兑付完毕」的定义）同步。 ⑩ **「结转钱包」的入口位置写死 + 按钮改为「置灰可见」（产品追问"最后这一步财务在哪里点"）** —— 入口唯一 = 「**贷记单**」菜单（`财务_贷记单.html`）→「**已发送**」Tab → 该行操作列「**结转钱包**」（R70 ② 早已如此规定；账单管理页不提供、资金账户/资金流水页只有记账结果）。**同时修掉一个原型缺陷**：原实现是"客户未确认时按钮**不渲染**"，加上演示数据里恰好没有"已确认但未结转"的单 → **整页看不到这个按钮**，财务问"在哪点"就答不上来。现改为**按钮常显**、未确认时**置灰 + 悬浮说明原因**（`客户还未确认这笔赔付金额；发送满 7 个自然日（{日期} 00:30）系统自动确认后即可结转`）—— 与「作废」「核销」同一套"置灰 + 原因"规范，**不用"按钮消失"表达不可用**；并**补一张演示单 `AR-GD-202609-00002`**（华美 · 广东飞点 · CNY −180.00，已发送 09-15 14:00、客户当天确认 15:20、未结转）让按钮真的可点，进而演示"结转后从「已发送」移到「已结转」"。PRD §5.12 已发送 Tab 行操作同步。 ⑪ **「单据类型只看净额、明细允许异号流水」口径写明（产品追问"应收账单是不是必须 > 0，那贷记单里能不能含正数流水"）** —— ① **是的**：应收账单净额恒 > 0、贷记单净额恒 < 0、净额 = 0 不出账（R72）；② **贷记单可以含正数流水**（此前实现已允许、但文档没写明）：同一组合的流水按 客户 × 主体 × 币种 分组，异号天然同组（如 `+620 运费 −80 赔付 −200 赔付`），**单据类型只看整单净额符号**，明细是"轧差过程"；因此 **贷记单里可以有正数运费行、应收账单里可以有「赔付抵扣 −N」行 —— 同一条规则**；③ 唯一的硬闸仍是**净额跨 0 阻断**（跨 0 = 单据类型变了，须整张作废重开）；④ **经营统计不受轧差影响** —— 分析口径走 `ar_flow`（每条流水金额完整落库），账单只是结算凭据。⑤ 原型的「添加流水」弹窗说明改写为「**可以并入正数流水** —— 单据类型只看整单净额；但并入后净额不得跨 0」；⑥ 演示数据在「华美 · 广东飞点 · CNY」草稿池补一条 **`+620.00` 运费**（09-15 15:00 才审计通过、晚于本组出账时点 → 归下期），两端草稿池一致（账单管理草稿池 **7 → 8 行**）：并进那张 −450 的贷记单 → 并入后 **−110.00 仍为负 → 允许**；只勾这一条正数 → **+170.00 跨 0 → 阻断**，两种结果都能现场点出来。PRD R72 / §5.12（弹窗 2）与数据设计贷记单小节同步。 ⑫ **成账颗粒度定案：按净额轧差、不做「按符号拆单」（产品确认"不拆单"）** —— 同一 客户 × 我司主体 × 币种 的流水**打成一张单**，**单据类型只看整单净额符号**（> 0 应收 / < 0 贷记 / = 0 不出账），**明细允许异号流水**（应收账单里可以有「赔付抵扣 −N」行、贷记单里可以有正数运费行）。**不把正负流水拆成两张单**（拆了会让账龄虚高 —— 应收金额变成未扣赔付的全额、而钱包里可能正躺着可抵的赔付额度；且客户每期要多确认一张贷记单、单据与操作量翻倍）。**"本期赔付总额"这类需求走 `ar_flow` 报表**，不靠拆单解决。文档：PRD R72 边界 + §12.4 决策（含 `+620 / −280` 的数字对照）；数据设计自动出账伪码补 `bill_type` 判定与轧差口径。 ⑬ **一致性清扫（主动体检，非产品反馈）**：① **checker 增强** —— 全仓扫出两处「图标撞原生元素名」的隐藏缺陷：`供应商管理.html` 的 `<Picture v-if="…" />`（自闭合）与 `客户管理.html` 的 `<Picture></Picture>`（显式闭合但撞原生 `<picture>`）→ 附件列表里**图片附件的图标一直不显示**（Vue 把与原生元素同名的标签当普通元素处理）。两处改为 `<component is="Picture"></component>`（style-guide 规则 4 的既定写法）。同时把 `check-prototype.js` 的「HTML 元素同名冲突检查」从**只抓自闭合**扩展到 **① 自闭合（含带属性写法 `<Picture v-if="…" />`，原正则漏检）+ ② 显式闭合但撞原生元素名**，`style-guide.md` 自查说明同步（原先写"checker 只抓规则 2"）；② **跨页跳转按单据类型路由** —— 运单审计的「关联账单」跳的是 `财务_账单管理.html?billNo=`，而该页现在只应收账单：编号若属于贷记单会被静默忽略（点了没反应）→ 改为**用 `window.location.replace` 转到「贷记单」菜单并定位**（同一标签页替换，不新开第二个标签）；③ **弹窗文案体检**（上一轮定的"一句话别塞多个动作"标准）：`发送账单` 补「客户要确认 + 满 7 天自动确认 + 不可撤回」、单笔 `核销` 补「从哪本钱包扣 + 账单与钱包的前后值 + 撤销入口在「核销记录」」、`批量发送` 补确认链路、`批量核销` 补「从哪本钱包逐张扣 + 撤销入口」；PRD §5.2 / §5.3 / §10 错误文案汇总同步；④ **可发现性** —— 贷记单页「已发送」Tab 标签在有待结转单时追加 **「· 可结转 {K}」**（K = 0 不追加），财务一眼知道有几张等着结转。 ⑭ **`el-checkbox` 取值坑导致"审错方向"（★ 真 bug，影响两页）** —— 本仓 Element Plus 的 `el-checkbox` **以 `label` 为值、`value="ar"` 不生效**：`<el-checkbox label="应收" value="ar">` 勾选后 `v-model` 拿到的是**「应收」**，而提交逻辑判的是 `dir === 'ar'` → **判不成立、落到 else 分支，勾「应收」实际把「应付」审掉了**（运单审计页与运单管理页同一处写法）。已把方向取值**归一化**（`应收`/`ar` 都识别为应收，其余为应付）：`财务_运单审计.html` 的提交与默认方向、`Waybill.html` 新增的审计入口都按归一化处理；`style-guide.md` 补条目防复发。**实测**：勾「应收」→ 提示 `已审计 2 票运单的应收流水` → 再开弹窗「应收」已置灰、「应付」仍可选 ✅。 ⑮ **「运单审计」页的运单级审计弹窗重复定义两份**（同一 `v-model="waybillAuditVisible"` 写了两个 `el-dialog`：一份用页面共享 class、一份是内联样式的旧稿）—— 点「审计」会**同时弹出两个一模一样的弹窗**（后者盖住前者，改完一份不生效另一份）。已删掉内联样式那份（保留共享 class 版）。由新增的「重复弹窗扫描」发现（全仓仅此 1 处）。 ⑮ **「批量发送」按钮点击无反应（产品实测报缺陷）** —— 根因是拆分菜单那轮**误删了 setup 的 5 个导出**（`onSendCustomerChange` / `onBatchSendOpened` / `openBatchSend` / `submitBatchSend` / `sendTableRef`）：按钮本身渲染正常、静态语法检查与模板作用域检查**全过**，所以上一轮的渲染体检（数 Tab、看 DOM 字符数）没抓到 —— 点下去只在控制台报 `openBatchSend is not a function`。已补回导出并**实测按钮可打开弹窗**。**连带补一道防线**：新增 `.agent/scripts/check-setup-refs.js` —— 交叉校验「模板标识符 × `setup()` 返回键」，把这类**静默失效**（模板用了、setup 没返回）变成可自动发现；反证测试：故意去掉导出 → 报 `openBatchSend, onBatchSendOpened, onSendCustomerChange, submitBatchSend` ✅。全仓 60 页跑一遍，另有 2 处同类缺陷（见 港口仓库 / 货主端 变更记录）。 |
| 2026-09-15 | **手工新增应收的打标口径明确为显式幂等去重** —— 「添加应收」选中来源规则后为运单打标，**同一运单上同一标识只打一次**（去重键 = 运单 + 标识）：命中多条共用同一标识的规则、或人工添加与自动命中撞在同一标识上，都不重复打、不报错；未选规则的手工行仍不打标；**规则命中与流水来源规则留痕不受影响** |
| 2026-09-14 | **应收业务流水生成逻辑定稿：优惠独立成行 + 逐行取数** —— 运单收货完成时**逐行**生成应收业务流水（运费 / 运费优惠 / 泡比优惠 / 各命中附加费 / 各命中附加费优惠各一行），**优惠行的费用类型取自优惠规则上配置的费用类型**、单价=优惠值（带符号，负=减钱）、金额=优惠值 × 计费量；运单应收净额与账单金额统一为**各行金额的代数和**（原「同行三列：公布价/优惠/应收净额」口径作废）。缘由：优惠独立成行后**运单审计与客户价格争议可以直接下钻到「哪条规则给的、减在哪个费用类型上」**，一期不再需要额外的优惠明细表，也与「优惠一期即按独立营销模块落地」的定位一致。**同日追加**：明确**计价单位来源**（R55）——运费 / 运费优惠 / 泡比优惠行取运单所关联**服务组合的计费方式**（KG / m³），附加费 / 附加费优惠行取该条**附加费规则的计价单位**；**优惠值量纲 = 元 / 计价单位**，优惠行与其父行的计价单位与计费量必须一致，一律 `金额 = 单价 × 计费量`。**同日明确**：**附加费优惠行不单独配置费用类型** —— 其费用类型取**所属附加费规则计价明细「应收」行的费用类型**（与附加费行同类型，账目天然对得上）；运费 / 泡比优惠行取各自优惠规则上配置的 `fee_type`（R03 / A27 / U01 与数据设计 §4.7、字段备注同步）**同日按反馈再调两侧**：**运单管理** —— 进入菜单默认命中 **「已收货」**（原「收货中」；收货完成前不产生应收流水，业务进来要看的就是这批待审流水）。**运单审计** —— ① **状态 Tab 去掉「收货中」**（该状态无费用可审）并把默认态改为 **「已收货」**，演示运单同步收敛到审计范围内（去掉 已下单 / 收货中 / 已取消 三态）；② **查询条件控件统一 240px** —— 日期区间（拣货时间）被 Element Plus 默认的 350px 撑着，比其它条件宽一截，整排条件参差不齐；③ **工具栏收敛为 4 个按钮：审计 / 反审计 / 费用管理 / 复制运单号** —— 本页是从运单管理页改出来的，创建运单 / 导入 / 快递比价 / 打印标签 / 路由管理 / 资料修改 / 标识 / 供应商互联 / 更多操作这些**与财务审计无关的操作按钮一律撤掉**；「费用管理」下拉 = 添加应收 / 添加应付（上下文 = 勾选运单，按「费用 × 票」批量铺开），「复制运单号」把勾选运单的运单号按行复制到剪贴板；④ **主列表与查询条件补「关联单号」**（`relNo`，紧接「客户单号」之后）；⑤ **修掉一处把演示数据整屏过滤空的缺陷** —— 查询对象里 `exportInspectFirst` 沿用了运单管理的多选数组 `[]`，**空数组在 JS 里是真值**，于是 `if (search.exportInspectFirst)` 恒成立、而「空字符串 === 空数组」恒不成立，列表**恒为 0 条**（看起来像演示案例被清掉），改为单值空字符串；⑥ **新增流水按「适用运单」落库** —— 原来无论从哪票进入详情，「新增流水」都写进演示运单（YD…005）的流水数组，现在按卡片上所选「适用运单」写入对应运单。同步：财务 PRD **§5.1**（列表范围 / 状态 Tab / 查询条件 13→14 项且同宽 / 列表列改为「40 业务列 + 多选 + 操作」并清掉过期的 24 项旧表 / 工具栏 4 按钮 / 弹窗 1 改为「运单级审计·反审计」方向选择 / 运单详情费用 Tab 可写 / §9 补运单级审计与流水级审计两条接口）、**§5.10**（运单管理默认态 收货中 → 已收货）、RDD **⑥⑦** 与业务动线（业务侧审计入口由运单管理改为运单审计页）。**同日把「只读」落到运单详情的每一个操作按钮上**：上一轮把运单审计定为「费用维护与审计的唯一入口」，但**运单详情抽屉是从运单管理页整页复制过来的，里面 40 个操作按钮全部可点** —— 财务在审计页里能编辑运单、编辑货箱 / 测量数据 / 快递单、取消退件货箱、加 FBA 运单与 SKU、添加标签比价、批量下载标签、重新比价、打印标签、创建工单、上传 / 修改 / 删除附件、增删改轨迹，**等于把整个运单管理的能力从后门带进了财务页**，与「本页只做费用核对与纠正」冲突。现按产品要求**把运单资料类操作按钮全部置灰**（新增页面级开关 `waybillOpsReadonly`，24 个按钮统一绑定）：编辑运单、编辑货箱、编辑测量数据、编辑快递单、取消·退件货箱、+FBA运单、+添加SKU、添加标签/比价、批量下载标签、重新比价、打印、创建工单、上传附件、修改附件、删除附件、轨迹 添加 / 编辑 / 复制 / 删除；**保留可用的只有查看类动作**（返回列表、申报信息「查看」、附件「下载」、货箱与轨迹的展开收起、流水「详情」）与**费用管理 Tab 的全部写操作**（新增流水 / 编辑 / 删除 / 审计 / 反审计）。置灰编辑态入口后，依赖「已进入编辑态」才渲染的控件（保存 / 取消 / 手动添加货箱 / 删除 SKU 行 / 可点击的 SKU 输入框）自然不可达，无需单独处理。同步：财务 PRD §5.1 弹窗 2（运单详情）补「运单资料类操作按钮一律置灰」清单与可用动作白名单、RDD ⑥。**同日补两处**：① **主列表多选 → 费用管理 → 添加应收，弹窗没体现「一票费用会加到多票上」** —— 弹窗顶部取的是 `currentRow`（上次打开详情的运单）而不是本次勾选的运单，也没有「适用运单」多选，多票进来和单票长得一模一样，财务看不到自己这条费用会落到哪几票上。现按 §5.10 既有口径把模板补齐：顶部改为 **已选 N 票：YD… / YD…**（单票仍显示「运单号（主运单号）」），客户 / 服务组合多票时显示「多票 · 见各流水」；每张卡片首行新增 **「适用运单」多选**（默认选全部勾选的运单、可逐票取消，**该方向已整票审计的票置灰不可选**），卡片标题显示 **「× N 票 · 合计 = 单价 × 计费量 × 票数」**，金额字段也跟着切换成「合计金额」；保存仍按「一条费用 × M 票 = M 条流水」逐票落库、逐票打标（本轮同时修掉「新增流水一律写进演示运单」的落库缺陷）。此前这些计算函数（`feeAddWaybillText` / `feeRowWaybills` / `feeBatchAmountText` 等）**只定义了没导出到模板**，所以模板里根本用不上。② **「审计状态」查询条件的取值不够** —— 原来只有 `未审计 / 已审计` 两个值，且过滤只比对**应收**一个方向的运单标记，**既表达不了「应收 / 应付分别什么状态」，也表达不了「整票没审但已经有流水审过了」这种中间态**（这正是 R61 两层审计标记的必然产物：流水级审计不动运单级标记）。现改为 **6 个取值、分两组**：**运单级（整票，按方向两态）** = 未审计 / 应收已审计 / 应付已审计 / 均已审计；**部分审计（该方向有流水已审计、整票未审）** = 应收部分审计 / 应付部分审计 —— **不新增查询条件位**（仍是产品指定的 13 个条件），判定口径写进 PRD §5.1「审计状态判定」表（未审计 = 两个方向都未整票审计且都无已审计流水；「已审计流水」只认 `已审计`，`已红冲` 不计入）。主列表「审计标识」列同步：出现部分审计时在对应方向标签后追加 **「· 部分已审」**，标签 tooltip 显示该票的综合判定值；演示数据补齐 **均已审计（YD…014 两方向 + 全部流水）** 与 **应付部分审计（YD…009）** 两种态，应收部分审计由 YD…005 / 006 承载。同步：财务 PRD §5.1 查询条件表 auditStatus 行、查询区规范后新增「审计状态判定」表、列表列「审计标识」行。**同日按反馈调三处展示口径**：① **运单审计主列表「审计标识」列前移到「附加费标识」右边**（原来是 40 列里的倒数第二列、紧挨供应商，财务扫一眼审计状态要滑到最右；审计标识与附加费标识同属「这票身上有哪些标记」，放一起看更顺），列顺序同步进 PRD §5.1。② **主列表「应收 / 应付 / 毛利」从裸数字改为「总额（金额 + 币种）+ 悬浮费用明细」**：单元格单币种且汇率恒为 1 时显示 `CNY 1,265.00`，**含外币或存在 `rate ≠ 1` 时显示 `≈ CNY 1,975.00`（折合本位币、前加「≈」）**；悬浮小表固定 3 列（费用类型 / 金额 / 币种），**该方向存在汇率 ≠ 1 的流水时追加 2 列（汇率 / 折合 CNY）**，底部给总额与多币种时的「原币小计」。**汇率展示口径（本轮新设计）**：因为一条运单可以混币种，**运单级金额一律先按行汇率折合本位币再相加**（`折合金额 = 行金额 × 行汇率`），**毛利 = 应收折合 − 应付折合**，费用明细表的金额列**始终是原币金额**、币种与汇率紧随其后 —— 口径成文为新规则 **R62「多币种折合与运单级金额展示」**（缺 `rate` 按 1、本位币取主体记账币种、多币种不做汇率合并展示）；费用 tab 汇总条的「运单应收净额」同步改按折合口径并标注「（折合 CNY）」；演示数据补一条 **USD 报关费（rate 7.1000）**，让「≈ 折合 / 汇率列 / 原币小计」看得见。**运单管理主列表的「应收 / 应付」列同口径同步**（同名字段两端不得分叉）。③ **费用管理里的「汇率」字段从「金额」右边挪到「币种」右边**（员工端运单审计 / 运单管理 + 货主端运单管理共 3 页 7 处：应收流水表、应付流水表、新增应收卡片、编辑应收弹窗、新增应付弹窗等）—— 币种与汇率是一组换算信息，中间隔着单价 / 计价单位 / 计费量 / 金额不便于核对；PRD 字段顺序与接口字段清单同步。 |
| 2026-09-14 | **账单草稿池落地 + 生成节点分节点增量** —— 收货生成的流水全部先进入**账单草稿池**（未关联账单即停留池内，不新增账单状态机），「账单管理」新增**草稿池 Tab**：按 客户 × 主体 × 币种 × 账期 分组展示流水条数 / 可成账金额 / 待审计金额，**仅已审计流水可勾选成账**；生成节点分节点增量——字段匹配型附加费随收货完成、字段&公式型附加费拣货实测后追加、人工添加型手工新增，未审计的自动行在运单信息变更时重算覆盖、人工添加行永久豁免重扫。缘由：原流程从「流水生成」直接跳到「财务聚合出账」，财务看不到"本期已发生多少、还差多少没审计"，草稿池把这个中间态显性化，并与附加费规则的触发节点口径对齐。**同日补齐接口契约**：① 明确**取数入参口径**（R56：运单只传自身字段，尾程派送方式 / 计费方式 / 包税方式 / 客户等级 / 可用优惠规则由超级运价服务端反查）；② 新增 **`POST /api/finance/ar-flows/generate`**（收货完成 `trigger=10` / 拣货实测 `trigger=20`）与 **`POST /api/finance/ar-flows/recalculate`**（运单信息变更，**整单重跑覆盖**未审计自动行）；③ R52 / R53 细化到「事件 + 变更字段清单 + 变更字段→受影响行矩阵（见超级运价数据设计 §6.6）」，影响范围补「运单信息变更事件」。**同日按原型补齐页面规格**：① 币种互转弹窗新增**只读「汇率来源」**（接口实时 / 手工覆盖，与 `rate_source` 同值，R37 同步）；② 账单详情「费用明细」新增**「计价单位」列**（与草稿池流水明细弹窗拉齐）；③ §5.1 注明**业务侧交互（添加应收 / 整单审计 / 反审计）归头程管理-运单管理**、财务 PRD 不重复其页面规格。**同日补齐费用流水的到期日与编辑口径**：① `ar_flow` 新增 **描述 / 来源类型（10 系统自动计算·20 人工添加）/ 来源规则名快照 / 到期日期** 四个字段，`source_rule` 之外单独存**规则名快照**，供规则改名或删除后仍能举证「当时是哪条规则给的」；② 新增 **R57 到期日期算法**（业务时间 + 合同账期 + 天数/号：按月类取账单月同号、天数/号=0 取月末、该日已过顺延下一月；按天类 +N 天；事件型账期先留空待回填；**账龄口径不变**，仍按 账单日 + 账期天数 起算）与 **R58 编辑口径**（**只有「人工添加」行可编辑**，系统自动行只能删除或修正运单信息触发重算，已审计冻结、进账单彻底冻结）；③ 财务 PRD 新增 **§5.10「运单费用流水」页面规格**（承载页 `Waybill.html` 费用管理 tab：字段可见性矩阵 + 行详情下钻来源规则/创建人，并明确**货主端只看应收且只读、内部字段不下发**）与 **`SourceType` 枚举**，接口 Query 增 `caller` 过滤、Mutate 对系统自动行返回 400；④ 验收补 A01（到期日与留痕）与 A04（编辑限制）。缘由：运单审计与客户对账需要「这笔钱哪条规则给的、什么时候该付」两项可举证信息；系统自动行若可人工改动，会与运单信息变更后的重算结果冲突。**同日按反馈再收敛两处口径**：① **到期日期从运单费用流水页撤下**（员工端与货主端一致）—— 字段仍按 R57 生成入库，**展示位置迁到财务「账单管理 → 草稿池」（分组列 + 流水明细弹窗）**，即「什么时候该付」只在出账/对账场景露出，不占运单操作页；② **编辑口径由「仅人工行可编辑」改为按字段分档**：系统自动行**仅可改 描述 / 备注**（金额、币种、计价单位、计费量、汇率等由规则与重算唯一维护），人工添加行可改全部字段，已审计冻结、进账单彻底冻结（R58 与接口 PUT 权限同步）。缘由：业务需要给自动生成的流水补「对客说明 + 内部备注」，但金额口径不能允许手改（否则与运单信息变更后的重算结果打架）。**同日再定两处时间字段**：① **「业务时间」改为「拣货时间」**（`biz_time` → `pick_time`）—— 取值 = **该行所属运单的拣货实测完成时间**（运单级属性落在行上，随收货生成的行先留空、**拣货实测完成时回填该运单全部应收行**，含人工行），**到期日期基准同步改为「拣货时间 + 合同账期 + 天数/号」**：账期从「实际计费发生（拣货实测）」起算，而不是从下单/收货起算；② **新增「创建日期」`created_at`** —— 后端自动写入（系统生成=生成时间 / 人工新增=提交时间），**前端两端都不展示**，仅供后台追溯与对账取数；③ **业务手工新增应收时，拣货时间与创建日期由后端自动赋值**（拣货时间取运单拣货时间、创建日期取提交时间），「添加应收」弹窗不再提供「业务时间」录入。同步：财务 PRD 新增 **R59**、字段表/接口/A01/术语与自检行（R01-R59）更新，数据设计字段表与 §4.7 到期日基准更新，RDD 新增业务规则⑩与 AC1 口径。**同日定手工新增应收的规则绑定口径（R60）+ 运单详情默认态**：① **费用类型决定模式** —— 被**人工添加型附加费规则**（`rule_type=30`）计价明细「应收」行引用 → **规则绑定型**：必选来源规则，**币种 / 单价 / 计价单位按规则带出且只读**、业务只填计费量，落库 `流水种类=附加费` + `source_rule_type=40` + `source_rule_id/name`；未被任何引用 → **自由费用型**：自由填写，落库 `流水种类=手工新增`；被**自动型规则**（字段匹配 / 字段&公式）或**优惠规则**引用 → **一律拒绝手工新增**并给出对应提示。缘由：手工加钱必须能回答「这条是按哪条规则加的」，否则运单审计与客户争议无法下钻；同时避免手工造优惠、避免与自动行重复。**同日按反馈去掉这层死板校验**：运费等**通常由系统自动生成**的费用类型**不再拦截** —— 业务认为自动行不对时的标准动作是「**先删除原自动行 → 再手工补录**」，故所有费用类型一律允许手工新增：有「人工添加型」附加费规则候选的仍是「来源规则」**可选**（选了按规则带出单价、落 `flow_kind=40` + 规则留痕；不选则自由填写、落 `flow_kind=60`），无候选的直接自由填写；通常自动生成的类型只在「流水种类」标签上给悬浮提示（不拦截），**优惠类**手工新增须在「描述」写明原因；与已有自动行命中同一去重键 → 阻断并提示先删原行（不静默覆盖）。同步：R60 与 A03a 重写、§5.10 弹窗规格表改为「有候选 / 无候选」两态、RDD ⑪ 与数据设计落库口径同步。**同日补「打标联动」并改弹窗排版**：① **打标联动** —— 手工新增应收时若**选中了来源规则**（人工添加型附加费规则），保存成功后按该规则绑定的**附加费标识**（`surcharge_rule.tag_id`）为运单打标：与规则自动命中时的口径完全一致（加收费用型规则的标识本就打在运单上供后续费用归类），**同一标识不重复打**（幂等），**打标失败只写后台任务告警、不回滚流水**；未选规则的手工行不打标。② **弹窗由超宽表格改为卡片式逐条录入**（每条流水一张卡片、字段分三列网格排布，卡片右上角删除、卡片底部显示关联标识），弹窗宽度 1240→900 —— 原「一长条」横向表格读起来费劲。**同日打通「运单主列表」入口**：原「添加应收」在列表工具栏与运单详情两处都能开，但弹窗的运单上下文一律取 `currentRow`（= **上次打开过详情的运单**），从列表勾选进入时要么显示「未选择数据」、要么带出**别的运单**，多票也没有指定运单的地方，打标更是打到了错误的运单上。现改为**显式上下文**：① 列表工具栏进入 → 上下文 = **勾选的运单**（未勾选 → 阻断提示；1 票 → 顶部带出该票运单号 + 客户/服务组合；**多票 → 逐行必选运单号**，卡片标题显示该行所选运单的「运单号 · 客户 · 服务组合」，顶部客户/服务组合显示「多票 · 见各流水」）；② 详情 · 费用管理 tab 进入 → 固定该运单、顶部显示主运单号；③ **打标按行上的运单执行**（多票逐票打标，幂等），不再依赖「当前打开的运单」；④ 保存校验补「每条流水必须有适用运单」。**同日把多票做成真正的「批量铺费用」**：原设计是「一行 = 一票、多票需逐行指定运单号」，勾 10 票加同一条报关费就得手工加 10 行，与「批量添加费用」的意图相悖；现改为 —— 每张卡片带**「适用运单」多选（默认全选勾选的运单，可逐票取消）**，卡片标题显示「× N 票 · 合计 = 单价 × 计费量 × 票数」，**保存时按票展开：一条费用 × M 票 = M 条流水**（每行带自己的 `waybill_id`、其余字段一致），**打标与留痕逐票执行**，提示改为「已添加 1 条费用 × 2 票 = 2 条应收流水」。同步：财务 PRD R60（入口与批量口径）、§5.10 弹窗规格与 A03a 验收（第⑦条改为批量验收），RDD ⑪ 与数据设计落库口径。② **「添加应收」弹窗** 顶部改为**默认带出主运单号**（原实现取列表勾选、单选进入时为空 → BUG），多选进入时逐行选运单；弹窗改为**按模式切换列**（规则绑定型多一列「来源规则」、单价口径置灰），保存前校验「费用类型 → 来源规则 → 计费量」，并在前端补落库逻辑（原「保存」按钮无实现）。③ **运单详情默认态**：默认停在**费用管理** Tab、右侧**轨迹追踪默认收起**（员工端与货主端一致）—— 业务每次进来都要手动收起轨迹、再切到费用 tab，属高频无效操作。**同日把「费用操作权限」落成文并补齐原型**：此前只有原型里的一句注释口径（列表「费用管理」按钮按运单状态置灰），**详情抽屉的费用 tab 与行内编辑/删除完全没有门禁**，「已审计冻结、进账单彻底冻结」（R53）在原型里**既无数据也无交互体现**。现统一为 **R61 三层 AND**：① **运单状态** 允许 = 已收货 / 转运中 / 已到港 / 已清关 / 已到仓 / 已签收（已下单 / 收货中 尚未计费、已取消 / 退件 须走赔付或红冲，一律只读）；② **审计状态** 待业务审计可改、**已审计冻结**（需先反审计）、已红冲只读；③ **账单关联** `bill_id` 非空即**彻底冻结**（反审计整票运单后红冲）。原型同步：状态集合抽常量供列表与详情共用、详情「添加应收」按状态置灰、**行内编辑/删除按审计与账单置灰并给出对应原因**、mock 流水补 `auditStatus` 与 `billNo`、编辑与保存两处兜底校验防绕过。同步：财务 PRD R61 + §5.10「可编辑权限」小节 + A03b 验收、RDD ⑫、数据设计「编辑前置条件」，头程管理 PRD §8 权限矩阵补「运单费用」两行。**同日审计 / 反审计入口唯一化**：运单管理页「费用管理」下拉**去掉「审计 / 反审计」**（连带删除两个弹窗与 `dialogFeeAudit` / `dialogFeeAntiAudit` / `antiAuditScope` / `billLinked` 残留，及 `handleCommand` 的映射与权限 guard 项），**审计与反审计统一走「财务中心 → 运单审计」（`财务_运单审计.html`）** —— 除你说的「有专门菜单」外，同一动作保留两处入口还会带来权限口径分叉（运单管理页的审计只看得到单票费用 tab，看不到运单级审计台账与关联账单状态）。同步：财务 PRD §5.1「操作入口划分」、§5.10「审计 / 反审计不在本页」、R61 触发点、A03b 第⑤条，财务 RDD ⑥，头程管理 PRD §8 权限矩阵。**同日把审计标签拆成两层（R61 重写）**：查下来原实现**只有流水级** `ar_flow.audit_status`（待业务审计/已审计/已红冲），**运单级审计在原型里只有一张「已审计运单台账」列表、没有任何状态字段** —— 因此「**运单审计了 → 整票不允许加费用**」这条规则**根本落不了地**，「运单未审计但某条流水已审计 → 该条不可变更」也只是行级置灰、没和运单级联动。现补全：① **运单审计标签** = 新增 `waybill_fee_audit` 表（`audit_status` 待审计/已审计/已反审计 + 审计人/时间 + 反审计留痕），**整单审计通过** → 写运单标签 + 该运单全部流水置已审计；**反审计** → 运单标签置已反审计 + 流水回到待业务审计；② 权限判定改为**四级优先级 AND**：运单状态 → **运单审计标签（已审计 = 整票不允许新增费用、行级全冻结）** → 账单关联 → **流水审计标签（该条已审计 = 不可变更、只能新增流水）**；③ 原型：运单列表加「**费用审计**」列（运单标签，带审计人/时间 tooltip）、费用 tab 加「**审计状态**」列（流水标签），列表「费用管理」下拉项在整票已审计时禁用，批量弹窗的「适用运单」把已整单审计的运单置为不可选且默认不勾，保存兜底拦截。同步：财务 PRD R61 / §5.10 权限表（四层）/ A03b 第⑤条、RDD ⑥、数据设计新增「表 11 运单费用审计」，头程管理 PRD §8 权限矩阵。**同日与产品确认后定型「两态 · 按方向」**：① **运单级审计标记收敛为两态 `未审计 / 已审计`**（去掉临时的「待审计 / 已反审计」，反审计即回「未审计」，反审计人/时间/原因单独留痕），且**按费用方向各一条**（应收已审计不影响应付仍可加费用），表 11 唯一键改 `(waybill_id, fee_direction)`；② **两级审计动作确定**：**运单级** = 主列表选运单 →「审计」→ 选**应收 / 应付** → 该运单**该方向全部流水统一审计**，运单该方向标记置已审计；**流水级** = 进运单详情后在**应收 / 应付模块勾选流水** → 审计 / 反审计，逐条生效；**反审计同样两档**（运单级按方向 / 流水级勾选）；③ 权限判定按方向落地：应收已整票审计 → 整票不允许新增应收费用、应收流水全冻结，**应付方向不受影响**；原型运单列表「费用审计」列拆为「应收 / 应付」两个小标签，列表下拉「添加应收 / 添加应付」分别按方向禁用，批量弹窗「适用运单」按应收方向判定。**同日落地运单审计功能（两级动作）**：① **主列表加「审计」「反审计」两个独立按钮**（按你的要求不放回费用管理下拉）：审计 = 勾选运单 → 弹窗选**费用方向（应收 / 应付）** → 该方向**整票流水统一审计**并写运单级标记；反审计同理（默认勾选已审计的方向，可留反审计留痕）；② **运单详情 · 费用 tab 加流水多选列 + 顶部「审计 / 反审计」**：勾选流水后逐条生效，**已审计的流水允许勾选**（用于反审计）、已关联账单的流水反审计被阻断、运单该方向已整票审计时行级审计禁用并提示改走整票反审计；③ **「财务中心 → 运单审计」页改造**：列表由「只列已审计台账」改为**全量运单**（含未审计，财务能看到还差多少没审）、新增**应收审计 / 应付审计两列**与两个状态筛选，**反审计范围由「整个运单 / 指定大类」改为按费用方向**（未审计方向置灰）。同步：财务 PRD §5.1 页面规格全面改写 + §9 新增 3 个审计接口 + **A03c** 验收 + §5.10 订正，RDD ⑥ 补入口，头程管理 PRD §8 权限矩阵订正。**同日把审计入口归位**：上一轮把「审计 / 反审计」按钮做在**运单管理主列表**（+ 费用 tab 行级按钮与多选列）与「有专门的审计反审计菜单」冲突 —— 现全部撤出运单管理（含相关 JS），**统一收敛到「财务中心 → 运单审计」页**：① 列表加**多选列** + 工具栏「**审计**」「**反审计**」两个按钮（勾选运单 → 选费用方向 → 该方向整票统一处理）；② **点运单号进详情** → **应收 / 应付双 Tab** + 该方向流水明细（多选 + 审计状态列 + 条数 / 合计）+ 行级「审计 / 反审计」（**已审计的流水允许勾选**用于反审计、已红冲行不参与、已关联账单时流水级反审计阻断、该方向已整票审计时按钮置灰提示走整票反审计）；③ 运单管理页**只保留审计标记的展示**（运单列表「费用审计」列 + 费用 tab「审计状态」列）与据此的冻结判定。同步：财务 PRD §5.1 / R61 / A03c / §5.10、RDD ⑥、头程管理 PRD §8。**同日按产品口径细化运单审计页**：① **审计范围收敛为运单状态** —— 已收货 / 转运中 / 已到港 / 已清关 / 已到仓 / 已签收（已下单、收货中、退件、已取消不进本页），并加状态筛选；② **列表字段按产品给的 24 项铺开**（运单号 / 客户名称 / 昵称 / 服务组合 / 地址库编码 / 国家 / 件数 / 实重 / 材重 / 体积 / 收费重 / 泡比 / 报关方式 / 清关方式 / 交货条款 / **应收 / 应付 / 毛利** / 客服代表 / 销售代表 / 拣货时间 / 签收时间 / 商品属性）+ 应收审计 / 应付审计 / 操作，其中 **应收 / 应付 / 毛利由该运单流水实时汇总**（与明细天然一致，避免两处口径）；③ **运单详情结构与运单管理一致**（运单概览 / 货箱 / 商品 / POD / 附件 / 费用管理，本页**全程只读**），**费用 tab 的应收 / 应付比运单管理多「审计 / 反审计」两个按钮**；④ **流水关联的账单号在本页用独立列集中展示**（财务按账单归属对账用），运单管理侧则**收进流水行详情**、不占列表列（列表已 14 列，账单号作为「这条为什么改不了」的解释足够了）。**同日按反馈改版运单审计页顶部**：① 「运单状态」**不再做成查询条件下的下拉**，改为**状态 Tab**（全部 / 已收货 / 转运中 / 已到港 / 已清关 / 已到仓 / 已签收，带条数）并置于查询区上方，与运单管理的状态分组口径统一；② 查询区按既有规范重做（标签右对齐 + 控件 220px、**默认 3 个条件**、其余收进右侧「展开 / 收起」、按钮组右对齐 `展开 · 重置 · 搜索 · 导出`、与 Tab 之间加虚线分隔），替换原来一排裸控件 + 独立按钮的写法。**同日纠正一处规范违背**：我最初把「状态 Tab」放到了查询区**上方**、并用了「标签 + 控件」的外置 label 写法，与 `frontend-designer/style-guide`「**查询区永远在 Tab 上方**（不放进 `el-tab-pane` 内）」+「**搜索字段描述写在 `placeholder` 内、不外部放 label**」两条规范相反 —— 现改为 **第一层查询区（条件文字在框内）+ 第二层状态 Tab**，条件默认 3 个 + 「更多」，按钮顺序 `查询 → 重置 → 更多 → 导出`。**同日做职责切分（运单管理只读 / 运单审计可写）**：① **运单管理侧**去掉工具栏「费用管理」下拉、应收流水表只留「详情」、应付流水表去掉操作列、三个费用弹窗（添加应收 / 添加应付 / 编辑应收）从页面删除 —— 只保留「费用审计」/「审计状态」两个展示标记与行详情里的关联账单，业务在这里**只看不改**；② **运单审计侧**在运单详情 · 应收 / 应付 Tab 内补上「**新增流水**」与行内「**编辑 / 删除**」（新增沿用卡片式录入 + 来源规则候选 + 按规则带出单价 + 自动打标；编辑按字段分档；删除与新增都按 R61 校验：**该条已审计不可变更只能新增、已关联账单彻底冻结、该方向已整票审计则整票只读**），与既有审计 / 反审计构成完整闭环。缘由：业务侧要的是「看结果」，财务侧要的是「核对并纠正」，写操作留在运单管理会让权限与冻结口径分叉、也让业务容易误改已审计流水。**同日按产品逐条指示细化两侧**：**运单管理** ① 主列表去掉「费用审计」列；② 默认命中「收货中」；③ 应收流水表新增「关联账单」列（员工端直接可见）；④ **已下单 / 收货中的演示运单不再带应收应付流水** —— 流水改为挂在各票运单上，只有「已收货」及之后才有（对齐「流水生成节点 = 收货完成及之后」）。**运单审计** ① 状态范围去掉 已下单 / 取消；② 默认命中「收货中」；③ **退件放开**（客户送货后不发货要退货，但收货产生的成本仍需客户付 → 退件允许新增 / 编辑 / 删除流水与审计，R61 同步）；④ 主列表换成产品指定的 **38 项字段**（应收 / 应付 / 毛利由流水实时汇总、关联账单取流水账单号、审计标识展示应收应付两方向标记）；⑤ 查询区 **13 个条件**，默认只露 **运单号 / 客户昵称 / 拣货时间**，其余收进「更多查询条件」，且**点击查询后默认收起**；⑥ 应收流水表补齐「关联账单」列 |
| 2026-09-12 | **文档维护（无业务变更）**：数据设计文件日期前缀 `09-11 → 09-12`，并修正其中一处指向超级运价模块的**过期交叉引用**（超级运价数据设计已更名为 `2026-09-12-数据设计.md`）；同时在文首加注说明本次内容一字未改、版本仍 `v0.3`。缘由：超级运价侧文档改名后该引用会指向不存在的文件 |
| 2026-09-11 | RDD v1.0 定稿（Phase 1 = 应收管理 + 资金账户）：补齐业务流水/账单/核销/资金账户各实体字段（公布价与优惠拆列、账单账期天数、红冲与审计留痕、单据关联），补 AC16-18（银行账户/资金流水/入账记录），功能 13→16 项（新增入账记录），分期口径统一为 Phase 2 才含应付/对账/毛利/开票；主体名称统一为「香港富力顿」对齐客商中心，签约账期枚举对齐客商中心 payment_term（101-205）；**清理 U01 未落地的残留**——RDD 中 3 处「优惠负数流水」表述统一改为同行三列口径（优惠不单独成行，写入所属费用行的「优惠」列），`ar_flow.amount` 备注同步（负数仅用于红冲/调整） |
| 2026-09-11 | 新建财务 PRD v1.0（Phase 1 = 应收 + 资金账户）：页面规格 + R01-R50 业务规则 + 18 条 AC 验收映射 + 接口清单；待确认项 10 条已全部确认并转决策记录（优惠口径取同行三列、导出需求登记但一期不实现仅出原型入口、不做统计卡、汇率跟接口展示 6 位、香港账期采用财务补录方案、保留应付占位、我司账户余额一期手工维护）。同步：回补 RDD 的「代客户充值」规则与 AC13；菜单收敛为单页「资金账户管理」（双 Tab：我司账户 + 客户资金账户，原「钱包」菜单并入并下线）；我司账户加 用户是否可见/账面余额/SWIFT；客户资金账户含 未核销金额/待确认账单/实际金额/开通方式；货主端充值主体与币种按已开通资金账户收窄；账单生成支持账期为空时财务补录账期天数并标记来源；员工端 7 个财务列表页统一加「导出」入口占位；汇率取数口径统一调阿里汇率接口实时获取、可手工覆盖留痕、一期不记汇损；原型交互规范统一（多 Tab 页面查询区提到 Tab 上方、条件按 Tab 切换、查询条件 >3 收起至「更多」、按钮顺序统一为 查询→重置→更多→导出→主操作，修复资金流水「重置/导出」换行），规范已写入前端 style-guide；**记录优惠明细缺口**——一期业务流水只落「公布价/优惠金额/应收净额」三列，优惠金额是汇总值、无法下钻到「哪条规则给的、减在哪个费用项」，一期不修，由二期营销模块的优惠落库明细表承接（历史数据不回溯拆解） |
| 2026-09-11 | 钱包开通与生命周期定稿：自动开通统一卡在「合同签约完成」（开户完成不再触发，统一覆盖正常/特殊入驻流程），默认币种各主体统一人民币（香港富力顿不特殊）；财务手工新增钱包的主体边界 = 该客户签约过的主体（含已过期/已解约，用于存量账单与赔付结算），未签约主体一律阻断不设特批；钱包与合同生命周期解耦（过期/解约不影响充值、核销、互转、新增币种钱包）；「默认钱包」字段保留在库、两端不展示，员工端该列改为「开通方式」（系统自动/财务手工） |
| 2026-08-29 | 货主端资金账户加「入账记录」页签（退款/赔偿待入账，待入账→已入账，入账归最新一期抵扣）；头程费用类型加赔付类型标记；账单号主体代码正式定义（广州GZ/深圳SZ/广东GD/香港HK/墨链ML） |
| 2026-08-29 | 运单管理业务流水落地：费用 tab 对齐 ArFlow 字段 + 手工新增删除 + 审计统一为单审计+反审计 |
| 2026-08-29 | 货主端资金账户改主体×币种表格（账户名称/未核销金额/待确认账单/实际金额）+ 账户余额/资金流水/充值记录页签 + 钱包生成节点对齐 |
| 2026-08-27 | RDD v0.4 模板化重写 + 审计模型修正（双锁→单审计+反审计）+ 业务流水生成逻辑细化（优惠负数流水/报价匹配/数据来源）+ 原型完成（货主端2页 + 员工端7页 共9页） |
| 2026-08-27 | 数据设计完成：应收+资金账户 9表（业务流水/账单/核销/钱包/资金流水/币种互转/充值申请/银行账户）+ 账龄视图 + ER图 + 3状态机 |
| 2026-08-27 | 资金账户对齐完成：钱包(客户×主体×币种) + 6类资金流水(充值/充值退回/提现/核销/核销撤销/转汇) + 币种互转 + 员工端3菜单(钱包/财务流水/资金流水) + 入驻自动开人民币默认钱包 |
| 2026-08-18 | 应收管理对齐完成：收款核销（钱包池化/手工核销/部分核销/强制同币种/反核销）+ 账龄台账（账单日+账期天数起算/未到期单列+逾期四段/客户×主体×币种三维度） |
| 2026-08-17 | 财务模块框架启动：2租户5账套 + 计费口径（运单收货触发）+ 8模块分期 + 应收管理细分（流水/账单两层 + 双锁审计 + 红冲纠错） |
