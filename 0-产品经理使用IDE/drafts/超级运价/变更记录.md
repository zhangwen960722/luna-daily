| 日期 | 变更内容 | 变更原因 |
|------|---------|---------|
| 2026-09-24 | RDD 与 PRD 合并定稿为「产品文档」（原 用户需求.md / PRD.md 已删）：业务规则按流程分段 R[流程号].[序号]、状态机保留、枚举/接口下沉数据设计、页面规格缩为功能清单原型轻索引 | 落实文档职责边界：业务规则唯一权威在产品文档、枚举/接口在数据设计，消除 RDD/PRD 双份重复定义 |
| 2026-09-24 | **计算公式 §5 计费生成链路与变更重算口径校正**：①「上限与截断」澄清——优惠行金额为负是正常现象（优惠值带符号、负=减钱），「不得为负」约束的是费用项最终金额（费用项金额 + Σ带符号优惠值 ≥ 0），非优惠行本身；② 计费取数入参清单、每类流水行参数依赖的英文字段名统一改为中文业务描述；顺带修正 §5 内过期规则编号引用（R31→R7.3 / R34→R8.5 / R35→R8.6） | ① 原「任一费用项最终金额不得为负」与「优惠配置负数即减钱」口径打架，易误读为优惠金额不许为负；② 参数用英文裸字段名产品/财务读不懂，改中文描述（字段级契约仍见数据设计） |
| 2026-09-24 | **计算公式 §5 计费生成链路与变更重算归并到财务**：删 §5 整节（含计费取数入参清单——属接口字段契约、产品看不懂），替换为指针「见财务产品文档 流程一」；§3 的 R8.5/R8.6 瘦身为指财务；超级运价只保留计价口径（计算公式 §1~§4 + R6/R7/R8）。数据设计「§7.5」引用改指财务 / 计算公式 §4 | 应收业务流水生成 / 重算 / 冻结属财务域，原 §5 与财务 R1.x 重复，收敛到财务单源；入参清单是接口字段契约，产品文档不承载 |
| 2026-09-23 | **成本与运价口径校正（RDD 全篇复查，对齐产品原型）**——周运维总流程补「系统异步重算（变化触发）」、新建周批次不再「全部标记待重算」；成本项模板去掉「是否换汇」独立字段（改由币种派生）、预置项订正为揽收段 1 个 + 派送段 2 个；批量更新与渠道同步改为「标记待重算、工作台人工触发」而非自动更新成本底价；渠道成本项按原型补全「启用开关 + 模板默认价对照」；派送段仓点由「仅 FBA」放开为「所有平台仓库代码」；运价查询结果对齐原型（按专线分组、盈亏折叠进费用明细弹窗）。PRD R08、数据设计派送仓点字段名同步 | RDD 多处与现行原型 / PRD / 数据设计口径不一致，全篇复查校正 |
| 2026-09-23 | **数据设计职责边界清理（越位内容归位）** —— 优惠冲突与叠加矩阵（原数据设计 §3.19）迁入 PRD 新增 **§6.1**（R32 展开），数据设计 §3.19 瘦身为「见 PRD §6.1 / R32」；删数据设计 §6.4/6.5/6.6 空壳跳转标题（内容此前已迁 PRD §7.5）、§7 风险探测报告（结论已落在 PRD 业务规则「异常处理」列）、§8 信息架构（菜单/页面清单由 PRD §4 承载）；§5「关键设计决策」改写为「建模约定（表结构设计理由）」。RDD §9.0/§9.3 对 §3.19 的引用改指 PRD §6.1；三份文档头部互加「分工声明」（RDD 头部 / 数据设计 §3 / PRD §4），明确字段结构→数据设计、业务规则→PRD、功能→AC→RDD 的唯一权威，存量字段表不机械返工、并物理删除数据设计 §6.1/6.2/6.3 业务流程图（RDD 已有对应）与 RDD 27 个字段表「类型」列（字段结构以数据设计 Schema 为准）。**同日续：RDD 内联业务规则/计算公式正文收口 PRD**——RDD 各流程章节内联的「业务规则/校验规则/计算公式」正文（§3.1~§3.4 服务渠道校验、§4.2~§4.3 航线/周批次规则、§5.3.1/§5.4.1 批量更新与同步、§5.5/§5.6/§7.2 成本公式、§9.3 优惠规则与最终报价公式、§10.1 尺寸换算公式）替换为「见 PRD Rxx / §7.x」，消除 RDD 与 PRD §6/§7 的规则重复；顺带下线 RDD BPR-1~BPR-5 独立前缀，PRD §6.1「依据」列与「RDD 对照」注释同步。 | 业务规则 / 校验逻辑 / 计算公式唯一权威在 PRD（§6 / §7），数据设计只装表 / 字段 / 接口契约；原 §3.19 冲突矩阵、§5 设计决策、§6 业务流程、§7 风险报告、§8 IA 属越位，导致「看逻辑不知道去哪」，按 AGENTS.md 新立「文档职责边界」铁律归位 |
| 2026-09-23 | **附加费优惠新增「费用方向 / 费用类型」显式字段 + 精简优惠弹窗描述** —— 附加费优惠表 `surcharge_discount` 新增 `fee_direction`（一期固定应收、前端置灰）与 `fee_type`（必填，来源基础资料-费用类型，打开弹窗默认带出所属附加费规则计价明细「应收」行的费用类型、可改），与运费/泡比优惠对齐，不再从所属规则计价明细继承费用类型；优惠弹窗去掉「优惠值口径（锚定应收单价…）」与「匹配规则说明」两段描述。数据设计 §3.16、PRD §5.11 / §7.4 / §7.5、RDD（AC19 / 落库形态 / I1）、原型 `超级运价_费用与标识规则.html` 同步 | 附加费优惠此前不配费用方向/费用类型、靠继承所属规则计价明细「应收」行，与运费/泡比优惠（自带这两个字段）口径不一致；补显式字段后三张优惠表统一，优惠流水行费用类型直接取优惠规则自身配置、无需再反查父规则，也避免弹窗看不到锚定费用类型而配错 |
| 2026-09-23 | **服务组合优惠弹窗去掉描述文案（单条 + 批量）** —— 单条服务组合「优惠配置」弹窗去掉顶部「优惠在费用生成时实时取数…」、运费优惠 Tab「按客户等级 / 客户特批匹配…」与泡比优惠 Tab「基于货物泡比…」三处描述；批量添加优惠弹窗去掉「选择多个服务组合并配置优惠规则…」顶部提示条与运费优惠 Tab「按客户等级或客户特批匹配…」描述。PRD §5.11 页面规格、原型 `超级运价_服务组合.html` 同步 | 这些说明文案与计费引擎口径重复（优惠正负号、匹配优先级、叠加规则已落在 RDD/数据设计/PRD 规则与校验里），页面只需呈现结果与操作，不必再承载口径解释 |
| 2026-09-22 | **计费取数与变更重算规则从数据设计迁到 PRD §7.5** —— 收货完成算运费 / 附加费 / 优惠的计费取数链路、逐行口径、共性口径、计费取数入参清单、每类行参数依赖、运单变量变更重算矩阵、冻结边界，由数据设计 §6.4/6.5/6.6 迁到 PRD 新增 **§7.5**（业务规则归 PRD）；数据设计 §6.4/6.5/6.6 瘦身为「见 PRD R05/R06/R34/R35/§7.5」引用；超级运价 PRD R34/R35 及 §7.4、超级运价 RDD、财务 PRD R53/R56/A28、财务数据设计 §4.7 的交叉引用一并改指 PRD §7.5 | 计费生成与变更重算是业务规则，不该放数据设计（数据设计只管表 / 字段 / 接口契约），业务规则统一收敛到 PRD，避免两份维护打架 |
| 2026-09-21 | **附加费规则「字段&公式」触发节点改「收货完成」+ 人工添加枚举 30→50 + 新增费用类型引用约束**：① 字段&公式型附加费触发节点由「拣货」改为「运单收货完成」（收货过机即可取实测长宽高重量材积重、按箱计），`trigger` 枚举取消 20（拣货实测）；② `rule_type` 人工添加枚举 30→50；③ 新增约束——同一费用类型最多只能被一条人工添加型附加费规则引用（字段匹配 / 字段&公式不限制）。PRD（§2.8 / §5 / R05 / R34）、数据设计（§3.14 / §6.4 / §6.5 / §6.6）、RDD（§8 系列）同步 | 实测（过机）发生在收货环节，收货完成时已可取长宽高重量材积重，字段&公式型（超重超长）无需再等「拣货实测」节点，与字段匹配型一并随收货完成一次性生成；人工添加枚举改 50 与流水类型枚举避免撞号；同一费用类型关联多条人工添加规则会让「该收多少」出现两个手工来源、无法下钻举证，故限一条 |
| 2026-09-21 | **计费取数变更重算矩阵补全 + 优惠快照口径定案**：变更重算矩阵重写为完整清单——删「客户填尺寸」（下单预估只用于查价试算、不参与应收流水计费），新增「整单收费重 / 件数·箱数 / 泡比（→ 泡比优惠）」，字段匹配业务开关（报关方式 / 自送 / FBA / 出口先查后装）拆独立行，「客户」改「客户 ID（→ 反查等级/特批重匹配优惠）」，补「我司主体（换合同整单重算）」「单箱/总体录入」；补快照口径——客户等级 / 特批名单 / 优惠规则变更只影响后续生成、存量流水不追改；R34 入参业务开关补 `exportInspectFirst`。PRD（R34/R35）、数据设计（§6.6）、财务 PRD（R53）、财务数据设计（§4.7）同步；**同日把数据设计正文表名/字段名英化中文化**（产品看不懂英文表名，业务口径不变） | 原矩阵把下单预估与过机实测混在一行、漏收费重与泡比两个直接计费量、优惠匹配参数未显式列出；客户等级是主数据、非运单事件，需定快照口径防「等级一改历史账单全变」 |
| 2026-09-19 | **费用与标识规则：人工添加型规则动作类型仅「加收费用」** —— 规则类型=人工添加时，动作类型「标识提醒」隐藏不展示（仅「加收费用」可选），前端 `onTypeChange` 同步防御（切换到人工添加时若动作类型为标识提醒则强制改加收费用）。PRD（§2 动作类型两态 + §5.10 action_type 字段）、数据设计（action_type 字段）、RDD（AC7）、原型 `超级运价_费用与标识规则.html` 同步 | 人工添加型规则由操作员在运单界面手动触发、按计价明细加收费用（如贴标费），不存在"规则命中后打标提醒人工处理"的场景——「标识提醒」是字段匹配型（运单命中字段条件才打标）的语义，人工添加型天然只加收费用，故收口为仅「加收费用」可选 |
| 2026-09-19 | **服务组合优惠配置调整：主列表去掉「优惠」列 + 新增「批量添加优惠」入口** —— ① 服务组合、费用与标识规则两个主列表的「优惠」汇总展示字段去掉，操作列「优惠」按钮保留（优惠配置入口不变）；原型清理 `freightCountOf` / `densityCountOf` / `discountCountOf` 三个计数方法。② 服务组合工具栏「新增服务组合」旁新增「批量添加优惠」按钮 → 独立弹窗：勾选多个服务组合 + 配规则（运费/泡比双 Tab），保存后每条规则按目标展开、`scope_combo_ids` = 全部选中组合，批量挂到多个组合。PRD（服务组合列表字段表 + 页面规格 + A22）、RDD（H7 + AC13c）、原型两页同步 | ① 主列表「优惠」列只是"配了多少条优惠"的汇总计数，优惠明细与配置都在操作列「优惠」按钮的独立弹窗里，列表再占一列属冗余、白占宽度。② 逐个组合点「优惠」按钮配优惠太低效，补一个批量入口，一次给一批组合配同样的优惠 |
| 2026-09-19 | **字段匹配型计价单位修正：按数量 → 按箱** —— 附加费规则「规则类型 × 计价单位」口径校正为：字段匹配 = 按票/按KG/按m³/**按箱**、字段&公式 = 仅按箱、人工添加 = 仅按数量（数据设计 §3.14 与 PRD 此前把字段匹配误写成「按数量」，实际应为「按箱」）；原型 `超级运价_费用与标识规则.html` 字段匹配计价单位下拉补「按箱」。另顺带把「贴标费」人工添加型规则状态由「已冻结」改「正常」（财务侧「添加应收」来源规则候选对齐用） | 字段匹配型附加费（如按箱收取的超长费）需支持「按箱」计价，此前文档误写成「按数量」、原型又漏了「按箱」选项，三处口径不一致；「按数量」只在人工添加型（操作员填数量）使用 |
| 2026-09-16 | **费用与标识规则：编辑态「骨架置灰」** —— 编辑已有规则时，**规则类型 / 动作类型 / 维度（条件行的维度）三项置灰不可改**，条件行也一并**不可增删**（新增行必然要选维度）；三项置灰处给一句骨架说明条、增删按钮给悬浮说明。**新增规则时三项照旧可自由选**（否则建不出规则）。可改的只剩「参数级」内容：规则名称 / 取值 / 公式 / 计价明细 / 附加费标识 / 优惠。PRD（§5.10 + R37 + A21）、RDD §8.1 与原型 `超级运价_费用与标识规则.html` 同步 | 规则类型 / 动作类型 / 维度是规则的**骨架**，落库后再改等于换成另一条规则 —— 已配的取值、计价明细、优惠与**已生成的费用流水**都会失去对应关系（流水靠 `source_rule_id` 留痕，规则语义一变就答不出「这笔钱当时是按什么规则收的」）。要变更就新建规则，让新旧规则各自留下完整链路 |
| 2026-09-15 | **字段匹配型维度新增「出口先查后装模式」+ 附加费标识共用与打标去重** —— 字段匹配型维度由 10 个增至 **11 个**，新增「**出口先查后装模式**」（取值 **是 / 否**，**取自运单**）；原型 `超级运价_费用与标识规则.html` 同步（维度下拉 / 取值下拉 / 条件文案），并新增演示案例——**同一业务原因拆两条规则**：「出口先查后装加收（加收单价）」（按KG 1 元 · 出口费用）与「出口先查后装加收（报关费）」（按票 150 元 · 报关费），**两条规则共用同一标识「出口查验」**，列表另补**「附加费标识」列**让"多条规则共用一个标识"一眼可见；三份文档（RDD / 数据设计 / PRD）与版本索引同步。**同日追加（打标去重）**：新增 **R36 附加费标识打标去重（幂等）** —— 打标按**去重键 = 运单 + 标识**判重，同一运单同一标识**只打一次**（多条规则共用同一标识同时命中、人工添加与自动命中撞标，均不重复打、不报错），而**每条命中规则的流水仍各自带来源规则留痕**；**同一 `tag_id` 明确允许被多条规则共用**（不设跨规则唯一性约束）；财务侧「添加应收」的打标口径同步明确化 | 出口先查后装模式是**运单级业务开关**，此前要按它加收只能拆多条规则（用服务组合等间接条件变通）或走人工添加型手工补费，纳入字段匹配维度后可直接按运单字段配置，与「自送标识」「是否FBA运单」同口径。**同日追加**：同一业务原因要出两笔应收，一期靠拆规则实现；若两枚标各不相同，下游按标识归类费用会分裂、运单上并排两枚近义标也难解释，故取「**共用同一标识 + 系统打标去重**」——标识归集口径保持唯一，且不必放开计价明细多行（计费与优惠锚定模型零改动）。演示数据里即现成反例：「粉末品名加收（按KG）」与「粉末品名加收（按m³）」两条规则共用「粉末品名加收」，同票命中即会打两枚 |
| 2026-09-14 | **撤回员工端「超重超长」提示条**（09-12 决策 6 的员工端部分）—— 移除 `Waybill.html`（运单管理·费用管理 tab）顶部的「超重超长费用将在拣货实测后计算」说明条及其细则（字段&公式型判定依据 / 费用重扫 / 审计后冻结 / 进账单冻结 / 人工添加型永久豁免）。**规则口径不变**：字段&公式型附加费的正式触发与流水生成仍在**拣货实测**节点，运单信息变更触发重扫、审计后冻结、进账单彻底冻结、人工添加型永久豁免重扫——这些规则仍写在 RDD / 数据设计 / PRD 中，只是不再以页面说明条承载。**货主端 `查价订舱.html` 报价区上方的提示条未动**（本次指示仅指运单管理）。**同日另**：工作台「导入成本 / 导入加成额」入口在原型中由 `v-if=false` 隐藏改为**恢复展示**（导入能力仍为一期 P0，D9–D12 / F5 与 AC30–AC34 不变） | 说明条属口径解释，放在员工端业务操作页面上不合适（页面只需呈现结果与操作）；口径有文档与计费引擎承载，不需要第二处表述 |
| 2026-09-14 | **「批量设置优惠」一期不做 + 优惠一期即按独立营销模块落地 + `surcharge_discount` 父实体泛化** —— ① 「服务组合列表勾选多条 → 批量设置运费优惠 / 泡比优惠」的入口与对应验收标准（AC3a / AC3b）从三份文档中删除（文档先行、原型从未实现）；② 三张优惠表明确为**营销模块的独立表**、接口为**独立子资源**（`GET/PUT /api/service-combinations/{id}/freight-discounts` 运费优惠、`GET/PUT /api/service-combinations/{id}/density-discounts` 泡比优惠、`GET/PUT /api/surcharge-rules/{id}/discounts` 附加费优惠 —— **三类优惠各自一个子资源**），**父实体接口不再含优惠子表**；原「一期挂在父实体属过渡形态、二期才新建营销模块」的定性改写为「**一期即独立落地，只是配置入口复用父实体列表操作列**」；③ `surcharge_discount` 的父实体引用由 `surcharge_rule_id` **泛化为 `fee_rule_type` + `fee_rule_id`**（一期恒 20=附加费规则），唯一性键 / ER 图 / 二期迁移映射同步，**页面与原型不受影响** | ① 批量设置是文档比原型早、一期确认用不上；② 优惠已由父实体主列表操作列的「优惠」按钮打开**独立弹窗**维护，保存时机与父实体不是同一次请求——塞进父实体 PUT 会出现"改优惠顺手覆盖了父实体其他字段"的并发风险，且二期营销模块迁移时独立子资源更好收敛；③ 泛化父实体引用为后续同类费用规则复用同一张优惠表预留，属纯数据层改动。**同日另**：核对功能清单后，把正文与分期的功能数由 **59 校正为 67**（统计口径 = 功能清单表 P0 行逐行计数，二期 3 项为模块 K 审计与日志），并把该口径写进 RDD §15；货主端文档对「超级运价侧 Phase 1 功能数」的引用一并同步。**同日再校**：功能数经复核补入「重量段模板管理」（C9）与「折算基准管理」（D13）两条，正文与分期口径由 67 → **69**（总 72 项），并同步 PRD §1 / §5.11 与货主端引用。**同日按原型统一列表列**：服务渠道列表删「目的国」、补「包税方式 / 服务类型 / 尾程派送方式」（字段均在 `service_channel` 上，只是此前未列入表格）；成本项模板列表按原型改为「成本项 / 建议默认价 / **启用默认价** / 状态 / 操作」（「是否换汇」为派生字段、列表与编辑弹窗均不展示）；费用与标识规则列表删「生效范围」、补「触发条件·公式 / 费用类型 / 费用方向」 |
| 2026-09-14 | **运单收货完成 → 应收流水取数链路定稿（数据设计新增 §6.5）+ 计费取数接口** —— 明确逐行取数口径：运费行取**当周批次**运价行公布价；运费优惠 / 泡比优惠按服务组合与客户身份**实时匹配**（客户特批 > 客户等级、泡比取阈值最大档且仅作用 KG 计价行）；附加费行匹配**动作类型=加收费用**的规则、附加费优惠按父规则匹配；**优惠独立成行、费用类型取自优惠规则配置的 `fee_type`**；每条流水留痕**来源规则类型 + ID + 规则名**。新增被财务调用的 **`POST /api/pricing/calc`**（与查价 `POST /api/freight-rate/query` **共用同一套计算内核**，差别只在 caller 与是否落库）；**明确「计价单位跟着规则走」**——运费 / 运费优惠 / 泡比优惠行的计价单位 = 运单所关联**服务组合的计费方式**（KG / m³），附加费 / 附加费优惠行的计价单位 = 该条**附加费规则的计价单位**（按票 / 按箱 / 按KG / 按m³ / 按数量），**优惠值量纲 = 元 / 计价单位**（与公布价、应收单价同口径），故一律 `金额 = 单价 × 计费量`；§3.20.4 优惠明细缺口收窄为「同一费用项上多条优惠的叠加运算顺序明细」与「浮动优惠的券核销载体」，二期 `discount_apply_log` 承接 | 财务侧应收流水生成必须有唯一定稿的取数口径，否则会出现"查价一套、落库另一套"；优惠独立成行 + 来源规则留痕后，运单审计能直接回答「这笔钱是哪条规则给的」，一期优惠明细缺口随之收窄；查价与落库共用计算内核可避免两套口径漂移。**同日明确**：附加费优惠**不单独配置费用类型** —— 其流水行的费用类型取**所属附加费规则计价明细「应收」行的费用类型**（与附加费行同类型）；运费优惠 / 泡比优惠仍取各自规则上配置的 `fee_type`。**同日补齐接口契约**：数据设计新增 **§6.6 计费取数入参清单与变更重算矩阵** —— 明确 `calc` 的五组入参（调用上下文 `trigger`/`changedFields[]`/`batchId`、运单与身份、服务与路径、货物参数含拣货实测、业务开关）、**每类流水行的参数依赖**，以及**运单变量变更 → 重算矩阵**（服务组合变更 = 五类全部重算；重量/体积/箱数变化可能重量段跳档；备注/附件等不触发）；PRD 新增 **R34 计费取数入参**（运单只传自身字段，尾程/计费方式/客户等级等由服务端反查）与 **R35 运单参数变更 → 重算**（由财务 `recalculate` 整单重跑覆盖） 　**同日追加（手工触发也打标）**：**人工添加型附加费规则被手工触发时同样打标** —— 财务「添加应收」选中该规则后，保存成功即按规则 `tag_id` 为运单打上该**附加费标识**（与规则自动命中时的打标口径一致、同标识不重复打、打标失败仅写后台任务告警不回滚流水，未选规则的手工行不打标）。缘由：「加收费用型规则的标识本就打在运单上供后续费用归类」（§5.10 附加费标识字段说明），人工触发的附加费若不补打标，同一运单的标识会因触发方式不同而不一致，下游按标识归类费用时会漏 |
| 2026-09-12 | 「超重费提示文案」落地（决策 6）—— 员工端 `Waybill.html`（运单管理·费用管理 tab）与货主端 `查价订舱.html`（报价结果区上方）各加提示：**「超重超长费用将在拣货实测后计算」**；员工端另附「字段&公式型以拣货实测尺寸/重量为判定依据、下单与收货阶段暂不体现、运单信息变更触发重扫、业务审计后冻结、进账单后彻底冻结、人工添加型永久豁免重扫」，货主端按客户口径说明「报价为预估、实际费用以实测结果为准」 | 「超重超长」属「字段&公式」型附加费，触发节点是**拣货**（要用实测尺寸重量），而客户在下单/查价时看到的是自填数据 —— 不提前告知会出现「下单时说 0 附加费、账单多出几百元」的纠纷；提示条同时把**重扫冻结边界**讲给操作员，避免误以为审计后还能改 |
| 2026-09-12 | **三项范围变更 + 两张原型页调整**（原型与 RDD / 数据设计 / PRD 四份同步）：① **「客户特批」进入 Phase 1 且三张优惠表统一**——运费优惠 / 泡比优惠 / 附加费优惠的「优惠对象类型」一期同时开放客户等级与客户特批，目标客户（多选）一期可用；优先级「客户特批 > 客户等级，高覆盖低不叠加」一期即生效。② **「飞书审批编号」进入 Phase 1**——客户特批时必填，UI 新增「审批编号」列（仅在存在特批行时出现）；**一期为人工录入编号、不对接飞书审批流**（不做审批单同步与「审批中/已通过」状态机），编号仅作追溯凭据。③ **「泡比优惠」整体进入 Phase 1**——密度阈值阶梯 + 固定减免 + 客户等级/客户特批 + 永久·固定期限 + 费用方向固定应收；多条命中取阈值最大档（不叠加），与运费优惠叠加；**Phase 1 功能数 55 → 59**，模块 M 优先级 P2 → P0。④ 原型调整：`费用与标识规则` 页计价说明改「成本预估、销售成本方向后续拓展」、字段匹配计价单位去掉「按数量」、服务组合维度取值加「全选/反全选」；`服务组合` 页给泡比优惠补上装配与回写链路（此前 openEdit 完全未装配，该节恒空）并补客户特批/泡比演示数据。**仍留 Phase 2**：单次有效、运费与泡比的折扣率、浮动优惠、营销模块、叠加顺序可配、飞书审批流自动对接 | ① 只给两张表放开客户特批会让「三张优惠表共用同一套 target_type 枚举、口径必须一致」这条既有原则失效，同一枚举在不同表里含义不同会让开发与运价管理员都踩坑，故三表统一；②「客户特批」的存在理由就是可追溯是谁批的，没有审批编号的特批＝无法举证的特殊折扣，财务与审计必追问，故编号随特批一起进一期（但审批流对接仍属二期，一期只录编号）；③ 泡比优惠是既有已建模能力（`density_discount`），原型早已恢复该节，文档却仍写「二期不做」，属文档与原型不一致，且它本就是「额外优惠」里最易落地的一项，故整体回归一期。**另（同日口径收口，并入本条）**：① 「交货站点」取值来源锁定为 `仓库列表_主列表.html` 的「本地仓」Tab、不依赖「末端管理」；② 明确**优惠取数口径**——费用生成时从三张优惠规则表（运费/泡比/附加费）按当周批次 + 客户身份实时匹配，**查价时点的优惠金额不具约束力**；③ 明确**优惠唯一性的精确口径**——同一父实体 × 同对象类型 × 同目标最多一条**生效中**规则（已失效不占名额），**泡比优惠另加「同一密度阈值」维度**（阶梯可按不同阈值各配一条），该阈值限定同时补入 RDD §9.3 与 PRD §2.9/§5.11/R32（此前仅数据设计写对）；④ **不加「手动过期」入口——直接删除规则即可**（三张表通用，删除仅影响后续计算）；⑤ 「单次有效」状态机补 `★ 二期` 标注（PRD §3.3 + A12）；⑥ §14 算例**重算**为含泡比优惠的完整口径（美森360 `10.0−0.5−0.8+1.0=9.7` 等）；⑦ 文档补「**同一客户同时被特批与等级命中时只取特批那一条参与计算、不累加**」的覆盖语义；⑧ `tag_id` 类型统一为**长整数**（原 PRD 写小整数、数据设计写长整数）；⑨ §6.1 查询链路图补「泡比优惠」节点与「超重超长在拣货触发、查询阶段不计算」说明 **另（同日追加）**：① **运价查询新增「客户等级」查询条件**——原查询区只有「当前客户」一个优惠相关条件，导致**销售按等级报价拿不到干净结果**（必须选具体客户、且不同同等级客户可能各有特批）；现新增独立「客户等级」下拉（SS/S/A~G 共 9 级），**只选等级不选客户 → 只匹配等级优惠、不匹配任何客户特批**；选客户 → 等级自动带出且下拉置灰、特批优先覆盖等级。货主端与公开计算器**不加**该条件（分别取登录货主身份 / 无身份）。② **明确「优惠值正负号铁律」并修正说明文案**——**固定减免填正数=减钱（正向优惠）、填负数=加价（反向优惠）**（系统按 `金额 − 优惠值` 计算，存正数才是减钱）；折扣率 `<1` 折扣 / `=1` 原价 / `>1` 加价（一期 0.01~1 不支持加价）；列表一律正数显 `-¥X`、负数显 `+¥X`。运价查询费用明细弹窗底部说明补全该口径（原文仅写「优惠支持负数（加钱）」，方向本就正确，现补足"正数=减钱"以免误读）\|**另（同日追加②）**：**「优惠值正负号」整体翻转**（产品与开发共同确认，覆盖原约定）——**固定减免：负数=减钱（正向优惠）、正数=加价（反向优惠）**（原「正数=减价」作废）；计算随之由「金额 − 优惠值」改为「**金额 + 优惠值**」；显示口径改为**负显 `-¥X`、正显 `+¥X`**；附加费优惠上限校验改为**绝对值比较**「`\|优惠值\| ≤ 应收单价`」（否则负数永远通不过）；**折扣率不变**（乘数：`<1` 折扣 / `=1` 原价 / `>1` 加价，一期 `0.01~1` 不支持加价）。原型侧同步翻改 4 个页面（运价查询 / 服务组合 / 费用与标识规则 / 货主端查价订舱）的 mock、算式、显示与校验。**同时修掉一个报价缺陷**：运价查询原只有「当前客户」一个优惠条件，按等级报价时不含客户特批会导致**报价虚高**（实测：S 级通用价 `¥9.0/KG` 而该等级下 C001 特批实为 `¥8.5/KG`）——已新增「客户等级」条件，并在按等级查询时给出**提示条 + 每行标出该等级最优特批**，引导销售改用「当前客户」以取得准确价**另（同日追加③）**：① **字段更名** `discount_amount` → **`discount_adjustment`**（中文仍「优惠值」）——原名字含「额」，取正数时却是加价、名实不符；**未用 `discount_value` 是因该名已被更早的废弃字段占用**（同名两义）；`discount_rate`（折扣率乘数）不变。② **「特批劣于等级」处置落定**：仍按「特批覆盖等级」执行（特批可优可劣，保留"收紧"能力），但**配置时弹窗警示**；原型在 `超级运价_服务组合.html` 与 `超级运价_费用与标识规则.html` 已实现（运费/泡比/附加费三类优惠都检测，客户列表补 `level`）。③ **「列值 vs 显示文案」写进 PRD §5.12 与数据设计 §3.19**，防前端渲染出 `--¥0.5` 双重负号。**另（同日追加④）**：**优惠配置入口从「父实体新增/编辑弹窗」独立出来**——`服务组合` 与 `费用与标识规则` 两页的新增/编辑弹窗**不再承载优惠**（服务组合编辑弹窗折叠区 **4 → 2**，只剩「基本信息 + 计费规则」；费用与标识规则由 4 步收敛为 ①触发条件 / ②执行动作 / ③附加费标识 3 步）；改为**主列表操作列新增「优惠」按钮**，点击打开**独立「优惠配置」弹窗**（服务组合：顶部信息条 + 「运费优惠 / 泡比优惠」两个 Tab，泡比 Tab 在计费方式≠KG 时提示"只作用于按 KG 计价的运价行、对该组合不生效"；费用与标识规则：单表，顶部展示**应收单价**与「`\|优惠值\| ≤ 应收单价`、折扣率 `0.01~1`」上限口径，**动作类型=标识提醒的规则该入口置灰**）。两页主列表各新增一列**「优惠」**（服务组合显示 `运费 N` / `泡比 M` 标签；费用与标识规则显示 `N 条` / `未配置` / 标识提醒显 `—`）。**已失效的优惠行一并载入并灰化只读**（可查、不可改、不占唯一性名额）；父实体冻结时优惠弹窗转只读。**优惠仍无独立页面**，一期入口仍挂父实体列表，二期由营销模块统一承载；**新增组合需先保存才能在列表配优惠** \| 优惠与父实体本体的编辑节奏与权限并不相同——组合/规则的字段（服务渠道、计费规则、触发条件、公式）属**结构性配置**、改动频率低；而优惠（尤其客户特批）是**按客户、按季度反复调整的经营性配置**。混在一个 1200px 大弹窗里改一次特批要滚过全部结构字段；且新增组合时优惠行会被表单校验连带拦截、连草稿都存不下。独立入口同时让「这个组合/规则到底有没有优惠」在主列表一眼可见（靠新增的「优惠」列），避免运价员漏配、也避免冻结后才想起还有优惠挂在里面 |
| 2026-09-12 | 附加费优惠编辑子表**去掉「优惠后单价」列**（费用与标识规则页，新增/编辑规则第四步）—— 优惠表仅保留 7 个可配置列（对象类型 / 目标 / 优惠方式 / 优惠值 / 有效期 / 有效期起止 / 状态）；同时删除仅供该列使用的 `discountAfter` / `discountAfterText` 两个方法及 `receivableUnitShort` computed。PRD 同步补「该子表不提供优惠后单价预览」口径并升至 V2.24（日期前缀 09-11→09-12），RDD / 数据设计无需变更 | 「优惠后单价」是**计费时点的计算口径**而非可配置字段：同一等级可能命中多条、折扣率与固定减免先后顺序影响结果，单行预览给不出终值，反而会与计费引擎形成第二套口径；页面只需保证输入合规（`优惠值 ≤ 应收单价`、折扣率 `0.01~1`、无应收行拒绝配置），结果一律由计费引擎算 |
| 2026-09-11 | 附加费规则「规则类型三态化 + 优惠量纲落定」（第二批，原型已定稿、三份文档同步）：**规则类型改为三态**——字段匹配（维度条件 AND，≥1 条）／**字段&公式**（新增：维度仅「服务组合」多选且最多 1 条 ＋ 公式条件 OR 且必填；**计价单位仅按箱**）／人工添加（**计价单位仅按数量**）；原「公式阈值」类型废弃并入「字段&公式」。**菜单收敛并定名**：删除「事件&规则」菜单（该页删除、菜单项移除），原「附加费规则」菜单**更名为「费用与标识规则」**（页面文件同步改名；页面标题、列表卡片头、新增/编辑/查看弹窗标题、保存提示、状态变更提示五处同名），公式类规则统一由「字段&公式」承载。**「公式条件」补必填星标**（字段&公式型：0 条公式或存在空白公式行均拦截保存）。**修复动作类型选「标识提醒」时「附加费优惠」仍残留可见**的缺陷。**动作类型去「阻止下单」**（三值→两值）。**附加费标识改单选且必选**（不分动作类型）。**计价明细**：费用方向固定「应收」、一期仅 1 行、同一费用方向最多 1 行。**附加费优惠**：仅「加收费用」时显示；优惠量纲定为**应收行单价级**（`优惠值 ≤ 应收单价`、折扣率 `0.01~1`）；对象类型仅客户等级、有效期仅永久/固定期限、去审批编号列；**「可优惠级别」字段与可优惠性校验能力一并删除**。**触发节点改为后台预置**（字段匹配→运单收货完成／字段&公式→拣货／人工添加→人工触发；运单信息变更触发重扫，审计后与进账单后冻结），原型不展示、数据结构保留。RDD/数据设计/PRD 三份同步 | ①「超重超长」判定天然是「维度 且 公式」，原「规则类型三选一」的模型强迫二选一——要么丢维度、要么丢公式，模型本身错了；②拆「事件&规则」菜单后两页内容高度重叠（同一套维度+公式），维护与理解成本翻倍，故合并回单菜单；③附加费优惠原无计价单位归属，「减 0.5」量纲不明（同规则下按 KG 1.5 与按 m³ 200 差 133 倍），且 RDD 按单价级、PRD 按金额级互相矛盾，统一为单价级；④「可优惠级别」想表达的"最多能优多少"已被「优惠值 ≤ 应收单价」精确表达，且该字段是旧校验唯一输入源，删除后改由数值约束承担；⑤原「附加费规则」这个名字只能概括"加收钱"，而本菜单还承载「标识提醒」这类**只打标不收费**的动作，且与已删除的「事件&规则」在语义上无法区分，故定名为「费用与标识规则」 |
| 2026-09-11 | ① **优惠体系治理**：三张优惠表优惠值字段拆分（原共用字段整数位仅 2 位、上限 99.9999，而 m³ 组合加成额即 250 元/m³、附加费「可全免」为数百元级，一期即会溢出，拆为折扣率与固定减免两个字段）；RDD 新增「优惠体系演进路线」——一期固定优惠内嵌于服务组合/附加费规则属过渡形态，二期新建营销模块统一配置固定+浮动优惠（优惠券等），并厘清「固定减免/折扣率」＝优惠方式、「固定/浮动」＝优惠性质；分期汇总修正为营销模块整体入 Phase 2、原 Phase 3 独立促销引擎改为营销深化；数据设计新增优惠冲突与叠加矩阵、二期优惠规则统一模型（目标态表清单 + 一期→二期字段级迁移映射，明确一期无需预留字段、但父实体外键禁用级联删除）；PRD 新增优惠上限强制截断（任一费用项不得为负）/冲突判定/父实体删除保护三条规则，并修正优惠方式枚举（折扣率一期仅附加费优惠可用）。② **附加费规则引擎调整**（原型 + 文档）：字段匹配维度新增「服务组合」（下拉多选）、移除「用户等级」（等级优惠统一走优惠规则，不留两个入口）、「本地仓」更名「交货站点」（改下拉多选，取仓库列表-本地仓的仓库名称）、「用户」取值改下拉多选（取客商中心客户）；维度与取值均改必填并逐行校验；公式阈值补齐算术运算符 + - * / ( ) 与括号（此前只声明比较符，而超重超长判定式天然含括号）；附加费案例「粉末加收」触发条件去掉「且 尾程=卡派」。③ **优惠一期范围收缩（原型已同步）**：泡比优惠（模块 M）整体移入 Phase 2、「客户特批」与「单次有效」移入 Phase 2（二者同属"额外单次优惠"场景，一期无审批流与单次核销承载）、运费优惠的费用方向前端固定应收；服务组合编辑弹窗已移除泡比优惠规则整节、对象类型仅保留客户等级、有效期仅保留永久有效/固定期限、审批编号列移除；Phase 1 功能数 59→55。④ **登记两个已知缺口（一期接受、二期闭环）**：优惠算完查不到明细（财务只落汇总值）；**报价不留痕**——查价时点优惠实时算且不落库、收货时重新匹配，而优惠规则不随周批次快照、运单也不存报价，导致客户对质时无法举证。二期由 `query_log` + `discount_apply_log` + 运单报价快照三件套闭环 | ① 优惠值类型缺陷一期即溢出属必修；一期内嵌配置不是终态，需明确演进路线与分期，否则开发按老口径排期会做出只有折扣率、没有活动与券的半成品；二期浮动优惠多出「配额消耗」与「领取-核销」两个状态机，一期需把绑定抽象、冲突矩阵、落库缺口写清。② 附加费条件维度缺「服务组合」导致"仅某几个组合加收"只能拆多条规则变通；「用户等级」与优惠规则的等级语义重叠会打架；本地仓/用户取值靠自由输入会造成同义异写、匹配静默失效；公式不支持括号会把超重超长算错。③ 泡比优惠是个案化的"额外优惠"，与一期已取消的单次有效强相关，硬做会出现"一次泡比优惠永久挂着"；客户特批需飞书审批与单次核销，一期不具备承载能力 |
| 2026-09-10 | 服务组合编辑弹窗：运费优惠、泡比优惠规则表各新增「费用方向（应收/应付）」「费用类型（来源基础资料-费用类型）」必填字段，置于审批编号后；运价更新工作台「重算成本底价」「已知晓不调整」点击后弹窗列出带 ⚠ 服务组合可多选，仅对选中项执行；RDD/数据设计/PRD/原型同步 | 优惠需明确作用的目标费用方向和费用类型；待重算需支持按服务组合选择性处理，避免一刀切 |
| 2026-09-08 | 货物属性改「来源商品管理-货物属性（技术配置可配）」不再硬编码；交货地/本地仓统一为「交货地」、枚举来源仓库列表-本地仓（花都仓/白云仓等仓库名） | 货物属性是商品管理可配的，不是硬编码；交货地=本地仓=仓库名，非区域（珠三角） |
| 2026-09-07 | RDD/数据设计对齐原型：流程八查询入口补邮编/渠道专线/简易多件双模式、计费方式改结果筛选、目的仓点改仓库代码、产品类型改货物属性；泳道图「发布」改系统定时流转；枚举字典补渠道专线/货物属性/客户等级、批次状态补已作废；数据设计字段名中英混写统一英文、理赔/尾程编码统一、成本段布尔改状态枚举、附加费优惠补折扣率 | 三份文档与原型不一致，研发按文档实现会做错查询入口/流程图/枚举/字段 |
| 2026-09-07 | 员工端运价查询：结果表新增「附加费优惠」「泡比优惠」独立列 + 加价演示客户 + 结果分组默认展开 + 修正美森360 m³包税成本 + 邮编改输入框按前缀计价 + 填邮编过滤卡派且专线联动 + 收货仓降为选填且结果表加「交货地」列 + 拖车费计入公布价 + 选交货地过滤组合 + 无匹配组合不显示 + 未选交货地不返回有拖车费组合 + 仓库代码与邮编互斥(二选一)；PRD/原型同步 | 附加费优惠原内联易忽略、泡比优惠需独立列、缺负数加钱场景；分组需手工展开；m³包税成本抄错误判红色；邮编无固定菜单应自由输入、填邮编即快递派场景；收货地是通用影响因素应交由结果表展示；拖车费按§7.3应同时计入公布价与成本底价、选交货地仅返回覆盖该地的组合；不符合查询条件的组合不显示、未选交货地时拖车费算不准故不返回有拖车费的组合；仓库代码(FBA仓)与邮编(快递派地址)为互斥目的地，填一个自动清空另一个 |
| 2026-09-07 | 公开计算器对齐员工端：查询条件加交货地/邮编、仓库代码改选填(与邮编互斥)，结果表改「服务组合/交货地/公布价/附加费/最终报价/船期链接」，去掉模拟登录、立即登录跳转货主端登录；员工端结果表去掉「船期/最快提取」列；PRD/原型同步 | 公开计算器查询条件与员工端不一致维护成本高；公开页纯免登录，登录应跳货主端而非页内模拟；船期以链接弹窗呈现、结果表只展示公开层信息 |
| 2026-09-07 | 公开计算器补简易/多件装箱+渠道专线勾选+计价列；货主端查价订舱去掉当前客户下拉(后台默认当前货主)、仓库代码/邮编/专线联动同员工端、结果表改「服务组合/交货地/计价/公布价/运费优惠/泡比优惠/附加费/附加费优惠/最终报价/查看运输详情」+报价默认展开；RDD/PRD/原型同步 | 公开计算器查询体验对齐员工端；货主端客户身份应由登录态决定而非下拉切换，且联动逻辑与结果列需与员工端一致 |
| 2026-09-04 | 运价查询页（运营端）新增「交货地(收货仓·必填) + 邮编(快递派)」查询字段；成本底价改查本周运价成本快照（costTable+拖车费+快递派）；利润/盈亏移到最终报价后计算（含运费优惠/附加费优惠/泡比优惠，优惠支持负数加钱）；快递派无邮编自动过滤；KG组合补8KG+重量段+补美森360不包税/美森432包税案例；PRD/原型同步 | 成本底价原为固定值、未按交货地/邮编联动；盈亏原按运费侧(公布价-成本)判，未计入附加费与优惠，且优惠可能负数加钱；快递派必须有邮编才能报快递派单价 |
| 2026-09-04 | 公开运费计算器从货主端独立页改为员工端「头程管理→运价管理→公开计算器」菜单项（新窗口打开，供运价管理员预览/取分享链接），并内部对齐（结果列「渠道」改「服务组合」+ 包税筛选 + 完整客户优惠链）；PRD/原型/版本索引同步 | 公开计算器面向官网/公众号分享场景，应由员工端管理入口而非货主端嵌入；其报价维度与优惠逻辑需与运价查询页一致 |
| 2026-09-04 | 运价查询页（运营端+货主端）包裹录入改「简易(总重+方数)/多件装箱」双模式 + 查询区加当前客户下拉 + 结果列「渠道」改「服务组合」显示完整组合名 + 接通运费优惠/泡比优惠/附加费优惠并完善费用明细计算链；PRD/原型同步 | 销售/客户常只报总重方数需直录；报价维度应落在服务组合而非渠道；运费优惠此前为空壳、费用明细未演示完整计算链 |
| 2026-08-31 | 运价查询页（运营端+货主端）布局重构 + 新增公开运费计算器独立分享页；RDD/PRD/原型同步 | 查询条件与结果过滤分层混杂、结果表列过多、包裹弹窗不直观；公开计算器遗留已久，开发即将开发到询价出口 |
| 2026-08-24 | 服务渠道重量段「启用」改回「删除」（操作列仅删除，二次确认，删除后按段名称级联删除服务渠道及组合快递派单价费率行）；重量段模板编辑时运输方式/计价单位只读，新增/编辑/启用时校验重量段区间不重叠（重叠阻止并提示）；RDD/数据设计/PRD/原型同步 | 重量段应支持删除并级联清理快递派成本细项避免引用残留；运输方式/计价单位为重量段匹配键，编辑时锁定防止破坏已引用关系；启用冻结段时需去重避免出现区间重叠的重复段 |
| 2026-08-07 | 新增删除级联规则（渠道有影响因素的成本明细删除 → 保存时自动同步删除组合中对应行）；重量段删除按钮改为启用开关（去掉删除级联）；工作台新建周批次"从上周复制"改为"从历史批次复制"并增加批次选择下拉（必填）；RDD/数据设计/PRD同步 | 渠道删除成本项后组合侧残留数据需级联清理；重量段通过启停管理而非删除；当周最后一天开下周预告时复制上周会错位 |
| 2026-08-07 | 同步弹窗修正：拖车费去掉括号加交货地列、卡派仓点成本加拆送价/组合一口价/整柜直送价列、快递派单价修复显示"undefined"并加邮编前缀列 | 同步信息不完整，运价管理员无法在弹窗中核对明细 |
| 2026-08-07 | 成本项管理去"是否换汇"字段；服务渠道/组合成本项编辑界面影响因素（交货地/仓库/重量段/邮编前缀）已有值时只读、新增时可编辑；揽收段拖车费表增加币种显示列；服务渠道+组合三个特殊成本项币种置灰不可编辑；取消/保存按钮增加二次确认弹窗；卡派仓点成本同步弹窗去掉单价列 | 简化成本项配置；影响因素已有数据后编辑会导致匹配断裂；币种应由系统固化；误触取消导致数据丢失；卡派同步已有拆送价等明细无需重复单价 |
| 2026-08-01 | 服务渠道重量段"追加自定义段"改为单段选择器：从匹配模板中筛选未绑定的独立重量段，选中后自动绑定；绑定段参数由模板定义不可编辑；PRD/数据设计/用户需求同步 | 重量段应来源于模板而非自由创建，保证数据一致性；绑定后参数锁定防止误改 |
| 2026-07-27 | 新增成本Excel导入（普通成本项/拖车费/卡派仓点/快递派单价）+ 加成额导入，入口统一在工作台；新增三层询价体系（运营端/货主端/公开计算器）+ 公开运费计算器分享链接；RDD/数据设计/PRD同步 | 成本更新和公布价调整缺少批量导入手段，运价管理员每周一手工录入效率低；客户询价缺少对外出口，新媒体无法分享运价链接 |
| 2026-07-27 | 折算基准快照从4字段改为数组（从全局表拉取全部启用行）+ 快照表格KG/CBM列可编辑（运价管理员可针对本周覆盖全局默认值，快递派行锁定为1）；批次栏汇总信息行移除；批次名称改为可点击链接弹出基本信息卡片；PRD/数据设计/原型同步 | 折算基准存在海运/空运等多运输方式维度，单一4字段无法覆盖全量；部分周次需临时调整折算系数应对特殊货物结构，快照需支持本周覆盖；汇总行信息可通过点击批次名查看 |
| 2026-07-27 | 三个预置成本项统一编辑规则：模板层编辑仅名称可修改；批量更新按钮全部隐藏；默认价/币种/是否换汇固化。组合成本项操作列「启用」改「删除」对齐渠道页；派送段整段开关放开不受尾程方式限制；禁用后成本明细置灰不隐藏；拖车费「新增交货地」按钮统一移至段标题右侧（渠道+组合均支持）；RDD/数据设计/PRD/原型同步 | 预置成本项核心字段系统固化；组合与渠道操作一致性；派送段开关应允许独立控制；禁用后隐藏导致用户看不到配置内容；新增按钮放在标题栏便于批量操作 |
| 2026-07-23 | 从上周复制改为骨架重生+决策继承：本周启用航线/组合笛卡尔积铺底→匹配旧批次→命中继承加成额/公布价（成本底价用本周costRefMap重算）、未命中初始化并标记「新增」、失效架构自动移除；船期复制只保留骨架（航线+截关仓点+航程）、清空ETD/ETA/最快提取/截关时间；全部运价行标记待复核；RDD/数据设计/PRD/原型同步 | 底层架构持续变化（航线增减、成本项增减），纯数据深拷贝导致失效引用残留和新增架构遗漏；骨架重生对齐当前架构状态，决策继承保留运价员判断 |
| 2026-07-22 | 工作台本周运价重构：主表行=服务组合×交货地组，展开子表按派送方式分化（卡派=仓库组+成本底价列+重量段，快递派=邮编前缀+重量段成本嵌入每格），编辑按钮移至主表行；RDD/PRD/原型同步 | 交货地影响拖车费→影响成本底价，运价员应站在"组合+交货地"视角报价而非微观到仓库组；快递派成本随重量段而异无法抽固定列 |
| 2026-07-22 | 折算基准新增基准类型字段（head/tail）：head时仅维护头程KG/CBM、尾程方式置灰；tail时仅维护派送KG/CBM、尾程方式必填（卡派/快递派）。新建批次弹窗+批次栏同步；RDD/数据设计/PRD/原型同步 | 头程与派送折算基准独立管理，避免同时配置混淆；尾程方式作为 tail 基准的必填维度 |
| 2026-07-20 | 揽收段装柜费+拖车费标记为预置项（不可禁用/删除/批量更新），成本项弹窗拆分揽收段到独立「按交货地分别定价」区域；RDD/数据设计/PRD/原型同步 | 揽收段有交货地影响因素，与无影响因素的成本项混排导致表格混乱；预置项防止误操作 |
| 2026-07-17 | 周批次：新增已作废状态；预告和当前均可作废；去掉手动发布按钮（系统按日期自动流转）；汇率改为多币种数组快照；新建弹窗改为Tab结构（折算基准快照+汇率快照表格） | 作废替代废弃覆盖更多场景；汇率支持多币种对；发布自动化避免人为失误 |
| 2026-07-17 | 计费规则：计重精度→收费重精度；去掉尺寸精度字段；新增计重方式（按票/按箱） | 计费规则明确收费重计算方式；尺寸精度字段冗余 |
| 2026-07-13 | 重量段模板去掉排序字段 + 附加费规则去掉触发节点字段（系统自动确定）和批量设置附加费优惠功能；RDD/数据设计/PRD/原型同步 | 精简界面：排序由起始值确定，触发节点由规则类型确定，批量设置使用频率低 |
| 2026-07-13 | 计费规则「收费重方式」与「计费方式」级联（KG→3选项/m³→3选项互斥），weight_method 枚举新增 40/50/60；RDD/数据设计/PRD/原型同步 | 防止选错：KG 计费时不应出现 m³ 的收费重方式，反之亦然 |
| 2026-07-13 | 成本项模板「公式类型」改为「是否换汇」（是/否），根据币种自动派生、界面不可编辑；建议默认价+币种与是否换汇交换位置；新增界面去启用默认价开关（默认启用），主列表启用默认价改为只读标签；RDD/数据设计/PRD/原型同步 | 简化操作：是否换汇由币种决定无需手动选；启用默认价始终开启减少配置步骤 |
| 2026-07-10 | 利润模型从"利润率%"改为"固定加成额"（元/计价单位）；砍掉全局利润策略池实体，改为服务组合字段 `default_markup`；公布价=成本+加成额；盈亏灯统一为"只判运费侧"：工作台=加成额 vs（最大运费优惠+最大泡比优惠），销售查询=运费净利，附加费不并入判定、客户查询无灯；删利润策略页；RDD/数据设计/PRD/原型同步 | 一期优惠已是固定减免，利润用同口径（额）才能与优惠直接相减做盈亏判断；利润只是每周手工推翻的起步种子，命名策略池过度设计；两个盈亏灯口径要统一到运费侧，附加费成本未建模不参与判定 |
| 2026-07-10 | 澄清交货地成本规则：成本底价按交货地不同（揽收段提货/装柜/拖车费随交货地变，义乌远途成本高于珠三角），交货地分组随渠道而异（同揽收单价合并、不同拆行）；工作台 demo 补上按交货地变化的成本明细与成本底价 | 对方案表后发现 demo 未体现交货地对头程成本的影响，与"揽收段按交货地拆行"规则脱节 |
| 2026-07-08 | 运价查询【运输详情】改为「当周船期 × 各交货地截单」矩阵；服务渠道新增「关联航线」多选+保存强校验≥1条航线，渠道↔航线双向维护 `route_channel`；航线"船司"改"供应商"并按港口类型过滤；双向对称过滤（港口类型+国家） | 销售/客户查报价需同屏看船期；渠道必须绑定航线才能提供船期，强校验卡在源头；供应商过滤能防止海港航线误选非海运供应商 |
| 2026-07-07 | 送仓预估时效新增派送方式+仓库代码/邮编开头；理赔承诺时效快递派改为渠道级（隐藏仓库字段，仅一条） | 快递派不按仓点，改为邮编/渠道级 |
| 2026-07-07 | 船期录入三项校验：航程快照只读+ETD→ETA自动计算；截单日期<ETD；最快提取≥ETA | 时间链约束：截单<开船<到港≤提取 |
| 2026-07-06 | 航线关联渠道新增过滤规则：渠道下拉按航线港口国家过滤（出发港→起运国+目的港→目的国） | 防止航线关联到不匹配国家的渠道 |
| 2026-07-03 | 新增泡比优惠实体 density_discount；一期全优惠统一固定减免；折扣率+叠加顺序可配+硬拦截移至二期 | 老板需求：基于货物泡比的阶梯优惠 |
| 2026-06-12 | 运价查询页面重构：上查询栏（国家+仓库+包裹弹窗多件装箱）+ 下左右布局（左：渠道专线多选+货物属性+包税方式，右：按专线分组折叠 + 二次筛选固定 + 结果独立滚动）；PRD §5.12/§5.13 同步 | 从零搭建运价查询原型 |
| 2026-06-11 | 工作台矩阵视图：仓库组×重量段平铺 + 利润率盈亏色标 + 最大优惠列；RDD/PRD/数据设计同步 | 解决组合爆炸问题 |
| 2026-06-11 | 工作台多轮优化：主列表简化、弹窗加宽至1200px、成本明细含计算公式（头程+派送段拆解）、新增运价行重构（定价维度自动确定/利润率双向联动/交货地多选）、散货隐藏整柜直送价；RDD/PRD/数据设计同步 | 用户审查反馈——9+7+6+4 项迭代 |
| 2026-06-11 | 服务渠道/服务组合/成本管理联动：编辑模式维度disabled、重量段模板按运输+计费过滤、启用默认价开关、整柜演示案例、必填星号补全等 10 余项修补；RDD/PRD 同步 | 用户审查反馈 |
| 2026-06-11 | surcharge_rule 增加 tag_ids 字段（引用基础资料-标识管理） | 附加费规则需关联标识 |
| 2026-06-09 | 服务渠道启用级联规则：冻结/启用双向级联，RDD §3.1 + 数据设计 §3.1 + PRD §3.1/§5.2/R14 同步 | 原型交互变更 |
| 2026-06-08 | RDD §3.3 DW 滚动更新补充第三阶段（飞书表格→API→重试） | ship track 开发沟通结论落地 |
| 2026-06-07 | 工作台+运价查询 Demo-Drafts 对齐 + 版本索引增强 + 全局审计 14 项修复 | 三源融合对齐 |
| 2026-06-06 | 4 Workflow + 5 Skill 审计标准化 + 超级运价 25 项文档对齐 + 7 模块 28 份 drafts 批量生成 | 全局基础设施搭建 |
