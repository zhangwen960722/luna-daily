| 日期 | 变更内容 | 变更原因 |
|------|---------|---------|
| 2026-09-24 | RDD 与 PRD 合并定稿为「产品文档」（原 用户需求.md / PRD.md 已删）：业务规则按流程分段 R[流程号].[序号]、状态机保留、枚举/接口下沉数据设计、页面规格缩为功能清单原型轻索引 | 落实文档职责边界：业务规则唯一权威在产品文档、枚举/接口在数据设计，消除 RDD/PRD 双份重复定义 |
| 2026-09-24 | 费用类型新增「同行客户同步销售成本」开关（默认不同步，创建后仅此开关可改、其余字段不可改）——同行客户的应收流水是否复制销售成本流水，见财务 R5.10 | 同行客户（同业货代）的往来不计入我司销售成本，销售成本复制需按客户是否同行分层 |
| 2026-09-23 | 三份文档（RDD / 数据设计 / PRD）加「分工声明」标注各自权威边界；RDD 字段表删「类型」列（字段结构归数据设计）；数据设计清理越位内容（「业务约束」重复正文改见 PRD，设计决策章改写为「建模约定」） | 落实「同一条内容只在一处是正文」铁律：字段结构归数据设计、业务规则归 PRD，其余文档只引用不重复定义，避免改一处漏一处 |
| 2026-09-23 | 舱位管理「添加应付费用」新增**费用类型唯一性校验**：本提单已有同费用类型 → 阻断；「自动平摊 / 批量平摊」时任一目标运单已存在该费用类型 → 平摊失败 + 提单侧新增一并取消（整单不落） | 应付费用类型在提单维度与运单维度都唯一，平摊到已有同类型的运单会产生重复流水，跨层双向查重避免重账 |
| 2026-09-23 | 舱位管理「添加应付费用」弹窗的**币种、汇率**字段改为必填（金额原币本就必填），与财务新增应付弹窗口径对齐 | 财务侧统一新增应收 / 应付 / 销售成本弹窗的币种、汇率、计价单位、金额（原币）四字段必填，提单侧添加应付费用同口径，避免应付流水缺币种 / 汇率 |
| 2026-09-22 | 费用类型「费用方向」由单选改多选，新增「销售成本」方向（应收 / 应付 / 销售成本），唯一性由「同方向同名唯一」改为「费用名称唯一」 | 一笔费用科目可能既是向客户的应收、又是我司的销售成本，方向需多选才能表达同一科目跨多场景；方向成为多选属性后不再适合作判重维度，改按名称判重 |
| 2026-09-22 | 舱位管理（提单）「财务信息」Tab 明细表新增**「审计状态」列 + 流水级「审计 / 反审计」按钮**（勾选已摊行单条审，待摊行「待平摊」不可审计、挂靠行「运单审计」提单侧不审）；**提单级审计 / 反审计统一收敛到舱位主列表**（点「审计」弹窗选「审计范围：应付」整提单统一审、反审计一锅端），移除财务信息 Tab 顶部原提单级「审计 / 反审计」按钮；**审计归属、两层审计、平摊/撤销平摊三道前置、挂靠编辑权限、成账口径的完整规则收口见财务 PRD R76**（本页只留页面入口规格） | 提单审计升级两层（提单级 + 流水级），对齐运单审计模型——财务要能单条审提单应付流水、也能整提单统一审；规则集中到财务 R76 作唯一权威，避免跨模块重复口径 |
| 2026-09-22 | 舱位管理（提单）「财务信息」Tab 定位为**提单侧应付流水只读镜像** —— 除提单级费用（待摊 / 已摊）外，另展示运单审计「添加应付」挂靠同步到本提单的应付流水（`source_type=20`，提单侧只读、操作列仅「查看」，改删回运单侧）；提单审计 = **整提单冻结**（已审计 → 该提单下所有应付流水含挂靠行一律不可增/改/删） | 应付业务流水审计口径简化（审计归属看来源类型而非提单号是否为空），提单侧对运单侧挂靠流水只读、改删回运单侧，避免两边各改各的 |
| 2026-09-21 | 舱位管理（提单）详情新增「财务信息」Tab（加应付费用 + 批量平摊 + 提单审计/反审计），并登记提单配载明细（提单 → 运单 → 实收体积）作为财务提单平摊取数来源；提单审计 = 应付费用全部平摊完 + 未审计 → 审计（已审计不允许加应付费用），反审计 = 已审计 + 未关联账单；「添加应付费用」弹窗补「是否自动平摊」开关（默认勾选，勾选保存即平摊到运单、不勾选落提单待摊） | 财务应付业务流水新增「提单平摊」来源，需提单侧提供加费用入口、实收体积取数与提单审计锁定 |
| 2026-09-18 | **费用类型去掉「是否赔付类型 / 赔付类型」字段**：`fee_type.is_credit` + `credit_type` 删除 —— 列表去掉「赔付类型」列、新增/查看弹窗去掉「是否赔付类型」开关与「赔付类型」下拉；赔付（理赔）抵扣改由**负数应收流水**表达（业务手工新增负数流水、费用类型选超时赔付/丢件赔付/货损赔偿等赔付类）。数据设计 `fee_type` 删 2 字段、RDD（§5.1 字段 + 删除 R19）、PRD（§5.5 列表列/弹窗字段/接口 body）、原型 `费用类型_主列表.html` 同步；**跨模块**：财务 RDD/PRD、货主端 RDD/PRD 的「赔付类型 is_credit=true」引用改为「负数应收流水（赔付抵扣，金额为负）」 | 费用类型上的赔付标记不再保留，赔付行改按负数金额识别，消除 is_credit 标记与「赔付入账」二期台账的两套口径 |
| 2026-09-17 | **运单管理（`Waybill.html`）清理「费用新增 / 编辑 / 删除」残留死代码** —— 在「费用维护唯一入口 = 财务中心 → 运单审计」口径下，本页费用只读，故删除已成孤儿、无人调用的 JS 共 268 行：新增应收弹窗（入口上下文 / 行模型 / 费用类型字典 / 类型与来源规则绑定 / 逐行校验与落库 / 批量铺费用与打标）、行级冻结判定（`feeRowLockReason` / `feeRowEditable` / `feeWaybillOperable`）、编辑弹窗（`feeArEdit` 及其锁定与保存）、费用方向许可（`canFee` / `canAddFeeByDir`）、命令分支与导出清单中的对应项；**界面与可见行为一行未改** —— 运单应收净额、费用管理 Tab 流水表、「流水详情」弹窗、「运单审计」入口与弹窗全部保留并实测通过 | 上一轮移除按钮与弹窗时只删了模板，留下一整套没有入口的 JS；文件里挂着「本页可新增 / 编辑 / 删除费用」的假象，后续维护者极易把入口接回运单管理页，与已定口径（费用维护只在运单审计页）冲突 |
| 2026-09-16 | **运单管理页接上「运单审计」入口（业务侧整票审计）** —— 工具栏新增「**运单审计**」按钮（未勾选 / 状态不允许 / 该方向已审计 → 置灰 + 悬浮说明原因）→ 弹窗：运单号 + **审计范围（应收 / 应付，不预选，由人主动勾）** + 影响说明 → 确认后该方向标记置「已审计」（写审计人 / 时间）、该方向全部流水置「已审计」→ **整票不允许再新增该方向费用**。**反审计仅财务角色**，入口仍在「财务中心 → 运单审计」 | 原页面残留一份没有入口按钮、绑定全未定义的审计弹窗（不可达）；产品确认**业务侧要能在运单管理页直接审**，故按其口径把入口接上（整票审计仍由人主动选方向，避免误冻结） |
| 2026-09-16 | **运单管理（`Waybill.html`）主列表「应收 / 应付」与运单审计对齐** —— 演示流水由 1 票扩到 7 票（逐行同数）、悬浮明细小窗新增「审计状态」列（小窗宽 720）、运单应收净额改为按行汇率折合；两端同一票运单的应收 / 应付 / 明细 / 审计状态完全一致（口径见财务 PRD §5.10 / R62） | 运单管理与运单审计看同一票运单却显示不同数字与不同明细，属演示数据不同源；本页只读、口径以财务侧为准 |
| 2026-09-16 | **原型图标写法修复（全仓清理的其中 4 页：VAT管理_主列表 / VAT资料新增-优化版 / 异常处理池 / 物流事件_主列表）** —— **数字占位标签**（按钮上直接显示乱字符）与 **PascalCase 自闭合图标**（图标静默丢失）改为真图标 / kebab-case 显式闭合；规范见 `style-guide.md` §原型图标写法 | 原型是**浏览器内联模板**，HTML 解析器先于 Vue 工作 —— 数字开头的标签名属非法 HTML（被降级成纯文本、直接显示在按钮上）、PascalCase 自闭合会被小写化（Vue 找不到组件、图标静默丢失）；规范见 `style-guide.md` §原型图标写法 |
| 2026-09-15 | 费用类型「是否赔付类型」标记（`is_credit` + `credit_type`）的用途对齐财务一期口径：用于**识别赔付（抵扣）行** —— 业务在运单上手工新增该费用类型的**负数应收流水**，随账单抵扣运费；**独立的赔付入账记录归二期**（赔付管理立项时落地） | 财务侧一期把「赔付入账记录」整条链降级到二期（`credit_record` 不建表、不做货主端页签），赔付改为一条普通负数应收流水；原表述「供财务入账记录捕捉」在一期不再成立，研发照旧文实现会去做一张不存在的表 |
| 2026-09-15 | 补登运单级字段**「FBA 单号 / 转单号 / 产品数量」**，并显式区分**箱级**（原型「编辑快递单」弹窗按货箱维护的箱号 / 扩展箱号 / 承运商代码 / 转单号）与**运单级**两套字段，研发不得混用 | 财务「账单管理 → 账单详情」明细表需要引用这三个运单级字段，此前头程管理文档从未登记，属文档缺口补齐；**同日按产品口径把登记的第一个字段由「扩展单号（快递主单号）」改为「FBA 单号」**（账单明细展示 FBA 单号） |
| 2026-09-14 | **「头程管理 → 订单管理 → 运单管理」进入菜单默认命中状态 Tab 改为「已收货」**（原「收货中」） | 应收业务流水的生成节点是**收货完成**，收货完成前（已下单 / 收货中）没有任何流水；默认停在「收货中」等于给业务一个恒为空的列表，默认停在「已收货」才落在「有流水待核对」的那批运单上**同日运单管理主列表「应收 / 应付」列改为「总额（金额 + 币种）+ 悬浮费用明细」**：单币种且汇率恒为 1 → `CNY 1,265.00`；**含外币或汇率 ≠ 1 → `≈ CNY 1,975.00`**（按行汇率折合本位币）；悬浮小表列 费用类型 / 金额 / 币种，**存在汇率 ≠ 1 的流水时追加 汇率 / 折合 CNY 两列**，底部给总额与多币种「原币小计」。口径与运单审计页完全一致（财务 PRD **R62**），演示数据同步补一条 USD 流水以展示折合效果。 |
| 2026-09-14 | 员工端菜单在「头程管理」一级菜单下**新增二级分组「舱单管理」**（与订单管理 / 服务管理 / 成本管理 / 运价管理 / 事件管理 / 航线管理 / 基础配置七个分组同级），其下挂**菜单项「舱位管理」**（对应 `舱位管理.html`）；同步登记权限点「舱位管理」、菜单与面包屑解析（面包屑显示「**头程管理 / 舱单管理 / 舱位管理**」）、版本索引（头程管理 7 页）与功能清单菜单树（员工端 47 个 HTML）。**同日重排头程管理菜单顺序**：按业务动线「干活 → 资源与规则 → 成本与字典」排为 **① 订单管理 ② 舱单管理 ③ 事件管理 ④ 航线管理 ⑤ 服务管理 ⑥ 运价管理 ⑦ 成本管理 ⑧ 基础配置**，并把 menu index **规整为 `2-0`…`2-7`**（原为 2-0/2-1/2-7/2-5/2-3/2-6/2-4/2-8 的乱号）；组内顺序同步调整：航线管理改为「航线 & 船期 → 港口管理」、运价管理改为「运价更新工作台 → 运价查询 → 费用与标识规则 → 公开计算器」 | 舱单与舱位业务需要独立入口；分组名沿用业务口径「舱单管理」，具体页面先落「舱位管理」，后续同组页面可直接挂入该分组 |
| 2026-09-14 | 运单管理「费用管理 · 应收」流水表按新口径重构：**一条费用项一行、优惠独立成行**（运费 / 运费优惠 / 泡比优惠 / 附加费 / 附加费优惠各一行）；表格由 9 列扩为 13 列，新增 **流水种类 / 单价 / 计价单位 / 计费量 / 来源规则**，「金额」改**带符号**展示（负=减钱，红色），表头加「运单应收净额 = 各行金额代数和」；手工「添加应收」弹窗补 流水种类（人工添加）/ 单价 / 计价单位。同时**移除该 tab 顶部的两条说明条**（「超重超长费用将在拣货实测后计算」与「应收流水逐行生成」） | ① 收货生成的是逐条费用项流水，原表只列「金额」看不出「这笔钱是哪条规则给的」，加来源规则后运单审计可直接下钻；② 说明条属口径解释，**放在业务操作页面上不合适**，口径以文档与计费引擎为准。**同日再调应收流水表**：新增 **描述 / 到期日期 / 创建人** 三列，**来源规则**（规则类型 + ID + 规则名快照）从列表收进**行详情**弹窗（另含数据来源 / 创建人 / 创建时间 / 生成节点），操作列改为 **详情 / 编辑 / 删除** —— **「编辑」仅「人工添加」行可点**，系统自动行置灰并提示「系统自动计算的流水不可编辑，如需调整请删除后手工新增，或修正运单信息触发重算」；「添加应收」弹窗补「描述」列。缘由：① 来源规则是审计下钻用的内部信息，常驻列表挤占宽度，且客户对账更关心「描述 + 到期日期」；② 编辑限制对齐财务 R58，避免人工改动与运单信息变更后的重算结果冲突。**同日按反馈再收敛排版与编辑范围**：① 应收表**去掉「到期日期」列**（改在财务「账单草稿池」展示）；② **操作列改为一行三个按钮**（详情 / 编辑 / 删除，`link` 样式 + `white-space:nowrap`），不再折行；③ 汇总区改为**浅底汇总条**（流水条数 / 运单应收净额 + 右侧「添加应收」），单元格行高收紧、文本列加 `show-overflow-tooltip`；④ **编辑弹窗改两列布局**，且**系统自动行只放行「描述 + 内部备注」**（其余字段置灰，顶部一行提示「系统自动计算 · 仅可修改描述与内部备注」），人工添加行不受限；行详情弹窗同步去掉到期日期并重排为整齐两列。缘由：列多导致操作列折行、可读性差；自动行的金额口径由规则与重算唯一维护，但描述与备注需要人工补充。**同日把「业务时间」改为「拣货时间」**：列名与取值口径同步（= 该行所属运单的拣货实测完成时间，未拣货显示「—」），「添加应收」弹窗**去掉「业务时间」录入**（拣货时间与新增的「创建日期」由后台自动赋值，前端不填）；行详情弹窗改为展示「创建人 / 拣货时间」（**创建日期后台存、前端不展示**）。**同日调运单详情的默认态与「添加应收」弹窗**：① 运单详情**默认停在「费用管理」Tab**、右侧**轨迹追踪默认收起**（原来每次进来都要手动点收起再切 Tab）；② 「添加应收」弹窗顶部**默认带出主运单号**（原实现读的是列表勾选，从详情单选进入时为空 → BUG），多选进入时逐行选运单；③ 弹窗按**费用类型是否绑定规则**分两种模式 —— 绑定了「人工添加型」附加费规则的（超重费 / 周六派送费 / 库内贴标费）多出**「来源规则」必选列**，选中后**币种 / 单价 / 计价单位按规则带出并置灰**、业务只填计费量、流水种类自动落「附加费」；未绑定规则的（报关费）自由填写、流水种类落「手工新增」；**绑定了自动型规则（字段匹配 / 字段&公式）或优惠类规则的费用类型选择即拦截**并提示；保存改为真落库（原按钮无实现），落库带 `source_type=20` / `generate_node=30` / 来源规则留痕，拣货时间与创建日期由后台赋值**同日放宽手工新增的校验并瘦身弹窗**：① 「运费」等**由系统自动生成的费用类型不再拦截** —— 业务认为自动行不对时可「先删原行 → 手工补录」，故所有费用类型一律允许手工新增（有「人工添加型」附加费规则候选的，来源规则改为**选填**：选了按规则带出单价并落「附加费」+ 规则留痕，不选则自由填写落「手工新增」）；通常自动生成的类型改为在「流水种类」标签上给悬浮提示，优惠类手工新增须填「描述」说明原因；② **去掉弹窗顶部那条说明文案**，顶部只留「运单号（默认主运单号）/ 客户 / 服务组合 + 添加一行」一行信息条，表格列宽重排、单元格行高收紧、操作列改 `link` 按钮，弹窗加宽到 1240。**同日再补两件事**：① **打标联动** —— 「添加应收」选中来源规则（人工添加型附加费规则）后，保存成功即按规则配置的**附加费标识**给运单打标（与规则自动命中口径一致、同标识不重复打、失败仅告警），弹窗卡片底部与保存提示都会显示打标结果；运单列表的「附加费标识」列同步改为**绑定运单真实数据**（原为写死的「超长」标签）；② **弹窗排版由超宽表格改为卡片式逐条录入**：每条流水一张卡片，费用类型/来源规则、币种/单价/计价单位/计费量/金额/汇率、描述/备注分三行网格排列，卡片右上角删除、底部显示关联标识，弹窗宽度 1240→900。**同日打通运单主列表入口**：「添加应收」从运单列表工具栏进入时改为以**勾选的运单**为上下文（原来一律取「上次打开详情的运单」，会出现显示「未选择数据」或带出别的运单、多票无处指定运单、打标打错运单的问题）——单票直接带出该票运单号与客户/服务组合；**多票逐行必选运单号**，卡片标题显示该行所选运单信息；**打标按行上的运单逐票执行**；未勾选时阻断并提示「请先勾选需要添加应收的运单」。**同日把多票做成真正的「批量铺费用」**：原来的「多票逐行指定运单号」在勾 10 票加同一条费用时要手工加 10 行，与「批量添加费用」的意图不符；现改为每张卡片带**「适用运单」多选（默认全选勾选的运单、可逐票取消）**，卡片标题显示「× N 票 · 合计 = 单价 × 计费量 × 票数」，**保存时一条费用 × M 票 = M 条流水**（逐票带各自运单号）、**打标逐票执行**，提示为「已添加 1 条费用 × 2 票 = 2 条应收流水」。**同日补费用操作的状态门禁**：① 把「允许费用操作的运单状态」抽成常量（已收货 / 转运中 / 已到港 / 已清关 / 已到仓 / 已签收），**列表工具栏「费用管理」与详情「添加应收」按钮共用同一份口径** —— 原来只有列表按钮按状态置灰，**详情里对「已下单 / 已取消 / 退件」的运单照样能加费用**；② **行内「编辑 / 删除」按审计与账单冻结**（待业务审计可改、已审计冻结需先反审计、已进账单彻底冻结、已红冲只读），mock 流水补 `auditStatus` / `billNo`，点击置灰按钮给出对应原因；③ 编辑弹窗与保存各加一层兜底校验，防止绕过入口直接改。口径成文于财务 PRD **R61** 与头程管理 PRD §8 权限矩阵。**同日去掉费用管理里的审计 / 反审计**：运单管理页「费用管理」下拉只保留「添加应付 / 添加应收」，**审计与反审计统一在「财务中心 → 运单审计」页操作**（专门菜单），相关弹窗与变量一并清理。**同日补两层审计标签**：运单列表新增「**费用审计**」列（运单级标签：待审计 / 已审计，hover 显示审计人与时间），费用 tab 新增「**审计状态**」列（流水级标签）；权限判定改为**四级优先级 AND** —— 运单状态 → **运单已整单审计（整票不允许新增费用、行级全冻结）** → 账单关联 → **该条流水已审计（不可变更、只能新增流水）**；列表下拉与批量弹窗同步（整票已审计时「添加应收/应付」禁用、已审计运单在「适用运单」里不可选）。**同日运单审计标记定型为「两态 · 按方向」**：运单列表「费用审计」列拆成「**应收 未审计/已审计** + **应付 未审计/已审计**」两个小标签（hover 显示审计人与时间），权限按方向判定 —— **应收已整票审计 → 整票不允许新增应收费用、应收流水全冻结，应付方向不受影响**；「添加应收 / 添加应付」两个下拉项分别按各自方向禁用。**同日运单审计功能落地**：主列表新增「**审计**」「**反审计**」两个独立按钮（按勾选运单 + 弹窗选费用方向 = 应收 / 应付，执行该方向**整票流水统一审计 / 反审计**）；运单详情 · 费用 tab 新增**流水多选列**与顶部「审计 / 反审计」（逐条生效，已审计流水可勾选用于反审计，已关联账单的流水不可反审计）。**同日按指示把审计入口整体撤出运单管理**：主列表两个审计按钮、费用 tab 的行级审计按钮与多选列一律移除（运单管理只保留「费用审计」/「审计状态」两个**展示用**标记与冻结判定），审计 / 反审计**统一收敛到「财务中心 → 运单审计」页**（列表勾选运单按方向整票审计 + 点运单号进详情按应收 / 应付 Tab 勾选流水审计）。**同日按产品口径细化该页**：审计范围 = 已收货 / 转运中 / 已到港 / 已清关 / 已到仓 / 已签收；列表铺开 24 个业务字段 + 应收 / 应付 / 毛利（由流水实时汇总）；运单详情结构与运单管理一致（只读），费用 tab 的应收 / 应付多「审计 / 反审计」按钮且明细表新增「关联账单」列；运单管理侧账单号收进流水行详情。**同日运单审计页顶部改版**：状态改为 **Tab 分组**（全部 + 审计范围内的 6 个状态，带条数）、查询区按规范重做（默认 3 条件 + 展开收起 + 右对齐按钮组）。**同日职责切分**：**运单管理改为只读**（去掉「费用管理」下拉、应收只留「详情」、应付去掉操作列、三个费用弹窗删除），**仅保留审计标记与关联账单的展示**；**新增 / 编辑 / 删除流水全部移到运单审计页**（运单详情 · 应收 / 应付 Tab，「新增流水」+ 行内编辑 / 删除，按冻结边界校验），运单审计侧同时保留审计 / 反审计。**同日两侧细化**：运单管理去掉「费用审计」列、默认命中「收货中」、应收流水加「关联账单」列、**已下单 / 收货中不带流水**；运单审计状态范围去掉 已下单 / 取消、默认命中「收货中」、**退件放开**、主列表换 38 字段、查询区 13 条件（默认 3 个 + 更多 + 查询后收起）、应收流水加「关联账单」列 |
| 2026-08-29 | v2.2：费用类型新增「是否系统预置」标记（is_system，预置运费）+「是否赔付类型」标记（is_credit，仅应收方向）+ 赔付类型枚举（credit_type，可自定义），供系统自动生成运费流水/财务入账记录捕捉。同步更新 RDD 字段表+R19/R20规则、数据设计 fee_type 表、PRD 页面规格+接口参数、原型费用类型页面 | 支撑财务模块「入账记录」功能 + 运费业务流水自动生成定位 |
| 2026-08-29 | 运单管理从一级菜单移入「头程管理-订单管理」二级菜单；预置运费费用类型只读（查看） | 运单归属订单域，收拢到头程管理下 |
| 2026-08-29 | 费用类型唯一校验调整（同方向同名仅一个启用）+ 创建后所有字段不可编辑（仅查看） | 避免同名启用重复 + 避免改字段影响已关联业务流水 |
| 2026-06-06 | v2.1-final：确认3个待确认项。(1) T4 物流事件 event_template：notify=否时不必填；(2) T5 物流事件名称：全局唯一，跨租户不重复；(3) T3-标识 模块范围：采用"舱单/运单"。同步更新RDD/数据设计/PRD中相关字段说明、业务规则、融合对照表和待确认清单。 | 业务方确认最终决策，消除所有待确认标记 |
| 2026-06-06 | 初稿，基于 Demo 反向生成。包含：用户需求(RDD)（7条业务流程+20项AC）、数据设计（7张表+ER图+枚举汇总）、PRD（9个页面规格+26个接口+16项验收标准+14条错误文案） | AI PM 初始化头程管理模块完整文档 |
| 2026-06-06 | v2.0 完整重写（基于Excel源数据）。RDD修正：VAT同客户下VAT号+EORI号联合唯一、员工端可修改全部字段+无权限管控、注册国家差异化附件规则、费用类型同方向唯一、物流事件名称唯一+Ship Track集成+运单/提单路由接口、亚马逊物流事件状态启停+查询接口、标识查询接口供舱单/运单打标。数据设计修正：vat_registration唯一约束改为(tenant_id,user_id,vat_no,eori_no)、fee_type唯一约束改为(tenant_id,fee_name,fee_direction)、logistics_event新增(tenant_id,event_name)唯一约束、amazon_logistics_event新增status字段、tag.ModuleScope改为10:舱单 20:运单、新增4.5查询接口对外暴露表。PRD修正：枚举字典ModuleScope更新、状态机新增AmazonLogisticsEvent、VAT页面规格全部重写（国家差异化附件+员工全字段编辑+无权限管控）、费用类型页面规格更新（同方向唯一校验）、物流事件页面规格更新（名称唯一+条件必填修正+下游查询接口）、亚马逊物流事件页面规格重写（状态启停+下游查询接口）、标识页面规格更新（创建人列+下游查询接口）、业务规则从12条扩至18条、权限矩阵重写（员工VAT无权限管控+配置子模块无权限管控）、接口清单扩至27个（新增5个下游查询接口）、错误文案扩至17条、验收标准扩至19项 | Excel源数据对齐，修正Demo反向生成时的偏差 |
| 2026-06-06 | v2.1 三方数据融合（Demo+Excel+Draft）。对照9个Demo页面逐字段比对。发现：标识module_scope Demo用"提单"vs Excel/Draft"舱单"（以舱单为准）、物流事件event_template Demo始终必填vs Draft条件必填（待确认）、亚马逊物流事件Demo无status字段（Draft补充）、VAT Demo内部amazonLink/shopLink变量名不一致（BUG）、员工端VAT"查看"按钮标签与实际跳转mode=edit不一致（UX BUG）、异常处理池Demo页面归属运单管理模块。3份设计文件均增加数据融合附录 | 三方对照融合，消除Demo/Excel/Draft间的不一致，标记待确认项 |
