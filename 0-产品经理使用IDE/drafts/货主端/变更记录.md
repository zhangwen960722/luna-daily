# 变更记录 -- 货主端

| 日期 | 变更内容 | 变更原因 |
|------|---------|---------|
| 2026-09-24 | RDD 与 PRD 合并定稿为「产品文档」（原 用户需求.md / PRD.md 已删）：业务规则按流程分段 R[流程号].[序号]、状态机保留、枚举/接口下沉数据设计、页面规格缩为功能清单原型轻索引 | 落实文档职责边界：业务规则唯一权威在产品文档、枚举/接口在数据设计，消除 RDD/PRD 双份重复定义 |
| 2026-09-23 | 字段表删「类型」列（字段结构归数据设计）+ 数据设计清越位 + 三份文档加分工声明 | 落实「正文只在一处」——字段结构归数据设计、业务规则/校验/计算公式归 PRD、功能→AC 映射与流程归 RDD，避免同一条内容多处重复定义、改一处漏一处 |
| 2026-09-17 | **资金账户「充值」弹窗两处调整**：① 打款凭证支持截图后 Ctrl+V 直接粘贴上传（免「存桌面再选文件」，点击上传 PDF / 图片保留，单张 ≤ 5MB）；② 新增「支付时间」字段（日期选择、必填，供财务对账定位客户打款时点） | 截图是客户上传银行回单最常见来源，粘贴直传更省事、更不易漏传；支付时间补齐「客户何时打款」的对账信息，与凭证配套核对银行流水 |
| 2026-09-17 | **资金账户「资金流水」里贷记单结转那条流水的类型，由「赔付入账」更名「贷记单入账」**（与员工端一致） | 贷记单承载的负数流水不一定是赔付（赔付只是其来源之一），类型名跟单据走更准确，客户侧看到的类型名与员工端、财务口径统一 |
| 2026-09-17 | **账单管理按单据类型拆「应收账单 / 贷记单」两个 Tab** —— 原「待确认 / 已确认」两个 Tab 改为按 `bill_type` 拆：**应收账单 Tab**（11 列 + 核销状态 + 已核销 / 已冲账 / 未结清三列金额（已冲账金额未冲账显示 `—`）+ 查询区 5 条件：账单编号 / 所属主体 / 币种 / 确认状态 / 核销状态，确认状态默认「待确认」）、**贷记单 Tab**（7 列 + 查询区 5 条件：账单编号 / 所属主体 / 币种 / 确认状态 / 结转状态 + 「状态」列 = 待确认 / 待结转 / 已结转，无到期日期、无核销/冲账列）；贷记单 Tab 标签带「· N 待确认」红字角标防漏确认，演示数据补「待确认」贷记单 `AR-GD-202609-00003`（−150）；「超期自动确认」说明条在两个 Tab 的「确认状态 = 待确认」时显示 | 应收（客户付钱、结算 = 核销）与贷记（我司付钱、结算 = 结转钱包）是两个相反资金方向、两个不同结算出口，混排 + 贷记单 tag 让贷记单的「结转状态」无处安放、核销三列还硬打 `—`；拆 Tab 后列全部固定、无需类型自适应 |
| 2026-09-17 | **「资金账户 → 资金流水」Tab 加「流水号」列**（`WF+yyyyMMdd+序号`，资金流水自身编号，与员工端「资金流水」页同口径同源） | 资金流水是客户对账凭证，需稳定唯一流水号（等价银行流水号）；此前只有「关联单号」（业务单据号），缺流水自身编号 |
| 2026-09-16 | **原型展示修正（两项，不改业务口径）** —— ① **原型图标写法修复（全仓清理的其中 11 页：index.html（菜单导航栏）+ 商品管理 / VAT管理 / VAT资料新增 / 地址管理 / 子账号管理 / 角色管理 / 账单管理 / 资金账户 / 查价订舱 / WaybillShipper / 后台任务）** —— **数字占位标签**（按钮上直接显示乱字符）与 **PascalCase 自闭合图标**（图标静默丢失）改为真图标 / kebab-case 显式闭合；规范见 `style-guide.md` §原型图标写法；② **「账单管理」超期自动确认提示改成一整行纯文字**：「待确认」Tab 内、**主列表正上方**这条提示原是完整的 `el-alert`（边框 + 底色 + 左侧信息图标；图标贴在最左边缘、正好压在主列表左上角，看着像图标单独杵在列表前面），现改为**一行 13px 浅灰文字**（`<div class="auto-confirm-note">`：无图标、无边框、无底色），**位置不变**（仍在「待确认」Tab 主列表上方、只在该 Tab 出现）| ① 图标标签写在浏览器内联模板里会直接决定渲染结果（数字开头标签名属非法 HTML 会被当文本显示；PascalCase 自闭合会被小写化、图标静默丢失）；② 说明条属**对客通用口径**、不是某张账单的列表数据，放在列表里既观感"包裹"、又只对「待确认」Tab 可见 |
| 2026-09-16 | **账单管理展示「贷记单」**：财务侧组合净额 ≤ 0 生成的贷记单（我司应付客户的赔付抵扣，金额为负）**照常下发客户** —— 列表「核销状态」列显示「贷记单」黄 tag（悬浮说明"我司应付您的款项，不参与收款核销；已由财务结转至您的资金账户余额"）、「已核销 / 未核销金额」显示 `—`，详情「账单金额」旁同 tag；客户照常确认（走待确认 Tab 与 7 天自动确认兜底），确认后财务把这笔钱结转至客户资金账户余额（可用它核销其他账单或申请提现） | 财务新增贷记单（负数账单）结清路径，客户侧需要看得见这张单、并有确认动作，否则客户端没有凭据、流程断在财务侧 |
| 2026-09-15 | **账单管理收敛为 2 个 Tab + 列表与详情两端对齐（本日二次收口）** —— ① **Tab 由 3 个收敛为 2 个（待确认 / 已确认）**，撤掉「已核销」Tab（之后进入部分核销 / 已核销的账单仍留在「已确认」），**当时核销维度退出客户侧**（不展示核销金额 / 核销记录 / 核销单号 / 未收金额 —— 其中「未核销金额」后由⑥以只读列回归）；② **两个 Tab 的列表列完全相同，共 7 列（后由⑥扩为 10 列）**（账单编号 / 账单日期 / 到期日期 / **所属主体** / 币种 / 账单金额 / 操作），**查询条件统一 3 项**（账单编号 / 所属主体 / 币种）；③ **账单详情比照员工端改一版** —— 头部 **7 字段**（`我司主体` 在货主端统一叫「**所属主体**」；去掉未收金额（后由⑥以「未核销金额」回归）；**账期属内部口径、不对客户展示**），费用明细按员工端 **26 列**（「**扩展单号**」改为「**FBA 单号**」、补「**仓库代码**」），**明细不带审计状态列**，底部保留**金额构成**（正数行合计 / 负数行合计 / 账单总额净额）；④ **已作废的账单不下发客户**（不出现在任何 Tab），财务作废后会以**新账单号**重新出账，客户确认页的异议提示为「由财务作废账单后重新出账（新账单号）」。口径与字段源头在财务 PRD §5.9 / R63 / R64。**同日早前**：**赔付降级为运单上手工新增的一条「赔付类型」负数应收流水**，随账单按负数抵扣运费，**赔付入账台账归二期** —— 资金账户页**移除「入账记录」Tab**，Tab 收敛为 **账户余额 / 资金流水 / 充值记录**。货主端 RDD / PRD v1.6→v1.7→**v1.8**。**客户侧只展示「已发送」的账单**（未发送账单客户看不到），演示数据把华美的账期铺厚到 **5 张** —— 待确认 2 张（`AR-SZ-202608-00001` ¥1,000.00 CNY、`AR-GZ-202608-00002` $380.00 USD）、已确认 3 张（`AR-SZ-202607-00001` ¥2,160.00、`AR-GZ-202607-00002` ¥1,450.00、`AR-GZ-202608-00001` ¥1,330.50），并在员工端账单页与运单审计页各加 3 票运单（016/017/018）保证三页账单明细逐行、字段逐项对得上；确认提示条与二次确认文案**去掉「核销」字样**（改为「财务将据此账单收款」）。⑥ **列表新增核销三列（只读）** —— 两个 Tab 各加 **核销状态 / 已核销金额 / 未核销金额**，列表由 7 列变 **10 列**；**客户侧同样使用「核销」口径**（与员工端一致），但**核销动作、核销记录与反核销都不在客户侧**。缘由：客户侧原本已经在资金账户看到「未核销金额」与「收款核销」流水，账单列表却查不到「是哪几张单欠的」，链条断在中间；核销的「状态与金额」是账单自身属性应归账单列表，核销的「动作与明细」归财务侧「收款核销」；**客户侧用词与员工端统一为「核销状态 / 已核销金额 / 未核销金额」**。 | ① 客户侧只需要「这张账单我认不认」，核销是财务侧的催收动作，露给客户不仅无用、还会让客户以为可以自助核销；两个 Tab 的列与查询条件不一致也没有任何业务理由。② **账单详情是客户唯一能自证的凭据**，员工端本轮已把「扩展单号」换成「FBA 单号」、补了「仓库代码」与「账期」，货主端若不同步，同一张账单两端会出现字段名与列数分叉，客户核对不上「哪一票、哪一条费用、钱是怎么算的」。③ **账单里的行必然是已审计流水**，客户侧再挂「审计状态」列属于内部信息外露；且一期**无红冲机制**（已进账单的流水彻底冻结，调整靠新增负数流水、下期抵扣），故红冲展示与审计状态列一并撤除。④ 赔付既然只是一条**负数应收流水**，另立「入账记录」台账就会出现两处口径（台账与账单必然打架），故整条降级二期。 |
| 2026-09-15 | ⑦ **账单「超期自动确认」告知 + 资金账户标明是哪本账户（本日三次收口）** —— ① **账单确认加一条截止线**：账单**发送日（含当日）起满 7 个自然日**客户仍未确认 → 系统在第 8 天 00:30 **自动确认**（与手动确认**效力相同、不可撤销**）；「待确认」Tab 顶部加**常驻说明条**（`el-alert` info、不可关闭）告知该规则，**账单详情头部 7 字段 → 8 字段**、补「**确认信息**」（`已确认 · 客户确认/系统自动确认 · 时间`；待确认时显示 `待确认 · 将于 {时间} 自动确认`，把自动确认时间直接写给客户）；演示数据里 `AR-GZ-202607-00002` 改为**系统自动确认**案例（发送 07-01 → 确认 07-08 00:30）。② **「资金流水」「充值记录」两个 Tab 补「账户名称」**（`账号编号 币种代码`），并把「主体」列改名「**账户主体**」—— 本页是跨账户列表（同一客户在我司多主体、多币种下都有钱包），原来只有「主体 + 币种」看不出是哪本账户的操作；充值记录里「账户名称」= **入账钱包**、「打款账户」= **客户付款的银行账户**，两个概念不再混。口径见财务 PRD R16 / §5.8 / §5.9。 | 客户一直不确认会把账单永久卡在「待确认」，把财务侧的核销与账龄一起卡住；把截止线告诉客户，比事后催更有效。资金流水看不出是哪本账户，客户对账时无法定位这笔钱动的是哪个钱包。 |
| 2026-09-14 | **费用明细的「汇率」列从「金额」右边挪到「币种」右边**（货主端运单管理 · 费用明细表与相关弹窗表格），字段顺序与员工端保持一致 | 币种与汇率是一组换算信息，中间隔着单价 / 计价单位 / 计费量 / 金额不便于客户核对；一端改一端不改会造成两端字段顺序分叉 |
| 2026-09-14 | **撤回查价订舱页的「超重超长」提示条** —— 移除报价结果区上方的 warning 说明条（「超重超长费用将在拣货实测后计算」及其正文），页面改为**不承载说明性提示条**；**口径不变**：单箱录入（有长宽高）时可算出并作**预估展示**、总体录入不算不展示，预估**不落库、不进账单**，**正式计费以「拣货实测」为准** —— 这些口径保留在用户需求 / 数据设计 / PRD 与计费引擎中。同步调整：PRD「区域四」改为「结果区顶部（无说明条）」、R08a 由「提示条」改为「超重超长口径」、A13 验收标准改为口径校验、功能清单与页面构成去掉该提示条；RDD 对应区域 / AC-P8 / 功能清单 C10 / IA 树 / 专家建议同步 | 说明条属口径解释，放在业务页面上不合适（页面只呈现结果与操作）；口径有文档与计费引擎承载，不需要在页面重复一遍。**口径本身一个字未改**，只是不再以页面提示条表达。**同日同步**：① 账单管理页的费用明细改为**逐行流水口径**（流水种类 / 单价 / 计费量 / 金额带符号，优惠独立成行），页面 Tab 口径对齐为「待确认 / 已确认 / 已核销 + 账单明细弹窗」；② 查价结果区**底部补一行免责声明**「本报价为预估参考，不含成本底价和盈亏标识。实际结算以运单为准」；③ 文档中引用的超级运价 Phase 1 功能数由 59 更新为 **69**。**同日按指示摘除无页面菜单**：原型 `index.html` 移除**整个「订单中心」分组**（我的订单 / 草稿箱 / 异常订单）与财务中心的「**发票管理**」，`首页.html` 的「我的订单」快捷入口（死链）一并移除；4 个页面**仍待建**，文档改标注为「**页面待建、菜单已摘除**」，待页面落地后再挂回菜单。**同日新增「订单管理 → 运单管理」页**（`WaybillShipper.html` 挂回菜单，货主端首个订单域页面）：列表（状态 Tab + 搜索）+ 详情抽屉六 Tab（运单概览 / 货箱信息 / 商品信息 / 费用明细 / POD管理 / 附件管理）；**费用明细只读** —— 应收业务流水**逐行**展示（费用类型 / 流水种类 / 币种 / 单价 / 计价单位 / 计费量 / 金额（带符号）/ 汇率 / 描述 / 到期日期 / 业务时间 + 运单应收净额），**备注 / 数据来源 / 来源规则 / 创建人接口不下发**，客户不可新增、编辑、删除费用。同步：RDD v1.4→**v1.5**（功能清单 +页面 I、AC 补 AC-W1~W3、Phase 1 功能数 29→32）、PRD v1.4→**v1.5**（功能清单 + 页面导航图 + §5.9 页面规格 + 接口与验收 A21）、版本索引与功能清单菜单树同步。缘由：客户需要自助核对「这笔钱怎么算出来的、什么时候付」，费用字段可见性由财务 PRD §5.10 统一承载，货主端只做只读消费。**同日按反馈去掉「到期日期」列**（改在财务「账单草稿池」展示），描述列加宽；RDD 功能清单 I3 / AC-W3 与 PRD §5.9 字段表、接口返回字段同步调整。**同日「业务时间」改名为「拣货时间」**（= 该行所属运单的拣货实测完成时间，未拣货显示「—」）；新增的「创建日期」为后台字段，**货主端不展示** |
| 2026-09-12 | **优惠值正负号翻转**（产品与开发共同确认）：**固定减免 负数=减钱（正向优惠）、正数=加价（反向优惠）**（原「正数=减价」作废）；报价公式由「公布价 − 运费优惠 − 泡比优惠 + 附加费净额」改为「**(公布价 + 运费优惠值 + 泡比优惠值) + (附加费合计 + 附加费优惠值合计)**」（优惠值本身带符号，负值即原减项）；显示口径改为负显 `-¥X`、正显 `+¥X`；附加费优惠上限校验改为绝对值比较；**折扣率不变**（乘数语义） | 产品与开发讨论后确认：负数为「减」、正数为「加」更贴近账务直觉，且开发实现可省一层符号换算。货主端查价/报价展示、费用明细五段链、数据设计 §4.10 匹配伪代码需同步，否则与超级运价侧口径不一致 |
| 2026-09-12 | 货主端三份文档整体刷新（用户需求 v1.2→v1.3 / 数据设计 v1.2→v1.3 / PRD v1.2→v1.3），口径全面对齐现行原型与已定稿的超级运价：查价结果表列重排为服务组合/交货地/计价/公布价/运费优惠/泡比优惠/附加费/附加费优惠/最终报价/操作（无「优惠后」列）；三张优惠表统一为「优惠对象类型一期同时开放客户等级与客户特批、特批>等级不叠加」；泡比优惠整体进入一期并展示在结果表中；补齐财务中心、基础资料、后台任务等在原型中已实现的页面；菜单已挂但页面未建的四个入口标注为待建，不计入已完成功能 | 原文档停留在 2026-06-06，早于超级运价定稿，优惠口径、查价结果表列与原型实际菜单结构已全面落后；刷新后使货主端设计与上游模块和现行原型一致，避免研发按过期口径实现 |
| 2026-09-16 | **货主端运单页内部动作死代码清理 + 查询条件接上筛选 + 提现口径写明** —— ① 清掉 7 个内部动作的孤立弹窗与命令分支（**添加应收 / 添加应付 / 同行推单 / 提取数据 / 打印供应商标签 / 抓取POD / 状态调整**，共 92 行）+ 对应 refs / 命令拦截 / 导出 / 只服务它们的状态；② **查询区 6 个条件真正参与筛选**（运单号 / 客户单号 / 关联单号 支持逗号分隔多值精确匹配、收件人 模糊、国家 精确、下单时间 区间；列表 = 状态 Tab + 查询条件），此前点了「查询」列表不变（看着像坏了）；③ **写明提现口径**：一期**客户端不提供提现申请入口** —— 客户线下找财务，财务在员工端「财务流水 → 客户提现」操作，写一条「提现」资金流水并扣减钱包余额，客户只在「资金流水」里只读可见（线上申请列入二期） | ① 该页从员工端复制而来，内部动作弹窗与命令分支都还在（无入口按钮＝死代码，一旦接上入口即权限泄漏）；② 查询条件只绑了 v-model 却没参与过滤，与 placeholder「精确搜索」的承诺不符；③ 提现链路此前只在员工端 PRD 有定义，客户侧文档与原型都未写明"不提供申请入口"，容易被误加 |
| 2026-09-16 | **货主端运单页移除「审计 / 反审计」入口** —— 原型里还留着员工端的两个审计弹窗（含「应收-仅流水 / 应付-仅流水 / 应收-运单及流水 / 应付-运单及流水」四个选项）、命令分发条目、状态与导出（属从员工端复制过来的遗留壳）：弹窗 + `dialogFeeAudit` / `dialogFeeAntiAudit` + 命令拦截与分发表里的两项 + `auditTypes` 全部清掉 | **审计是内部动作**（业务/财务侧），客户既看不到也不需要 —— 与货主端 PRD「费用明细操作只读、添加/编辑费用与审计/反审计等入口按货主端权限收敛」一致；同族其余孤立弹窗（添加应收/应付、同行推单、提取数据、打印供应商标签、抓取POD、状态调整）仍在，待页面精修一并收敛 |
| 2026-09-16 | **运单管理页查询区的「查询 / 重置」按钮点击无反应** —— 模板 `@click="doSearch"` / `@click="doReset"` 调的两个函数**在页面里根本不存在**（只定义了 `search` 表单状态）→ 补上：查询给 `执行查询` 反馈、重置清空全部查询条件（列表本身按状态 Tab 实时过滤）；由新增的 `.agent/scripts/check-setup-refs.js` 全仓扫描发现 | 模板用了、setup 没返回 → 绑定静默失效（点了没反应），语法检查与模板作用域检查都抓不到 |
| 2026-09-12 | 查价订舱页报价结果区上方新增提示条：「超重超长费用将在拣货实测后计算」—— 说明报价依客户自填箱数/尺寸/重量预估得出，超重、超长类附加费以入仓拣货实测的尺寸重量为最终判定依据，实际费用以实测为准 | 客户在查价阶段看到的是自己填的数据，而超重超长的正式判定发生在拣货实测；不提前声明会造成「报价与账单不一致」的客诉 |
| 2026-06-06 | 初稿，基于 Demo 反向生成 | AI PM |
| 2026-06-06 | RDD v1.2 / 数据设计 v1.2 / PRD v1.2 — 货主端×客商中心数据融合：RDD新增 §12 融合章节（融合角色定位+5条融合链路F1-F5+融合业务规则R-F01~R-F08+跨模块接口依赖IF1-IF5+3项不一致标注）；数据设计新增 §6 跨模块数据融合（数据归属关系+4条同步数据流SF1-SF4+接口契约+完整登录校验链+失败降级策略+共享字段变更同步机制）；PRD新增 §13 数据融合（融合业务规则R-F01~R-F08+跨模块接口清单+完整登录校验链+下单校验链+融合验收标准A-F01~A-F08+错误文案补充E28-E33） | AI PM（融合） |
| 2026-06-06 | RDD v1.1 — 子账号管理增强+角色管理-货主端新增：子账号管理新增查询条件（账号/邮箱/姓名/手机号）+按钮（搜索/重置/新增子账号）+列表7列（账号/姓名/性别/手机号/邮箱/状态/操作）+新增弹窗（邮箱非必填+手机号数字校验）+分配角色范围（ROOT+启用角色）+修改密码+ROOT账号保护（列表不展示+不可操作）+登录方式（账号或邮箱）；新增角色管理-货主端流程（列表5列+新增角色代码+名称+分配资源权限树同员工端+冻结保护+ROOT角色自动生成+资源范围受ROOT限制）。数据设计 v1.1：sub_account表新增is_root字段；新增ROOT账号/角色设计说明（自动生成/保护/列表过滤/角色可选范围/资源上限）；新增角色表补充说明（货主端视角）。PRD v1.1：子账号管理页详细规格（7列+3弹窗含修改密码+ROOT过滤）；角色管理-货主端独立页面规格（5列+2弹窗含分配资源权限树）；业务规则新增R13-R20；接口新增角色CRUD/分配资源/ROOT资源树/修改密码；错误文案新增E19-E27。 | AI PM |
