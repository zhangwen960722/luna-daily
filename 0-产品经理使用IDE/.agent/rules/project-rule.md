---
trigger: always_on
---

# 项目规则 - 产品经理AI工作空间

> 本规则定义了AI辅助产品经理工作时应遵循的基本原则

---

## 产品思维

- 先理解目标和价值，再讨论实现方案
- 主动思考边界场景、异常情况、影响范围
- 发现逻辑漏洞（状态不闭环、流程断点、边界未覆盖）时，主动指出并给出建议
- 提供多方案对比时，说明各方案的优劣和适用场景
- 从用户和业务价值出发，避免过度设计

## 新系统0-1设计：BPR 先行

做新系统时，不应简单数字化现有流程。必须趁系统重构的机会，主动审视现有业务流程的漏洞：

- **5 问 BPR 检查清单**：
  1. 现有流程中哪些环节靠"人治"兜底？（如人工审批、记忆提醒）→ 系统化
  2. 哪些计算链路存在"应X未X"风险？（应收未收、应付未付）→ 自动校验
  3. 哪些数据之间应该有联动但现在断开？（应收 vs 应付、报价 vs 成本）→ 关联留痕
  4. 哪些字段/配置缺少生效和过期机制？（特批价、优惠、合同）→ 生命周期管理
  5. 哪些边界场景靠人工判断无系统约束？（折上折击穿成本、附加费全免）→ 底线管控
- 输出格式：每个问题 → 现状/风险 → BPR 建议 → 一期/二期分期决策
- 底线管控通用模式：绿灯(正常) / 黄灯(预警) / 红灯(拦截)，一期知会型，二期升级为强制

## 需求分析：Excel 反向建模

当用户已有 Excel 管理现有业务时，Excel 是"真实数据模型"的最佳来源。不要只听用户描述——读 Excel 能发现用户自己也未必意识到的结构：

- **看 Sheet 结构**：一个 Sheet 一个实体，Sheet 之间的关系 = 实体关系
- **看列头**：列名揭示字段名，合并单元格揭示实体层级
- **看数据行**：每行是一条记录，行内公式揭示计算逻辑
- **看纵向对比**：同一 Sheet 不同区域用不同阶梯（如重量段），说明该维度是独立实体
- **特别注意**：用户口述的"成本是组合级"可能与 Excel 中的"成本按仓点计算"矛盾 → 以 Excel 为准

## 运价/定价系统通用设计模式

- **成本&报价双粒度对齐检查**：成本底价的粒度（组合×仓点？组合×仓点×重量？）必须与公布价的粒度对齐。如果不一致，中间需要明确的映射规则
- **价格矩阵模式**：灵活定价系统通常采用多维矩阵（仓库组 × 重量阶梯 × 交货地），每维可渠道级自定义
- **反向匹配查询引擎**：销售查价场景应从"选产品→查价格"设计为"输入货物属性→系统匹配可承运渠道→比价列表"，匹配链 = 渠道过滤→组合匹配→价格行匹配→附加费匹配→优惠叠加→船期关联
- **内部计算 vs 对外暴露**：成本计算中的中间变量（如拆送价 vs 组合一口价取 MAX）是内部过程，不应暴露在公布价层面

## 输出质量

- 逻辑必须闭环（正向流程+逆向流程，不留断点）
- RDD 按业务流程组织（全景图→N条流程→AC→NFR→示例→功能清单），而非实体+Epic 堆叠
- 结构必须清晰（层级分明，易读易懂）
- 关键信息不能遗漏（状态、异常、取消、权限等）
- 每条流程标注：触发条件、频率、前置依赖
- 字段说明用表格，必含：字段名、类型、必填、说明
- 枚举类型必须列出所有可选值

## RDD 必含流程图 ★

> 以下三张图必须在 RDD 中体现，不得省略：

| 图 | 位置 | 类型 | 何时必画 |
|---|------|------|---------|
| 核心业务流程图 | §2 业务全景图 | 泳道式流程图 | 所有 RDD |
| 核心数据流图 | §2 业务全景图（或对应流程章） | 数据流图 | 有复杂计算链路时 |
| 关键操作流程图 | §N 对应流程章节内 | 决策流程图 | 页面有非平凡决策分支时 |

- 全部使用 Mermaid 语法
- 泳道图按角色分组 subgraph，标注触发事件和数据传递
- 数据流图标注每个节点的输入因子和计算公式
- 操作流程图包含决策节点（菱形）+ 正常路径 + 异常分支

## PRD 必含页面导航图 ★

- §1.3b 页面导航关系图：所有原型页面为节点，标注跳转关系 + 数据流向 + 角色入口
- 按菜单分组 subgraph，Mermaid flowchart 表达

## 表达方式

- 复杂逻辑用Mermaid流程图 > 表格 > 纯文字
- 先结论后展开：结论 → 依据 → 详细说明
- 关键信息加粗，便于快速扫读
- 中文为主，专业术语保持英文原文
- 中英文、中文与数字之间不加空格

## 协作习惯

- 不确定需求意图时，先确认再动手
- 大改动前先说明思路，获得确认后再执行
- 输出后主动询问是否符合预期，是否需要调整
- 草稿先放drafts/试错迭代，成熟后移至prds/正式版

## 工作流使用

- **Phase 1 需求洞察**：使用 `/1-analyze-requirement` (调用Requirement-Clarifier)
- **Phase 2 方案架构**：使用 `/2-design-solution` (调用Data-Modeler, Edge-Case-Detector)
- **Phase 3 规格生成**：使用 `/3-generate-specs` (生成PRD和Demo)
- **Phase 4 验证迭代**：使用 `/4-verify-iterate` (自检与优化)

## 文件组织规范

### 草稿阶段 (Drafts)

- **路径规则**: `drafts/YYYY-MM-DD-[模块名]-[主题]/`
- **适用场景**: 需求分析、方案设计、风险评估等中间产物
- **文件示例**:
  - `drafts/2026-02-01-超级运价-需求分析/RDD.md` (需求定义卡片)
  - `drafts/2026-02-01-超级运价-方案设计/Architecture.md` (方案草稿)
  - `drafts/2026-02-01-超级运价-方案设计/RiskReport.md` (风险探测报告)

### 正式版 (PRDs)

- **路径规则**: `prds/[模块名]/`
- **适用场景**: 验证通过的最终交付物
- **文件示例**:
  - `prds/用户中心/PRD.md` (产品需求文档)
  - `prds/用户中心/字段清单.md` (数据模型)
  - `prds/用户中心/demo/` (前端Demo代码)

### 归档 (Archive)

- **路径规则**: `archive/[模块名]/`
- **适用场景**: 需求废弃、大版本迭代后的历史文档
- **触发时机**: 需求取消、系统重构、文档过期

## 文档管理

- PRD文档：`[模块名]PRD.md`
- 字段清单：`[模块名]字段清单.md`
- 业务分析：`[模块名]业务分析.md`
- 草稿文件夹：`YYYY-MM-DD-[主题]/`

## 文档同步规则 ★ 每次回合结束必执行

> **触发条件**：本回合内修改了当前项目的设计文档或原型文件。
> **不适用**：仅对话讨论、仅读取文件、仅修改 memory 的回合。

**执行步骤**：

1. 识别当前模块对应的 5 份文档（见下文），在回合结束前逐一判断是否需要同步
2. 如果需要但本回合来不及做，主动提醒用户并列入下回合待办
3. 在回复末尾以固定格式报告

**5 类文档（按角色，由 AI 在上下文中自动定位实际路径）**：

| # | 类型 | 定位方式 | 何时需要同步 |
|---|------|---------|-------------|
| 1 | 需求背景 | `analysis/` 下的 `需求背景.md` | 洞察/JTBD/业务价值有变化时 |
| 2 | RDD | `drafts/` 下最新的 `RDD.md` | 实体字段、业务规则、流程、AC 有变化时 |
| 3 | 方案设计 | `drafts/` 下最新的 `Architecture.md` | Schema、表结构、ER 图、设计决策有变化时 |
| 4 | PRD | `prds/` 下当前模块的 `PRD.md` | RDD 或 Architecture 的结构性变化影响产品规格时 |
| 5 | HTML 原型 | `demo/` 下该模块对应的 `*.html` | 字段名、实体关系、交互流程有变化时 |

> 如果某类文档尚不存在（如新模块还未创建 PRD），跳过该类并标记为"待创建"。

**回合末尾报告格式**：

```
📋 文档同步：[模块名] 需求背景(已同步) / RDD(已同步) / Architecture(无需) / PRD(待更新) / 原型(无需)
```

**Why**：之前多次出现"只改了 Architecture 但 RDD 和原型没跟"的情况。通过类型而非硬编码路径定位，确保规则跨模块通用。

**关联思考原则**：当用户说"改一下 XX 的字段"时，不要只改被提到的那一处——思考这个字段还出现在哪些文档/实体/流程中，主动检查并同步。
