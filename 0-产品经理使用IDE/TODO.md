# 待办

> 最后更新：**2026-09-12**
>
> **★ 维护约定**：本文件**只放「还没做的」（📋）与「还没定的」（⏳）**。
> **决策/结论一律直接落地到 RDD / 数据设计 / PRD 与原型**（文档与原型就是唯一真相源），**不另设「决策记录」文件**；
> 已完成的过程 → `LOG.md`；变更经过与原因 → 各模块 `drafts/*/变更记录.md`。
> **不要往本文件堆已完成项，也不要写「今日完成了什么」的流水。**

---

## ⏳ 待确认（**2 条**）

| # | 事项 | 我的建议 |
|:--:|:---|:---|
| Q1 | **「批量设置优惠」要不要做、怎么做？** 文档（PRD §4/§5.11、RDD `AC3a`/`AC3b`、数据设计页面清单）都写了「服务组合列表勾选多条 → 批量设置运费优惠 / 泡比优惠」，但**两个原型里 grep「批量」零命中**（列表既无勾选列也无批量按钮）—— 属文档写得比原型早、原型没跟上。优惠入口改成列表操作列的独立弹窗后，批量设置正好是它的自然延伸 | **建议一期做**：列表加勾选列 + 顶部「批量设置优惠」按钮 → 弹出与单条相同的优惠弹窗，但作用对象是全部选中组合（写入时每个组合各存一份规则）。**优惠值 / 对象类型 / 有效期可批量套用，客户特批的目标客户需逐个确认**（不同组合的特批对象往往不同）。若你认为一期用不上、想砍，我就把三处文档措辞一并去掉 |
| Q2 | **优惠的接口口径要不要拆？** PRD §5.11「关联接口」现在写的是「运费优惠 CRUD 通过组合编辑接口 `PUT /api/service-combinations/{id}` 含子表」「附加费优惠 CRUD 通过 `PUT /api/surcharge-rules/{id}` 含子表」。但入口已经独立成弹窗，保存时机与父实体**不再同一次请求**（改优惠不会连带提交组合/规则本体） | **建议拆成独立接口**：`GET/PUT /api/service-combinations/{id}/discounts`（运费 + 泡比）、`GET/PUT /api/surcharge-rules/{id}/discounts`。理由：① 独立弹窗 = 独立保存动作，塞进父实体 PUT 会出现"改优惠顺手覆盖了父实体其他字段"的并发风险；② 二期营销模块迁移时，独立子资源接口更好收敛。**但这属接口口径，得你和开发点头我才改文档** |

新出现的待确认项往本区加，附上我的建议。

---

## 📋 待办

### 超级运价（3 条，均二期项）

- [ ] `surcharge_discount` 父实体泛化为 `fee_rule_type + fee_rule_id`—— 一期仍挂 `surcharge_rule`
- [ ] **二期营销模块独立立项**（**暂缓**，你已指示先不立项）：优惠规则统一配置 + 固定/浮动双性质 + 优惠券 + 优惠落库明细
- [ ] `drafts/超级运价/2026-09-12-数据设计.md` §3.20.4 / §3.20.5 两个已知缺口的二期闭环（`query_log` + `discount_apply_log` + 运单报价快照）

### 财务（4 条）

- [ ] 应收 / 账单细节继续细化（**你说先看当前文档与原型后再提**）
- [ ] 导出能力范围细化（一期仅原型入口占位；具体页面与字段后续单独讨论，Phase 2 随对账中心统一实现）
- [ ] 应付管理细节对齐（Phase 2）
- [ ] 对账中心、毛利分析对齐（Phase 2）

---

## ⏸ 已明确暂缓（已拍板，不必再问）

- **货主端 4 个「菜单已挂、页面待建」**：我的订单 / 草稿箱 / 异常订单 / 发票管理 —— 你的指示是「后面做到这块自然补充」，文档已如实标注、**不计入功能数**
- **客商中心 附件·资质过期阻断续约** —— 附件目前全是图片、系统无法识别有效期，**不做**
- **二期营销模块立项** —— 暂缓（见上方「待办」第 2 条）
