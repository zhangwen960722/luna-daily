# 待办

> 最后更新：**2026-09-23**（收工）
>
> **★ 维护约定**：本文件**只放「还没做的」（📋）与「还没定的」（⏳）**。
> **决策/结论一律直接落地到 RDD / 数据设计 / PRD 与原型**（文档与原型就是唯一真相源），**不另设「决策记录」文件**；
> 已完成的过程 → `LOG.md`；变更经过与原因 → 各模块 `drafts/*/变更记录.md`。
> **不要往本文件堆已完成项，也不要写「今日完成了什么」的流水。**

---

## ⏳ 待确认（**1 条**）

- [ ] **运单审计页的「运单详情」要不要与运单管理逐一致**：当前审计页详情的**结构一致**（概览 / 货箱 / 商品 / POD / 附件 / 费用管理），但除费用外的 5 个 Tab 是**只读精简版**（字段与明细齐全，没有运单管理里的编辑 / 打印 / 抓取等入口）。若你要「完全一模一样」，需要把运单管理详情整套搬进审计页（工作量大，建议单独一轮）。**我的建议**：保持结构一致 + 只读精简，审计页只做「核对 + 纠正费用」。

> 2026-09-14 的三条已全部拍板并落地：① **优惠接口拆三个子资源**（运费优惠 / 泡比优惠 / 附加费优惠各自一个）；② **泛化父实体按「应用层校验 + 禁止级联删除」**（`surcharge_discount` 的 `fee_rule_type + fee_rule_id` 不再声明库级外键）；③ **计价单位跟着规则走**（运费侧取服务组合计费方式、附加费侧取附加费规则计价单位，优惠值量纲 = 元/计价单位）—— 已写入超级运价三份文档与财务 RDD/数据设计/PRD（财务新增 **R55**）。

> 2026-09-16 本轮拍板并落地：① **贷记单 / 核销记录各自独立成菜单**（账单管理收敛为 4 Tab 纯应收页）② **单据类型 `bill_type`**（应收 / 贷记，净额 = 0 不出账）③ **成账按净额轧差、明细允许异号、不按符号拆单**（R72）④ **「结转钱包」= 终态、取消「撤销入账」**（`flow_type = 80` 下线，纠错走运单侧反向流水 + 下期抵扣）⑤ **贷记单照常发送客户 + 客户确认（满 7 天自动确认）后结转**；生成入口唯一、逐张发送 ⑥ **待发送开放编辑**（加流水 / 改账单日 / 留痕）+ **草稿池去勾选列** + **待发送批量发送（R69）** ⑦ **账龄范围 = 已发送**（待发送不计入）。
>
> 2026-09-15 本轮拍板并落地：① 附加费标识**可被多条规则共用** + 打标**幂等去重**（R36，同标识只打一次）② 账单**成账颗粒度 = 运单**（整票成账，避免负数账单）③ 账单**不跨币种折合**（混币种运单拆多张账单）④ 新增 **R63 账单作废与重开**（未核销可作废、流水回池、账单号不回收）⑤ 新增 **R64 账单红冲行与金额构成**（原金额不追溯、差额走下期）⑥ **账期 / 账单日口径统一**（分组键去掉账期、账单日默认自然月末、到期日 = 账单日 + 账期 − 1 天）⑦ **超级运价字段匹配维度新增「出口先查后装模式」**（10 → 11 项）。
>
新出现的待确认项往本区加，附上我的建议。

---

## 📋 待办

### 超级运价（2 条）

- [ ] **二期营销模块独立立项**（**暂缓**，你已指示先不立项）：优惠规则统一配置 + 固定/浮动双性质 + 优惠券 + 优惠落库明细
- [ ] `drafts/超级运价/2026-09-14-数据设计.md` §3.20.4 / §3.20.5 **剩余缺口**的二期闭环：① 同一费用项上**多条优惠叠加的运算顺序明细**；② 浮动优惠（券 / 满减）的**核销载体** —— 均由二期 `discount_apply_log`（M7）承接，与 `query_log` + 运单报价快照一起闭环。（**优惠「是哪条规则给的」已由一期 `ar_flow.source_rule_type/id` 解决，不再是缺口**）

### 财务（5 条）

- [ ] **运单审计页剩余事项**：**反审计留痕**（反审计人 / 时间 / 原因）目前只在数据设计表 11 定义，原型未展示 —— 待补。（「运单审计 ↔ 账单归属互跳」已完成：审计页「关联账单」列可点击跳账单管理并定位该账单。）
- [ ] 导出能力范围细化（一期仅原型入口占位；具体页面与字段后续单独讨论，Phase 2 随对账中心统一实现）
- [ ] 应付管理细节对齐（Phase 2）
- [ ] 对账中心、毛利分析对齐（Phase 2）

---

## ⏸ 已明确暂缓（已拍板，不必再问）

- **「批量设置优惠」** —— **一期不做**（2026-09-14 拍板）：三份文档的相关表述与 AC3a / AC3b 已清掉，不必再议
- **货主端 4 个待建页面**：我的订单 / 草稿箱 / 异常订单 / 发票管理 —— 页面未建，**菜单已按你的指示从原型摘除**（含整个「订单中心」分组与财务中心的「发票管理」），文档如实标注、**不计入功能数**；待页面落地时再挂回菜单
- **客商中心 附件·资质过期阻断续约** —— 附件目前全是图片、系统无法识别有效期，**不做**
- **二期营销模块立项** —— 暂缓（见上方「待办」第 1 条）
- **货主端「线上提现申请」** —— **二期**（2026-09-16 拍板）：一期**客户侧不提供申请入口**，客户线下找财务、财务在员工端「财务流水 → 客户提现」操作（写一条「提现」资金流水 + 扣减钱包余额），客户在「资金流水」只读可见
- **舱单管理「已出发」状态财务操作门禁** —— **不做**（2026-09-23 拍板"太复杂"）：原需求「状态为已出发才可做审计 / 反审计 / 添加费用 / 平摊 / 批量平摊 / 撤销平摊 / 删除等」，产品判定实现太复杂，本轮放弃、未落地
