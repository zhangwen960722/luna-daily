---
trigger: always_on
---

# 项目规则 - 产品经理 AI 工作空间

> 优先级链：AGENTS.md §安全/文件操作/联动检查 > Skill 方法论 > AGENTS.md §格式/命名
> 
> **称呼规则 ★**：每次回复用户，首行必须称呼"小祖宗"。

---

## 🚀 执行速查卡 ★ 强制 — 回答用户第一条消息前先逐条过本章节。不可跳过。

> 本卡 9 个 □。完整方法见下方章节。

### 📥 接到任务时 — 决定读什么文件

```
□ 1. 判断工作模式 → §工作模式判断
      模块名在版本索引里？→ 🎯 已有模块（只读当前模块文件）
      模块名不在？→ 🌐 新模块（读版本索引 + analysis/ 文件列表）
      两个以上模块名？→ 🔗 跨模块（读涉及模块产品文档，选读数据设计）
      都判断不了？→ ❓ 问"针对哪个模块？"

□ 2. 判断任务类型 → §任务→输入文件速查表
      语义匹配用户意图（非精确关键词）。调用 Skill 列有值 → 先调用该 Skill
      命中不了？→ 底线：产品文档 + 数据设计 + 原型

□ 3. 定位文件 → 查 context/版本范围与文件索引.md §一（文档）+ §二（原型）
```

### ✏️ 改文件时 — 改前 + 改后

```
□ 4. 改前：目标文件存在？→ 先读后改。新建？→ 直接创建  §修改前判断
□ 5. 改后（设计文件）：日期前缀 → 今天？+ 变更记录合并追加（同日同主题合并，≤3条）§修改后自检 Step 5+7
□ 6. 改后（设计文件）：联动同步 → 自动检查其余文档，有引用则直接改  §文件联动规则
□ 7. 改后（所有文件）：确认改 N 个 = 实际改 N 个？§修改后自检
```

### 🤖 调用 Agent 时

```
□ 8. prompt 开头附带预读指令（读 AGENTS.md + 对应 Skill + 输入范围定义）§Agent 预读规则
```

### 🏁 对话结束时

```
□ 9. 日常每轮：改文件 + 同步文档 + 记变更记录（**不 commit、不 push、不重建 oss-site**）；**听你说「收工」才跑 §收工四步**
```

---

## 📥 输入 — 接到任务时

### 输入文件范围定义

### 文档职责边界（唯一权威）★

> **同一条内容只在一处是「正文」，别处只许「引用」、禁止复制重复定义**（复制 = 埋"改一处漏一处"的雷）。想看某样东西，按这张表定位，别在文档间来回翻、别猜哪份过期。

| 你要找的内容 | 唯一权威（正文在这） | 别处只能 |
|:---|:---|:---|
| 字段结构 / 类型 / 约束 / 枚举值（TinyInt / 常量名） | 数据设计 §核心实体 Schema | 产品文档 §3 字段表只写「字段名 + 业务含义」，不逐字段重复 |
| 业务规则 / 校验逻辑 / 计算公式 | **产品文档 §3 各流程内（R 编号按流程分段）** | 数据设计**不写规则正文** |
| 状态机 | 产品文档 §4 状态机 | 数据设计字段备注只留状态值作引用 |
| 业务流程 / 端到端链路 | 产品文档 §2 业务全景 / §3 各流程 | 数据设计只画表级数据流，不画业务流程图 |
| 功能清单 / 页面清单 / 菜单 | 产品文档 §5 功能清单与页面映射（轻索引） | 数据设计不重复列 |
| 页面交互 / 弹窗 / 字段校验 | 原型（HTML） | 产品文档 §5 只挂原型链接，不写交互 |
| 表结构 / 接口契约（字段级） | 数据设计 | 产品文档只提「接口名 + 一句话用途」 |

**三条铁律**：

1. **正文只在一处**：规则 / 字段 / 枚举 / 流程，只允许一处是完整正文，其余一律「见 X §Y」。
2. **业务规则只在产品文档（跟流程走）**：校验、约束、计算公式，写在产品文档 §3 各流程内（R 编号按流程分段），数据设计不写规则正文。数据设计尤其**只装数据**（表 / 字段 / 索引 / 外键 / Schema / 枚举值 / 表级数据流），**不装**业务规则、业务流程、菜单 IA、风险报告、设计决策。
3. **AC 只在产品文档**：验收标准正文在产品文档 §10 验收标准总览。

### 工作模式判断 ★

> 加载文件前先判断模式。三种信号自动判断，无需用户显式声明。

**信号来源**：① 用户消息中的模块名（查 `context/版本范围与文件索引.md` §一）> ② IDE 打开文件路径中的 `drafts/[模块名]/` > ③ 对话历史中的模块上下文。

| 条件 | → 模式 | 加载范围 |
|------|--------|---------|
| 模块名不在版本索引 + 用户明确要新建 | 🌐 **新模块** | 版本索引 + `analysis/` 文件列表（可读的读，不可读的列文件名）；选读：从版本索引列所有模块实体摘要 → 识别可能数据交互的模块 → 问用户确认后加载产品文档 |
| 模块名在版本索引 + 仅提 1 个 | 🎯 **已有模块** | 按 §任务→输入文件速查表，限定当前模块文件 |
| 提到 ≥2 个模块名 | 🔗 **跨模块** | 涉及模块的产品文档；选读涉及模块的数据设计 |
| 无法提取模块名 | ❓ **询问用户** | "针对哪个模块？" |

### 任务 → 输入文件速查表

> 确定模块后，按任务类型决定读该模块的哪些文件。

| 任务类型 | 典型话术（语义匹配） | 调用 Skill | 必读 | 补充读取 |
|---------|---------------------|-----------|------|---------|
| 需求分析 | 提出新想法、讨论方案、梳理流程："加个功能""能不能""怎么做""分析""梳理""优化""讨论""看看怎么改" | `product-doc-writer` | 产品文档、需求背景 | 版本索引、原型 |
| 数据建模 | 问表结构、字段类型、实体关系："建表""ER图""字段类型""实体关系""主键""外键""索引""怎么设计" | `data-modeler` | 产品文档 | 版本索引 |
| 边界探测 | 担心遗漏、异常、并发、安全："漏洞""边界""异常""并发""万一""会不会""检查一下""漏了什么""有问题吗" | `edge-case-detector` | 产品文档、数据设计 | 已有流程图 |
| 写产品文档 | 方案定了要出正式文档："写产品文档""写PRD""出文档""定稿""归档""给开发的""整理成文档" | `product-doc-writer` | 产品文档、数据设计、原型 | — |
| 生成/修改原型 | 要可见页面或改页面："画页面""生成Demo""原型""做页面""改页面""界面" | `frontend-designer` | 产品文档、数据设计、两端 index.html + 同端 ≥2 参考页 | — |
| 字段/实体变更 | 增删改字段、实体、表："加字段""删字段""改类型""加实体""拆表""字段名""枚举值""加个列" | — | 产品文档、数据设计 | 原型对应页面 |
| 流程/规则变更 | 改状态流转、业务规则、校验逻辑："状态流转""流程改""规则变了""R01""校验规则""条件改成""改成...状态" | — | 产品文档、数据设计 | 原型对应页面 |
| 页面调整 | 改现有页面的样式、布局、按钮、交互、文案："改颜色""改样式""加按钮""弹窗""布局""间距""文案""换成" | — | 原型目标页 + 同端 ≥2 参考页 | 产品文档、数据设计 |
| 新增页面 | 要一个全新页面或功能模块："加页面""新建页""新页面""加个XX页""做个XX功能" | — | 产品文档、数据设计、两端 index.html | 同端 ≥2 参考页 |
**多任务命中**：合并所有命中行的文件（取并集）。**补充读取判断**：涉及 UI 变更 / 必读信息不足 / 用户要求对照 Demo 时触发，其他可跳过。

### 文件定位步骤

1. **判断模式**（§工作模式判断的 4 模式表）
2. **确定目标**：提取模块名 + 具体页面。模块未知 → 问；模块已知页面未知 → 问"涉及哪个页面？"
3. **查版本索引**：`context/版本范围与文件索引.md` → §一 定位文档路径；涉及原型 → §二 定位 HTML。若版本索引不存在（全新项目）→ 直接询问用户确认文件位置
4. **取最新日期**：同名多日期取最新。**若按日期拼接不存在**→ 列出实际文件后取最新，不要假设是今天
5. **读文件**：按模式 + 任务速查表加载


---

## ✏️ 文件操作 — 改文件时

### 修改前判断 ★

| 条件 | 操作 |
|------|------|
| 目标文件**不存在** | 新建 |
| 目标文件**已存在** | **先读后改**，在原文件上修改，不复制不另存 |
| 文件更新后 | 重命名日期前缀：`2026-06-01-产品文档.md` → `2026-06-07-产品文档.md` |

**原型 HTML 特殊规则**：页面文件始终在原文件上改，不改文件名。新增 → 新建 HTML + 注册到 `index.html`。删除 → 删 HTML + 从 `index.html` 移除。

**★ 设计文档正文只写「当前状态」（2026-09-12 新增）**：产品文档 / 数据设计 / 需求背景的**正文里不要写变更注记** —— 不写 `★ 变更（日期）`、不写 ~~删除线旧口径~~、不写「原『X』已废弃」这类对照。**变更经过与原因一律写 `drafts/[模块名]/变更记录.md` 与 `LOG.md`**；文档正文只描述**现在是什么样**，让读者不必区分"哪半句还算数"。
> **唯一例外**：**面向读者的设计理由**（回答"为什么这样设计"，而非"改了什么"）—— 这类内容**改写成正常陈述句保留**，不带「变更」字样。例如「字段未沿用 `discount_value`，因该名称已被更早的废弃字段占用」属设计理由，保留；而「原 `discount_amount` 已更名为…」属变更叙事，删掉。
> **判断标准**：一句话如果是**给未来读者理解当前设计**用的 → 留（改写为陈述句）；如果是**记录过去发生过什么** → 移去变更记录。

**★ 业务规则编号唯一化**：**R 编号只在产品文档**（研发可验收单元 + 缺陷回归的引用锚点），写在 §3 各流程内、**按流程分段**（R[流程号].[序号]）；数据设计不写规则正文、不编号。**AC 正文只在产品文档**（§10 验收标准总览），编号 `ACx` / `AC-Px`。
> **存量**：各模块 RDD 里与 PRD 撞号的 R 编号，随本次「RDD+PRD 合并为产品文档」一并清理（R 编号收敛为产品文档按流程分段）。

**文本替换防坑 ★（2026-09-12 新增）**：本仓库 Markdown **换行风格不统一**——`drafts/超级运价/变更记录.md`、`drafts/客商中心/变更记录.md`、`drafts/系统设置/*` 是 **CRLF**，其余多为 LF。用脚本做字符串替换时，**含 `\n` 的锚点在 CRLF 文件上匹配不到，而 `String.replace` 匹配失败不抛错 → 脚本会"假成功"（打印成功、实际零改动）**。规避：① 锚点改用 `/…\r?\n/` 正则或先探测 `eol`；② **写入后必须回读验证**（`content.includes(新内容)`）；③ 收尾 `git status` 核对目标文件**确实出现在变更列表里**。

### 修改后自检 ★

> 本回合文件操作全部完成后、回复用户前执行。

1. 回顾确认要改的内容（文件 + 改动点）
2. 核对实际改了哪些文件
3. 对比：确认的 ⊇ 实际的？
4. 遗漏 → 立即补
5. **文件索引同步 + 变更记录**：修改了设计文件（需求背景/产品文档/数据设计）→ 文件名日期更新为今天 + 同步更新 `context/版本范围与文件索引.md`。**新建了文件**（含 analysis/ 下的沟通文档等）→ 同步更新版本索引的对应章节。检查同模块文档内交叉引用链接是否失效（旧文件名→新文件名）。原型 HTML 不改前缀（豁免）

   **变更记录追加规则**：`drafts/[模块名]/变更记录.md`
   - 先读变更记录，检查当天是否已有同主题条目
   - 同日期 + 同主题（如"运价查询""工作台"）→ 合并到已有条目，不新增行
   - 新增条目时一句话概括业务变更（做什么 + 为什么），不加实现细节（不写 v2.0/v2.1 版本号、不列具体字段名）
   - 每天每个模块 ≤ 3 条
6. **联动检查**：改了设计文件（需求背景/产品文档/数据设计/原型）→ 执行 §文件联动规则。联动产生的修改不触发新一轮自检
7. 输出：

```
📅 日期前缀：产品文档(已更新至MM-DD) / 数据设计(已更新至MM-DD) / 原型(豁免)
📝 变更记录：[模块名] 变更记录.md | [变更内容一句话]
🔍 自检：确认改 N 个，实际改 N 个，全部命中 / ⚠️ 遗漏 X 个已补
```

### 文件联动规则 ★

> **触发**：本回合修改了设计文件（需求背景/产品文档/数据设计/原型/index.html）。仅改变更记录/LOG/TODO/版本索引/AGENTS.md 等元文件时不触发。

**硬规则**：改了任意一份设计文件 → 自动检查同模块其余设计文件中是否引用了被改内容（字段名、状态枚举、实体名、业务规则编号、页面引用等）→ 有则直接同步，不询问。

**产品文档全篇复查 ★ 按需触发**：**默认只改受影响的节**（省 token）；**只有你要求复查时**（"全篇复查""整体复查""通盘过一遍"），才通读全篇（核心洞察 / 用户故事 / 业务全景与流程图 / 数据流图 / 实体字段表 / 业务规则 / 功能清单 / AC / 分期与理想方案），把与新口径不符的一并刷新。

| 我改了 | 优先检查 |
|:---|:---|
| 需求背景 | 产品文档 §1 需求定义 |
| 产品文档 | 数据设计 §字段清单/§实体清单、原型字段 |
| 数据设计 | 产品文档 §3 实体字段表、§4 状态机、原型字段 |
| 原型 | 产品文档 §5 功能清单（页面链接）、数据设计 §IA设计 |

同步完成后，回复里用**一行**说明「产品文档 / 数据设计 已联动更新」即可 —— **不必逐节列点、不必逐份报改了哪一节**。

---

## 🤖 调用 Agent 时

### Agent 预读规则 ★

> 主对话调用 Agent 执行任务时，prompt 开头必须附带以下指令。Agent 不得跳过。

```
## 执行前必读

1. 读取**仓库根 `AGENTS.md`**（项目规则唯一真相源）
2. 根据任务类型，读取对应的 Skill 文件：
   - 需求分析 / 产品文档撰写 → `.agent/skills/product-doc-writer/SKILL.md`
   - 数据建模 → `.agent/skills/data-modeler/SKILL.md`
   - 边界探测 → `.agent/skills/edge-case-detector/SKILL.md`
   - 原型生成/修改 → `.agent/skills/frontend-designer/SKILL.md`
3. 根据任务类型查阅 AGENTS.md §任务→输入文件速查表 + §工作模式判断，确定需要加载的模块文件
4. 按 Skill 定义的输出模板和规范执行，不得凭记忆
5. 完成后执行 AGENTS.md §修改后自检 + §文件联动规则
```

---

## 🏁 对话结束时

### 收工四步 ★（**听你说「收工」才执行**）

| # | 步骤 | 操作 |
|:---|:---|:---|
| 1 | **LOG.md** | 索引表加日期行 + 插入 `<a id>` 锚点摘要条目（先读后插） |
| 2 | **TODO.md** | **只维护「还没做的」与「还没定的」** —— ⚠️ **不要往里堆已完成项**：已完成的过程记录写 `LOG.md`，结论/口径写各模块文档或 `变更记录.md`；`TODO.md` 里也不要写「今日完成了什么」的流水。**决策与结论一律直接落地到产品文档 / 数据设计 / 原型，不另设「决策记录」文件**（避免两处维护、口径不同步） |
| 3 | **git** | `git add` 变更文件 → `git commit` → `git push` |
| 4 | **oss-site** | 运行 `node oss-site/build.js` 全量重建文档站点（自动：MD→HTML + 本页目录 + 交叉引用链接 + 删旧日期文件） |

> **★ 时机（2026-09-14 立规）**：**日常每轮**只做三件事 —— ① 改原型 / 文档；② 同步设计文档（产品文档 / 数据设计 / 版本索引）；③ 追加 `drafts/[模块]/变更记录.md`。**不 commit、不 push、不重建 oss-site**；**只有听到「收工 / 下班 / 今天到这 / 明天继续」才跑上面四步**。说「更新文档」「同步」「定稿」只是让我改文件，**不算收工**。

> 变更记录在 §修改后自检 Step 5 即时追加，不在此处。

---

## 📁 规则文件约定

> **规则只有一份正文 = 仓库根 `AGENTS.md`**（本文件自带 `trigger: always_on`，会被自动加载）。
> 历史遗留的 `.claude/CLAUDE.md`（Claude 入口副本）与 `.agent/rules/project-rule.md`（旧指针）**已删除**，不再维护第二份，避免"改一处漏一处"。
> 若某个工具**只认 `CLAUDE.md`**：新建 `.claude/CLAUDE.md`，内容写一行 `@../AGENTS.md` 即可，**不要复制正文**。

---

## 版本范围与文件索引

完整文件索引见 `0-产品经理使用IDE/context/版本范围与文件索引.md`
